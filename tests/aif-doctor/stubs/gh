#!/usr/bin/env bash
# Stub: gh — returns a controlled SHA for repos/<repo>/git/refs/heads/<branch>.
# The real helper calls: gh api "repos/$REPO/git/refs/heads/$BRANCH" --jq '.object.sha'
# This stub ignores all args and echoes $STUB_REAL_SHA (set by the test harness).
set -euo pipefail
echo "${STUB_REAL_SHA:-}"
