{
  "name": "@rules-as-tests-aif/runtime-bridge",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "license": "FSL-1.1-ALv2",
  "description": "Runtime bridge for rules-as-tests — opt-in dispatch target for the CC-native runtime-bridge-dispatch hook: CLI commands invoked when a file opts in via the `<!-- bridge: auto -->` marker, keeping automation logic harness-agnostic.",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
