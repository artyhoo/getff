# generated by getff — do not edit by hand
# getff cargo-deny config (ecosystem-wiring W4). Dependency-level bans are the surface clippy.toml
# CANNOT express (render-clippy.ts refuses dep-graph selectors FF7001: "dependency-level bans live
# in cargo-deny [bans]"). Enforce as a FAILING gate with `cargo deny check bans`.
#
# getff delivers this as a starter config: the [bans] table is the getff-owned surface; the
# advisories/licenses tables are left for you to configure per your policy (getff does not opine on
# them). Delete this file to opt out of the cargo-deny lane.
[bans]
multiple-versions = "warn"
# getff dependency bans — extend per your convention, e.g.:
# deny = [
#   { name = "openssl", reason = "use rustls — the convention forbids native-tls" },
# ]

[advisories]
# Configure the RustSec advisory DB gate per your policy (getff leaves this to you).

[licenses]
# Configure the license allow/deny policy per your policy (getff leaves this to you).
