# generated by getff ruff backend v0 — do not edit by hand
# (hand-authored for the T-DTZ-A counter fixture: this fixture's ruff.toml bans
# `datetime.datetime.utcnow` via TID251 to verify that with `select = ["DTZ005", "TID251"]`,
# `datetime.datetime.utcnow()` fires EXACTLY ONE diagnostic — our TID251 — and NO DTZ003
# (which would double-report under `select = ["DTZ"]`). NOT in the firing.test.ts:97-110
# self-application block (which only checks the 3 main dirs against render([TID251, TID253])).
[lint]
select = ["DTZ005", "TID251"]

[lint.flake8-tidy-imports.banned-api]
"datetime.datetime.utcnow".msg = "datetime.datetime.utcnow() is deprecated and returns a naive datetime; use datetime.now(timezone.utc)"
