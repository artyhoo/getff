linters:
  disable-all: true
  enable:
    - forbidigo
linters-settings:
  forbidigo:
    forbid:
      - p: 'os\.Getenv'
        msg: 'Read configuration through the injected config accessor, never os.Getenv directly'
