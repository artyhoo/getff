// Legitimate non-boundary consumer source — should be visible to general find walks
// (any_glob_matches' no-token branch) but does not exercise boundary machinery.
export const shared = 'consumer-fixture-shared';
