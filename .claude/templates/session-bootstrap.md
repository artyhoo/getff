# Session bootstrap — your project's per-turn anchor

<!--
  Fill the digest block below with YOUR project's essentials. Everything between the two
  `digest` markers is injected into the AI's context EVERY turn — in the main session AND
  into every dispatched subagent — via `.claude/hooks/inject-project-digest.sh`.

  Keep it SHORT: it costs tokens on every turn. Good things to put here:
    • the project's one-line goal / what it is;
    • 2-4 load-bearing conventions the AI must not violate;
    • where to look first (key files / docs).

  Zero-setup default: the block below ships EMPTY, so nothing is injected until you fill it.
  Leave it empty (or delete this file) to inject nothing.
-->

<!-- digest:start -->
<!-- digest:end -->
