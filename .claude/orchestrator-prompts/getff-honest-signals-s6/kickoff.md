<!-- bridge-profile: Z.AI GLM-5.2 SDK -->
<!-- scope: stage S6 of the getff-honest-signals umbrella — FINAL stage, carries umbrella closure. Thin dispatch adapter — semantics are BINDING in the sources below, NOT restated here. -->

# getff-honest-signals-s6 — `inject-matching-rule` honest no-op + umbrella closure

> **Stage 6 of 6 — FINAL.** One stage = one PR onto `staging`.
> **Binding sources (read both, in order):**
> 1. `.claude/orchestrator-prompts/getff-honest-signals-meta-launch/kickoff.md` §4 «Stage 6 / S6» + §5 AI-traps.
> 2. `docs/superpowers/specs/2026-07-23-getff-any-stack-closure-design.md` §8.6 — BINDING for semantics.
>
> **Branch:** `fix/getff-honest-signals-s6`. **Base:** `staging`. **Precondition:** S5 merged.

## §1 The defect

`inject-matching-rule` ships to consumers who have **no `.claude/rules/` corpus**. With no corpus there is
nothing to match, so the hook does nothing — **forever, and silently**. The consumer sees an installed,
wired hook and has no way to learn it can never fire. A permanent silent no-op is the purest form of this
umbrella's defect class.

## §2 What to build — DECIDED «minimal» (do not redesign)

Corpus absent → the hook **reports ONCE, loudly**. Subsequent runs stay quiet.

- **Never a permanent silent no-op** — the consumer must learn, at least once, that the hook has no corpus.
- **Never per-invocation spam** — a hook that shouts on every edit gets muted, which recreates the silence.

The "once" needs somewhere durable to record that it has already reported; choose the mechanism consistent
with how sibling hooks in this repo already handle session-scoped state, and say which precedent you followed.

## §3 ⚠ All delivery copies — byte-identity gate

The hook exists in **three** delivery copies. Edits land in **ALL** of them:

1. the `packages/core` SSOT copy,
2. the `.claude` dogfood copy,
3. the `plugin/` twin.

Enumerate the live paths yourself (T3) and quote the byte-identity check. A fix in one copy with the others
left behind is a shipped inconsistency — and the twins are regenerated by a pre-commit hook, so verify rather
than assume.

## §4 «Works» — the paired fixture (binding)

RED pre-fix, GREEN post-fix, **both runs quoted** in the PR body.

- **Shape:** a consumer without `.claude/rules/` → **a single loud report**, and **subsequent runs quiet**.
  Both halves are asserted: one run proves it reports, a second run proves it does not repeat.
- **T-HS-A (binding):** assert **exit code / observable output presence first**, wording second.
- Keep a case proving the hook still fires normally when a corpus **is** present — the no-op path must not
  have disabled the real behaviour.

## §5 Umbrella closure (this PR only — S6 is the last stage)

Write `.claude/orchestrator-prompts/getff-honest-signals/done.md` per the [CLAUDE.md](../../../CLAUDE.md)
«Umbrella closure convention» — exact schema:

```text
# getff-honest-signals — DONE
- Final PR: #<num>
- Closed: <YYYY-MM-DD>
- Summary: <one-line>
```

Then sync to the coordination CANON:

```bash
cp .claude/orchestrator-prompts/getff-honest-signals/done.md \
   ~/.claude-coordination/rules-as-tests-aif/getff-honest-signals/done.md
```

`done.md` is a tracked exception in `.gitignore` (alongside `kickoff.md`) — commit it.

## §6 Park-don't-guess contract (aif agent — non-negotiable)

**aif agent — fork discipline:** On ANY genuine fork or ambiguity (two defensible implementations, an undecided design choice, a missing spec detail that changes behaviour) — **do NOT pick.** Park it as a question (set the task to `manualReviewRequired` / `blocked_external` with the fork stated as «Option A → consequence X / Option B → consequence Y») and **stop that task.** Proceed only on the unambiguous parts. Guessing a fork to "keep moving" is the failure this whole loop exists to prevent.

**Stage-specific park triggers (do NOT guess these):**

- The **scope of "once"** — once per session, per install, or per corpus-absent detection — if no sibling-hook
  precedent in this repo determines it. The three differ in observable behaviour; park with them stated.
- The **report wording**, if it must satisfy both "loud enough to be noticed" and this repo's language
  discipline (machinery in English) without an existing precedent to copy.

## §7 AI-traps active

See [.claude/rules/ai-laziness-traps.md §2](../../rules/ai-laziness-traps.md). **Active traps for this stage: T3, T7, T14, T15, T19, T21.**

- **T3** — enumerate the three delivery-copy paths live; do not trust this kickoff's description of them.
- **T7 / T14** — the theme is a silent no-op reported as fine; do not let the "quiet on subsequent runs" half
  turn back into permanent silence.
- **T15 (load-bearing here)** — this is the **last** stage: run the whole umbrella's own checks against the
  framework itself, including S1's fixed mutation runner, and report the result in the PR body.
- **T19** — own cold-review before handoff.
- **T21** — the backward-check must enumerate sibling surfaces the diff did NOT touch — other shipped hooks that
  can silently no-op when their input corpus is absent — never restate the PR.

**Domain-specific traps (NOT in the canonical catalogue):**

- **T-HS-A (umbrella)** — asserting on message TEXT instead of output presence/exit code.
- **T-S6-A (this stage)** — **editing one delivery copy and letting the pre-commit twin regeneration "handle
  the rest".** It may regenerate one twin and not the third; the fix then ships inconsistently. Counter: §3 —
  enumerate all three, quote the byte-identity check.
- **T-S6-B (this stage)** — **closing the umbrella on stage-count rather than on evidence.** `done.md` asserts
  the umbrella is finished. Before writing it, verify all six stage PRs actually merged
  (`gh pr list --search "is:merged head:fix/getff-honest-signals-s<N> base:staging"` for N=1..6) and quote it.
  Writing `done.md` because "this is stage 6" is the umbrella's own defect class applied to its closure.

## §8 Anti-scope

- Do NOT touch any other stage's surface (mutation runner logic, lychee/pre-push, datetime rules, CI templates, refresh).
- Do NOT re-open or amend earlier stages' decisions.
- Do NOT add npm deps.

## §9 PR body — §1.7 likely REQUIRED

Hook edits typically reach `.claude/skills/**`, `agents/**`, `packages/core/templates/**`, or `.claude/rules/**`
(§4b path list) → the PR body **MUST** then carry both `### §1.7 Forward-check applied` and
`### §1.7 Backward-check applied` (H3, literal «applied», ≥40 non-whitespace chars and ≥1 `path.ext:N`
citation each). Run the meta-launch kickoff §4b pre-flight grep before `gh pr create`.

State in the PR body that this PR **closes the umbrella**, and quote the six-stage merge verification from T-S6-B.
