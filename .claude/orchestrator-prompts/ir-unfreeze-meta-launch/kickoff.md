# KICKOFF — ir-unfreeze (meta-launch)

> **Type:** execution-build, 4 sequential stages (generated by `/pipeline ir-unfreeze` on 2026-07-21).
> **Origin:** plan-currency check + launch-table from `/pipeline` skill.
> **Deliverable:** a **relational** `ConventionNode` IR (owner-resolved **Option B** — structured relational rule-object / discriminated union) carrying `not:`/`has:`/`all:`/`any:` composition, unfreezing the ~53pt of census-gated conventions the flat scalar-`params` IR cannot precisely express, WITHOUT breaking the 24 consumers or the byte-locks. Delivered across S1→S2→S3→S4, each one PR onto staging, each gated on the prior merging.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + stage gates).
2. `.claude/orchestrator-prompts/ir-unfreeze/kickoff.md` — the umbrella kickoff (binding scope, §6 STOP lines, §4 traps).
3. `docs/meta-factory/research-patches/2026-07-21-ir-unfreeze.md` — R-phase (§0 ground truth + 24-consumer table, §2 blast-radius, §4 backward-compat).
4. `docs/superpowers/specs/2026-07-21-rule-tests-surface-design.md §9 R3` — the owner resolution record (UNFREEZE + Option B).

**Owner precondition — RESOLVED (do not re-open):** OWNER-FORK-1 = **UNFREEZE**; IR shape = **Option B** (structured relational rule-object / discriminated union). Recorded on staging in the spec §9 R3 (merged #1071, 2026-07-21) + the umbrella kickoff header. **No stage may re-decide the shape** (umbrella §6 STOP lines stand). Verified live @ `origin/staging` `59ac50515`.

---

## §1 Inputs

- **Plan-currency check:** Plan is CURRENT (as of 2026-07-21 `59ac50515`). Owner precondition resolved (#1071); R-phase merged (#1061); no open PRs / in-flight branches for this umbrella; the dup-detect `POTENTIAL_DUPE` against #1061 is the umbrella's own R-phase, not a duplicate deliverable.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/ir-unfreeze/kickoff.md`
- **Volatile facts re-verified live @ `59ac50515` (T3):** IR still FROZEN (`ir/types.ts` `params: Record<string, string | number>`, freeze marker `:3`); consumer count = **24** (matches R-patch `8747d8453`); kickoff + owner-record both present on `origin/staging`; byte-lock `synthesizer/to-node.test.ts:129-137` unchanged.
- **Cross-umbrella:** `ir-unfreeze` is UNBLOCKED (spec §9 R3, R5) and dispatches **in parallel with `ecosystem-wiring`** (independent scopes). No inter-umbrella gate.
- **Stale items:** None.

---

## §2 Launch-table

Launch table — ir-unfreeze (as of `59ac50515`):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| S1 | execution-build (IR shape + schema mirror + grammar gate + backward-compat proof) | Mode A (PAIR / Mode-SDD) | Yes | 1 | — | M |
| S2 | execution-build (astgrep relational render + 1 more backend + RED firing test) | Mode A | No | 2 | — | M |
| S3 | execution-build (ruff + cargo refuse + deterministic byte-lock/drift/snapshot re-baseline) | Mode A | No | 3 | — | M |
| S4 | hardening (24-consumer exhaustiveness sweep + `Prior-art:` trailer + SSOT row + `done.md`) | Mode A (PAIR / Mode-SDD) | Yes | 4 | — | S |

**Sequencing:** strictly linear — S1 → S2 → S3 → S4. No parallelism (S2 renders the shape S1 lands; S3 re-baselines after S2; S4 sweeps after S3). Each stage = one PR onto staging, branched from staging.

**Mode rationale:** each stage is a single-owner execution-build → **Mode A** (one inline session per stage). **S1 and S4 carry SDD/PAIR review** — S1 is the load-bearing IR unfreeze (frozen core `ir/types.ts` + JSON-schema mirror + grammar gate + 24-consumer compat proof); S4 is the 24-consumer exhaustiveness sweep + the capability commit. S2/S3 are single-backend-focused; the **mandatory Phase -1 cold-review between every stage** (§7) covers their review need without a dedicated in-stage reviewer.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

Branch convention: each stage worker pushes head `ir-unfreeze-s<N>` (or its worktree branch) and opens a PR base=staging.

### Stage 1 → Stage 2

Stage 2 dispatch waits for the Stage 1 PR merged to `staging`:

```bash
gh pr list --search "is:merged head:ir-unfreeze-s1 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: non-empty JSON (the S1 IR-shape PR). If empty → **HALT**. Do NOT dispatch Stage 2 (S2 renders a shape that does not exist yet).

### Stage 2 → Stage 3

```bash
gh pr list --search "is:merged head:ir-unfreeze-s2 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: non-empty JSON (the S2 astgrep-render PR). If empty → **HALT**.

### Stage 3 → Stage 4

```bash
gh pr list --search "is:merged head:ir-unfreeze-s3 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: non-empty JSON (the S3 backends + re-baseline PR). If empty → **HALT** (S4 sweeps consumers against the completed render surface).

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7 below). Re-verify volatile `file:line` (24-consumer count, byte-lock lines, schema ceiling) live against `origin/staging` at each stage entry — staging moves (umbrella §0 re-verify obligation, T3).

---

## §4 Sub-wave dispatch instructions

**All four stages** target `packages/core/**` (`ir/`, `backends/`, gates, synthesizer) → **§4b §1.7 PR-body mandate applies to every stage** (path-triggered).

- **S1 — grammar + IR shape change + backward-compat (smallest honest slice).** Land the owner-chosen **Option B** relational shape in `ir/types.ts` + `ir/convention-node.schema.json` + `ir/gates/grammar.ts`. Prove the legacy scalar node stays a valid instance (byte-lock `synthesizer/to-node.test.ts:129-137` green; no forced migration — R-patch §4). Grammar gate MAY add an FF-code for malformed relational params (Option B trees are deep-ajv-validatable — R-patch §2a). **No backend renders relational yet** — S1 is IR + gate + compat proof only.
- **S2 — astgrep render + one more backend.** Teach `render-astgrep.ts` to emit the relational `rule:` YAML at the `:26` insertion point; fire **RED** on a scratch consumer for one real relational convention from the census's 8-convention delta (e.g. a `require-via-ban` case) and clean on the good example (§3 «works» discipline — `ast-grep scan` exit 1 RED / exit 0 clean). Plus ONE more backend's degrade-vs-refuse decision wired (ruff or npm), reusing the `selectorClass`-refusal pattern (FF7001, `render-astgrep.ts:14-15`).
- **S3 — ruff + cargo + byte-lock updates.** Complete the remaining backends' degrade-vs-refuse (ruff/cargo/clippy — mostly FF7001 refuse for relational, R-patch §2c). Re-baseline the drift/snapshot/byte-lock tests **deterministically** via the in-file regen commands (`python-templates-drift.test.ts`, `snapshot.test.ts`, `to-node.test.ts` — R-patch §2d). The regen is deterministic but the `to-node` lock is order-sensitive — do not reorder params keys.
- **S4 — hardening + `done.md`.** Full consumer sweep (all **24** type-check + exhaustiveness under the Option B discriminated union — re-count live at ship, staging moves). Land the capability-commit `Prior-art:` trailer (relational IR = **ADAPT** of ast-grep's `any/all/not/has` vocabulary, not a novel BUILD) + a new SSOT row in `prior-art-evaluations.md`. Write the umbrella `done.md` per CLAUDE.md «Umbrella closure convention».

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to ALL Workers below — self-evaluate)

Workers whose target file matches ANY of the following paths MUST include in the **PR body** (not just commit message):

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

> **This umbrella:** every stage touches `packages/core/**` (`ir/`, `backends/`, synthesizer) and S4 touches `docs/meta-factory/prior-art-evaluations.md`. The §1.7 mandate is IN SCOPE for all four stages. The paths list above is the CI substance-gate trigger — `packages/core/**` outside `principles/`/`templates/` is not itself in the trigger list, but S4's `prior-art-evaluations.md` edit IS, so S4 unconditionally needs the §1.7 body; S1–S3 authors self-evaluate against their actual touched paths and include the §1.7 body whenever any listed path is hit.

**Required PR body sections (verbatim shape — CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- `build-first-reuse-default.md` — relational IR = ADAPT of ast-grep any/all/not/has; verdict + SSOT row. file:line evidence: `<path>:<line>`
- `no-paid-llm-in-ci.md` — relational-params semantic validation routes session-side / cold-agent, never a CI LLM gate. <verdict>
- `doc-authority-hierarchy.md` — <what was checked>. file:line evidence: `<path>:<line>`

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of the sibling ConventionNode consumers + byte-lock/drift surfaces — NOT a restatement of this stage's own diff (T21)>

- `<sibling consumer / byte-lock / drift surface>` — <SWEPT-CLEAN(evidence file:line) | GAP-FOUND(action)>. file:line evidence: `<path>:<line>`
- <repeat per sibling surface from R-patch §2 population>
```

**Hard rules (each has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2.** CI regex matches `^### §1\.7 ...`.
2. **Word «applied» is required** in each heading.
3. **≥40 non-whitespace chars in EACH section body** (awk-counted, post-strip).
4. **≥1 file:line citation per section** matching `[^[:space:]]+\.[a-z]+:[0-9]+`.
5. **Mechanical-maintenance escape hatch:** `### §1.7 Skipped: <rationale ≥60 chars>` only for purely structural PRs (snapshot regen, rename) with no discipline surface. Does NOT apply to any stage here (all carry a real IR/backend discipline call).

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # must be ≥2
```

**Backward-check discipline (T21 — load-bearing for this umbrella):** the sweep ENUMERATES the sibling ConventionNode consumers (R-patch §2 population) + the byte-lock/drift surfaces and verdicts each SWEPT-CLEAN / GAP-FOUND — it does NOT restate the stage's own diff. Delegate the class-sweep to a cold sub-agent (`agents/backward-sweep-auditor.md`), never hand it the PR.

> **Worktree setup:** Stages are sequential — a single worktree per stage suffices. Use `claude -w ir-unfreeze-s<N>` (CC-native, hook auto-symlinks `node_modules`) or the portable `bash scripts/create-worktree.sh ir-unfreeze-s<N>`. If a Worker reports `npm`/test failures on first run AND neither ran, set up the worktree manually per `parallel-subwave-isolation.md §1`.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue.

**Active canonical traps for ir-unfreeze:** T2, T3, T11, T12, T14, T15, T20, T21.

- **T2** — designing the relational shape ≠ landing it; every stage RUNS its firing test / byte-lock / type-check, never claims «would pass».
- **T3** — every `file:line` (24 consumers, byte-locks `to-node.test.ts:129-137`, schema ceiling `convention-node.schema.json:26-28`) re-verified live against `origin/staging` at ship, never from this doc or the R-patch (staging moves).
- **T11** — the relational IR is an ADAPT of ast-grep's `any/all/not/has` vocabulary; the BFR §3 consult + SSOT row land WITH the S4 capability commit, before any «I propose this shape».
- **T12** — do not scope the backend degrade-vs-refuse from training-data recall of ast-grep/ruff/cargo; read the actual render backends live.
- **T14** — the rust expressibility is a stated GAP (R-patch §3), NOT «clean». The unfreeze is python-data-only; report no rust coverage that was never measured.
- **T15** — self-application: this umbrella unfreezes the IR for CONSUMER conventions; it does NOT make the framework's own rules self-generating (explicit non-goal).
- **T20** — no «works» / «Option B renders» verdict without the same-turn evidence run quoted (the census fired exit codes, the R-patch §1/§2 file:line).
- **T21** — the S1..S4 backward-checks ENUMERATE the sibling ConventionNode consumers + byte-lock/drift surfaces (R-patch §2 population); they do NOT restate the stage's own diff. Cold sub-agent does the sweep.

**Domain-specific traps (ir-unfreeze-specific, NOT in canonical catalogue):**

- **T-LG-D — «unfreeze the IR to make researched rules expressive» (unilateral scope creep on a frozen artifact).** Even here, a STAGE may not unfreeze beyond the owner-resolved Option B, and may not silently widen the shape to «make S2 render». The owner resolved UNFREEZE+Option B; the stages IMPLEMENT that, they do not extend it.
- **T-IRU-A — «pick / widen one Option because it's less work / more elegant» (design-fork pre-emption).** The Option-A-vs-B fork is CLOSED (Option B recorded). A stage that reverts to A «because the tree is more work», or extends B's shape beyond what R3 recorded, has re-opened a closed owner decision (umbrella §6 STOP). Surface any shape ambiguity; do not resolve it in-stage.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without the enumeration above = T7 violation. This kickoff enumerates T-numbers AND adds two domain-specific traps.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass `principle 12` (AI-laziness-traps format validation). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer dispatch (between stages)

Before dispatching Stage N+1, invoke:

```text
Agent: Phase -1 cold-review for ir-unfreeze Stage N
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

Reviewer reads the Stage N diff (`git diff staging...ir-unfreeze-s<N>`) + this kickoff's Stage N acceptance criteria; emits GO / REVISE / STOP with BLOCKER/MAJOR/MINOR classification. For any strategy fork: «DECISION-NEEDED: <one-line>. Option A → X. Option B → Y. Maintainer decides.» — does NOT pick strategy, does NOT edit files, does NOT skip surface review because «CI is green» (T19). See `reviewer-discipline.md §2`.

**Load-bearing for S1:** the reviewer independently confirms the legacy scalar node stays byte-identical (the `to-node.test.ts:129-137` lock) and that Option B did NOT force-migrate any of the 24 consumers (umbrella §6 STOP).

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/pipeline/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 In plain words` block whose content follows the orchestrator-checkpoint substance shape (names sub-waves / AC items / file:line / REPORT-traces / dispatch-state), NOT the per-turn generic substance the end-of-turn hook already enforces. If the block is verbatim-copyable from `end-of-turn-reminder.sh` reminder text → `#two-prompts-drift`.

---

## §8 Stop conditions

- Any `gh` command returns error on a stage-gate check → HALT, escalate (do not guess merge state).
- Stage gate command returns empty AND Stage N was expected merged → HALT, report.
- Phase -1 reviewer returns STOP → HALT, surface to maintainer.
- Any sub-wave returns a BLOCKER finding → HALT the stage; surface before proceeding.
- **Any temptation to re-decide Option A vs B, add an IR field beyond Option B, force-migrate a consumer, or claim rust coverage** → umbrella §6 STOP violation; HALT and surface.
- The byte-lock `to-node.test.ts:129-137` goes RED for a legacy scalar node → HALT (a params-shape change reordered keys).
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this kickoff.
- Do NOT re-decide the IR shape (Option B is owner-resolved; umbrella §6 STOP).
- Do NOT force-migrate the 24 consumers (relational is additive-opt-in; R-patch §4).
- Do NOT claim rust coverage without a real fired rust census (R-patch §3 GAP).
- Do NOT route relational-params semantic validation to a CI LLM gate (`no-paid-llm-in-ci.md`).
- Do NOT add npm deps (substrate purity).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).

---

## §10 See also

- `.claude/orchestrator-prompts/ir-unfreeze/kickoff.md` — the umbrella kickoff (binding scope, STOP lines, traps).
- `docs/meta-factory/research-patches/2026-07-21-ir-unfreeze.md` — R-phase (§0 ground truth + 24-consumer table, §2 blast-radius, §4 backward-compat).
- `docs/meta-factory/research-patches/2026-07-13-lg-s1-expressibility-census.md` — the DATA (40/93, +53.3pt relational delta, 3 require-via-ban cases).
- `docs/superpowers/specs/2026-07-21-rule-tests-surface-design.md §9 R3` — owner resolution (UNFREEZE + Option B).
- `packages/core/ir/types.ts` (`:3` freeze, `:32-41` node) + `packages/core/ir/convention-node.schema.json` (`:6`/`:26-28` scalar ceiling) — the FROZEN artifact this umbrella unfreezes.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue · `.claude/rules/build-first-reuse-default.md §3` — BFR consult for the S4 capability commit.
- `.claude/rules/parallel-subwave-isolation.md §1` — worktree isolation · `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
