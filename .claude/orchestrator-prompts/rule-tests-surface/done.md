# rule-tests-surface — DONE

- Final PR: #1093
- Closed: 2026-07-22
- Summary: rule-tests skill surface shipped end-to-end — S1 write-half protocol + thin trigger (#1089), S2 enrichment sidecar + firing test (#1090), S3 evidence-regeneration runbook (#1091), S4 deps-hash staleness seam (#1092), S5 guarded consumer pre-push arm + shipped firing runner + /getff routing (#1093).

## Residuals / follow-ups

Gap-log (live-generation done.md precedent). Each with its PR-of-record; a whole-work corrective PR (post-#1093) closed several — status noted inline.

- **Sidecar shape validation in the shipped runner** — PR-of-record #1093. **CLOSED (ww-fixes):** the shipped `run-rule-tests-firing.sh` + the pre-push arm now mirror the S2 loader `validateRuleTestsSidecar` up front (a `badd` typo / empty `bad[]` / mis-shape is BROKEN MATERIAL → exit 1, not a silent green), with regression tests.
- **ruff per-rule isolation promotion trigger** — PR-of-record #1093. **OPEN:** the ruff lane isolates at ruff-code granularity (sidecar key = the reported code, e.g. `TID251`); true per-getff-rule isolation needs the sidecar to carry per-rule ban metadata. Promotion trigger: a sidecar carrying ban metadata → true per-rule ruff isolation.
- **ruff / cargo committed framework fixtures** — PR-of-record #1093. **PARTIALLY CLOSED (ww-fixes):** the ruff + cargo lanes now have end-to-end coverage via synthetic-smoke tests (sandbox-seeded delivered-shape config + sidecar). REMAINS OPEN: no *checked-in* framework fixture for ruff/cargo (astgrep is the only committed-fixture lane); a per-lane committed fixture is the residual.
- **npm mutation-runner root resolution at consumer depth** — PR-of-record #1093. **CLOSED (ww-fixes):** `run-generated-rule-mutation.sh` resolves `REPO_ROOT` via `git rev-parse --show-toplevel` (the fixed `../../..` pointed above a consumer's `scripts/` root → npm-lane theatre); committed consumer-depth regression test added.
- **Monorepo-subpackage (non-hoisted) consumer → npm arm loud-skips** — PR-of-record #1093. **OPEN:** when tsx/eslint are not resolvable at the repo-root `node_modules/.bin` (nested pnpm/workspace layout), the npm arm loud-skips rather than fires. Accepted-degradation; promotion trigger = a real consumer incident.
- **Aliased-import coverage gap** — PR-of-record #1090. **OPEN (routed to /rule-research):** single-rule isolation proves a rule fires on the sidecar samples, not that the *research→rule* generation covered aliased-import bypass forms — that is a rule-generation (rule-research) concern, not a test-material one.
- **Shape-validator inline mirrors (loader ↔ runner ↔ pre-push probe)** — PR-of-record: the ww-fixes corrective PR. **OPEN (revisit trigger):** `validateRuleTestsSidecar` (rule-tests-sidecar.ts, not shipped) is inline-mirrored in `run-rule-tests-firing.sh` `_validate_sidecar` and `pre-push.ts` `SIDECAR_SHAPE_PROBE`; current behavior is test-locked, but loader-schema CHANGES are guarded only by `keep in sync` comment anchors. Revisit trigger: a mirror-drift incident, or a loader schema change → either add a shared-corpus loader↔mirror equivalence test, or flip to Option A (ship the TS module + firing-runners) per OF-S5-runner. Related test asymmetry: the runner's own copy of the cargo opt-in line is not test-pinned (only the pre-push copy is).
- **Sidecar seeding-from-pairedExamples unexercised by tests** — spec §3; PR-of-record #1090. **OPEN:** v0 ships the write/repair *verification* half; `seedRuleTestsSidecar` (bad←negative / good←positive) has unit coverage of the inversion but no end-to-end generation→seed→fire test. Deferred (v0 scope = write/repair verification).
