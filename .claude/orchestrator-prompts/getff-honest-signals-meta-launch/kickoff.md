<!-- bridge-profile: Z.AI GLM-5.2 SDK -->
<!-- host-verify: none — orchestration document, no executable deliverable of its own: it emits the launch-table, the stage gates and the dispatch instructions, and every line of code this umbrella ships is produced under a per-stage kickoff (getff-honest-signals-s1..s6), each of which carries its own host contract. A contract here would either duplicate the stage contracts or assert nothing. The §3 gate commands ARE host commands, but they are per-stage preconditions run by the dispatching session between stages, not an acceptance check on this file's own diff. -->

# KICKOFF — getff-honest-signals (meta-launch)

> **Type:** I-phase-large / PAIR (Mode-SDD), 6 sequential stages (generated by `/pipeline getff-honest-signals` on 2026-07-24).
> **Origin:** plan-currency check + launch-table from `/pipeline` skill.
> **Deliverable:** six deceptive-signal defects (spec §1 wall 7 a–f) killed, each with its paired red→green fixture and quoted tool output; umbrella closed with `done.md`.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + stage gates).
2. `.claude/orchestrator-prompts/getff-honest-signals/kickoff.md` — the umbrella kickoff (S1–S6 semantics).
3. `docs/superpowers/specs/2026-07-23-getff-any-stack-closure-design.md` §8 (W5.1–W5.6) + §1 wall 7 — **BINDING** for semantics.
4. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot (**STALE**, see §1).

**Tier routing:** the umbrella kickoff carries `<!-- bridge-profile: Z.AI GLM-5.2 SDK -->` (Tier 1 — plan + implement + review all on the executor profile). Marker verified against the live profile list at generation time: exact-match on `Z.AI GLM-5.2 SDK`, 1 of 3 profiles — unambiguous under `AifHandoffBackend._resolveProfileId` (exact-match short-circuit landed in PR #1116, `packages/runtime-bridge/src/AifHandoffBackend.ts:139-142`). Re-verify before dispatch if profiles were renamed.

---

## §1 Inputs

- **Plan-currency check:** CURRENT for this umbrella; DRIFT-1 + DRIFT-2 below are non-blocking (as of 2026-07-24 `3bbc58b8d`).
  - Umbrella kickoff IS on `origin/staging` (`git ls-tree origin/staging .claude/orchestrator-prompts/getff-honest-signals/` → `kickoff.md`) — [kickoff-staging-placement.md §1](../../rules/kickoff-staging-placement.md) satisfied.
  - No open/merged PR for this umbrella; no ahead-commits on any branch; no aif task in the queue. In-flight: CLEAR.
  - Ordering-rule sibling `getff-any-stack-trace` (umbrella kickoff §0): NO branch, NO PR, NO aif task → this umbrella's python-lane stages (S3/S4) may land first, as the ordering rule requires.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/getff-honest-signals/kickoff.md`
- **Stale items:**
  - **DRIFT-1** — `docs/meta-factory/wave-sequencing-plan.md §0` last reconciled 2026-06-29; ~30 PRs merged since (#809 → #1120). Its «NO active in-flight build-umbrella» claim and its `getff-to-prod U4–U17 stubs` row predate the whole `getff-any-stack` closure program (#1100). **Non-blocking** for this umbrella (the binding source is the spec, not the wave plan). Proposed correction: a separate §0 reconcile PR — do NOT bundle here ([CLAUDE.md `PR strategy`](../../../CLAUDE.md)).
  - **DRIFT-2 (anchor line-drift, T3)** — the umbrella kickoff cites `packages/core/hooks/pre-push.ts:1273` as the lychee section owner. Live: the `owner: 'both'` registry entry is `pre-push.ts:1438`, the section fn is `lycheeSection` at `pre-push.ts:1318`. The other five anchors re-verified live and hold: `run-generated-rule-mutation.sh:176` (WARN + `continue` skip) / `:227` (`PASS — all generated rules ≥…`); both datetime rules `:9` (`pattern: "datetime.now($$$ARGS)"` / `"datetime.datetime.now($$$ARGS)"`); `ruff.toml:9` (utcnow msg); `github-actions-ci.yml:15-18` (`branches: [main]` ×2). **S2 uses the live line, not the kickoff's.**

---

## §2 Launch-table

Launch table — getff-honest-signals (as of `3bbc58b8d`):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| S1 — mutation runner: skipped ≠ green | execution-build | Mode-SDD (PAIR) | Y | 1 | — | S |
| S2 — consumer push not blocked by framework dangling refs | execution-build | Mode-SDD (PAIR) | Y | 2 | — | M |
| S3 — datetime false positive, BOTH rules | execution-build | Mode-SDD (PAIR) | Y | 3 | — | M |
| S4 — CI template targets consumer's real default branch | execution-build | Mode-SDD (PAIR) | Y | 4 | — | M |
| S5 — refresh reconciles renames + stale companions | execution-build | Mode-SDD (PAIR) | Y | 5 | — | M |
| S6 — `inject-matching-rule` honest no-op + umbrella closure | execution-build | Mode-SDD (PAIR) | Y | 6 | — | S |

**Routing derivation (§2.5 Step 5):** `TYPE=I-phase-large` (`classify-work.sh`: LOC=95, SURFACES=24) · `scope_decided=TRUE` (spec §8 binding — «All six semantics are PRE-DECIDED; stages are expansion, not design») · `SURFACES≥2=TRUE` · **`parallel_safe=FALSE`** → **PAIR / Mode-SDD**.

**ATTN — parallel sibling column is empty by rule, not by analysis.** The umbrella kickoff declares no `Parallel-with` for any stage, so the [§3 Step 2](../../skills/pipeline/SKILL.md) sequential default applies. Two independent reasons confirm it is also correct on the merits:

1. **S3 ∩ S4 collide on generated artefacts** — both regenerate `tests/install-sh/baselines/*` (`SNAPSHOT_MODE=capture bash tests/install-sh/snapshot.sh`). Parallel branches would conflict on every fingerprint file ([git-conflict-merge-forward.md §4](../../rules/git-conflict-merge-forward.md)).
2. **S2 / S4 / S5 all touch `setup.d/`** (delivery: `transform_internal_refs` · default-branch substitution · refresh reconcile).

S1 and S6 ARE structurally disjoint from everything else (`run-generated-rule-mutation.sh`; `inject-matching-rule.sh` ×3 copies). **Opting them into parallel is the maintainer's call, not this kickoff's** — the umbrella kickoff would need an explicit `Parallel-with` declaration first. Do NOT infer it.

**Auto-detection note (honest):** `launch-table-generator.sh` reported **0 auto-detected sub-waves** — its pattern does not match the umbrella kickoff's `- **S1 — …**` bullet shape. The six rows above are read from the umbrella kickoff §1 by hand, which is the [§3 Step 2](../../skills/pipeline/SKILL.md) judgment path (the helper emits a skeleton; the kickoff §1 is the authoritative decomposition). This is NOT the §9 dogfood HARD FAIL — that gate is scoped to the skill's own first-live dogfood invocation.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

> **⚠ FIXED 2026-07-25 — the commands below now carry the real prefix.** History: on 2026-07-24 (during
> the S1 run) this section was found to be written against `fix/getff-honest-signals-s<N>`, which **aif never
> produces** — it names task branches `feature/<umbrella>-s<N>-<short-taskid>` (S1 landed on
> `feature/getff-honest-signals-s1-8cab19`, PR #1140). A warning was added here, but **the five gate commands
> were left broken**, so every one of them returned an empty result that reads identically to «nothing merged»
> — this umbrella's own defect class, inside its own orchestration, with a prose warning as the only guard.
> Relying on the reader to mentally substitute the prefix is attention, not a mechanism
> ([attention-is-not-a-mechanism.md §1](../../rules/attention-is-not-a-mechanism.md)).
>
> The commands are now corrected in place and run verbatim: S1/S2/S3 resolve to #1140/#1145/#1150; S4/S5
> return empty because they are genuinely unmerged. The signal is truthful in **both** directions.
>
> **Still binding:** an **errored or mis-queried** gate is a HALT, never an empty gate (§6, §8). If a gate
> that should be green comes back empty, resolve the real branch from the task —
> `curl -s "$RUNTIME_BRIDGE_AIF_URL/tasks/<taskId>"` → `branchName` — and re-query before concluding anything.

Branch convention (what aif actually produces): `feature/getff-honest-signals-s<N>-<short-taskid>`. The
`head:` filter below matches on prefix, so the task-id suffix does not need to be known in advance.

### Stage 1 → Stage 2

```bash
gh pr list --state merged --search "head:feature/getff-honest-signals-s1 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: non-empty JSON. If empty → halt. Do NOT dispatch Stage 2.

### Stage 2 → Stage 3

```bash
gh pr list --state merged --search "head:feature/getff-honest-signals-s2 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

### Stage 3 → Stage 4

```bash
gh pr list --state merged --search "head:feature/getff-honest-signals-s3 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

**Extra gate (S3→S4 only, baseline collision):** after S3 merges, confirm the baselines on `staging` are the S3-regenerated ones before S4 regenerates on top:

```bash
git fetch origin staging -q && SNAPSHOT_MODE=compare bash tests/install-sh/snapshot.sh
```

Expected: N pass / 0 fail on a clean `origin/staging` checkout. Non-zero fail → S3's regen did not land cleanly; halt and surface.

### Stage 4 → Stage 5

```bash
gh pr list --state merged --search "head:feature/getff-honest-signals-s4 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

### Stage 5 → Stage 6

```bash
gh pr list --state merged --search "head:feature/getff-honest-signals-s5 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

**Re-probe before EVERY stage dispatch (umbrella kickoff §0 ordering rule):**

```bash
gh pr list --state open --search "any-stack-trace" --json number,headRefName --limit 10
git branch -r | grep -i "any-stack" || echo "(clear)"
```

Non-empty on a python-lane-touching branch while S3/S4 are pending → STOP and surface (the ordering rule requires this umbrella's python-lane stages to land first).

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7 below).

---

## §4 Sub-wave dispatch instructions

One stage = one PR onto `staging` = one worker session. **Do NOT collapse stages** (umbrella kickoff §1, binding).

**Every stage, without exception (umbrella kickoff §2):**

- The paired fixture runs **RED on pre-fix behavior** and **GREEN post-fix**. Both runs' actual tool output is quoted in the PR body. Prose-only claims are rejected at Phase -1.
- **T-HS-A counter (binding):** every fixture asserts the **EXIT CODE first**, message wording second. A fixture that greps new wording but not the exit code passes even when the gate still exits 0 — that is the umbrella's own defect class reproduced inside its fix.
- S3 / S4 additionally: **fresh-install smoke green** (template deliveries).

**Per-stage scope (semantics are BINDING from spec §8 — this is dispatch input, not a design restatement):**

- **Stage 1 / S1** — `packages/core/synthesizer/run-generated-rule-mutation.sh`: skipped rules counted + reported (`N skipped — NOT green`); `tested=0` with rules present can NOT print `PASS`. Live anchors: `:176` (`WARN … skipping rule` + `continue`), `:223-227` (`OVERALL_FAIL` gate → `PASS — all generated rules ≥${MIN_KILL}%`). Polarity wording per the neighboring `pre-push.ts:919-938`. Fixture: a rule whose selector does not fire on its negative input → runner shows the skip verdict and a non-PASS exit.
- **Stage 2 / S2** — two-part, DECIDED: (i) lychee section scoped to consumer-authored changed md, framework-shipped paths excluded — live anchors `pre-push.ts:1318` (`lycheeSection`) + `:1438` (`{ id: 'lychee', owner: 'both', … }`), **not** the kickoff's stale `:1273`; (ii) delivery rewrites framework-internal refs completely via `transform_internal_refs` coverage extension (`setup.d/lib.sh:52`), closing the 248-file `README.md#why-this-exists` class. Fixture: consumer-clean diff + a shipped file carrying a dangling framework ref → push passes.
- **Stage 3 / S3** — DECIDED narrow-to-naive, BOTH rule files (`getff-no-datetime-now.yml:9`, `getff-no-datetime-datetime-now.yml:9`): zero-arg `now()` stays RED; `datetime.now(timezone.utc)` goes GREEN; message text stays consistent with `ruff.toml:9`. Fixtures both ways for BOTH files. Template edits shift install fingerprints → regenerate baselines (`SNAPSHOT_MODE=capture bash tests/install-sh/snapshot.sh`) **in the same PR**.
- **Stage 4 / S4** — delivery substitutes the consumer's actual default branch into `packages/core/templates/python/github-actions-ci.yml:15-18` (`branches: [main]` ×2) + sweep sibling templates for hardcoded `[main]`. Fixture: install on a `master`-default repo → delivered workflow triggers match `master`. Same baseline-regen obligation as S3.
- **Stage 5 / S5** — DECIDED: refresh removes the superseded framework-owned `.claude/skills/rules-as-tests/` dir when `skills/getff/` is delivered; for consumer-owned `.lintstagedrc` it **PRINTS a migration offer** (diff + instruction) and never overwrites. Fixtures: pre-rename install → single skill dir after refresh; stale lintstagedrc → offer printed, file untouched.
- **Stage 6 / S6** — DECIDED minimal: corpus absent → `inject-matching-rule` reports **ONCE, loudly**; never a permanent silent no-op. Fixture: consumer without `.claude/rules/` → single loud report, subsequent runs quiet. Hook edits land in **ALL** delivery copies (`packages/core` SSOT + `.claude` dogfood + plugin twin; byte-identity gate). **Umbrella closure: write `done.md` in this final PR** ([CLAUDE.md `Umbrella closure convention`](../../../CLAUDE.md)).

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to ALL Workers below — self-evaluate)

Workers whose target file matches ANY of the following paths MUST include in the **PR body** (not just commit message):

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

**Stages hitting the path list: S3, S4, S5** (`packages/core/templates/**`), and **S6** if the hook edit reaches a shipped skill/rule surface. S1 and S2 do not match — §4b is path-scoped; if your stage does not match, ignore it (or use the escape hatch below when a mechanical-only diff is caught in a mixed PR).

**Required PR body sections (verbatim shape — CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- `doc-authority-hierarchy.md` — <what was checked>. file:line evidence: `<path>:<line>`
- `phase-research-coverage.md §1.12` — <what was checked>. file:line evidence: `<path>:<line>`
- `build-first-reuse-default.md` — <verdict + rationale>
- `no-paid-llm-in-ci.md` — <verdict>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the new rule's scope>

- `<existing-artefact-name>` — <how this PR interacts; supersedes / extends / orthogonal>. file:line evidence: `<path>:<line>`
- <repeat for each touched artefact>
```

**Hard rules (each one has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2 (`##`).** The CI gate's awk regex matches `^### §1\.7 ...` — H2 silently fails while looking plausible in rendered Markdown.
2. **Word «applied» is required.** Headings without «applied» fail the regex.
3. **≥40 non-whitespace chars in EACH section body** (awk-counted, post-strip).
4. **≥1 file:line citation per section** matching regex `[^[:space:]]+\.[a-z]+:[0-9]+`.
5. **Mechanical-maintenance escape hatch:** `### §1.7 Skipped: <rationale ≥60 chars>` shortcuts the gate when the PR is purely structural (snapshot regen, typo fix, rename) and no discipline call applies. **Caution for S3/S4:** a baseline regen bundled with a real template semantics change is NOT «purely structural» — do not reach for the escape hatch there.

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # must be ≥2
```

If any of the four checks fails → fix the body before pushing.

**Backward-check warning (T21):** the backward-check must enumerate **sibling surfaces the diff did NOT touch**, not restate what the PR did. For this umbrella the class is «a check that reports success while having skipped its work» — sibling surfaces include the other five stages' gates plus any other `WARN … continue` / `owner: 'both'` skip path. A surface list equal to the diff's own file list is non-conformant by format.

---

## §4c Autonomous aif-handoff dispatch — park-don't-guess contract

> **When this section is live:** the §10 report offered «autonomous dispatch via aif-handoff» for this umbrella AND the maintainer accepted. If every Worker below runs in a maintainer-pasted CC tab, §4c is inert — skip it. **If any stage is dispatched via `tsx packages/runtime-bridge/src/cli/dispatch.ts <this-kickoff>`, §4c is MANDATORY.**

**Bridge state (re-verified 2026-07-24 at `042e7f98e`, second `/pipeline` invocation — supersedes the generation-time reading):** aif **IS reachable** — `GET localhost:3009/runtime-profiles` returns 3 profiles, `/health` returns `{"status":"ok"}`, `/tasks` returns a live queue. The three env vars are **now SET** in the dispatching shell (`RUNTIME_BRIDGE_MODE=aif-handoff`, `RUNTIME_BRIDGE_AIF_URL=http://localhost:3009`, `RUNTIME_BRIDGE_AIF_PROJECT_ID=441c1c0c-…`) — they were unset at generation time, so `dispatch.ts` would then have fallen back to `ManualBackend`. Re-verify in YOUR shell before dispatch (they live in `~/.zshenv`, not `~/.zshrc` — non-interactive shells skip `.zshrc`).

**⚠ QUEUE CAPACITY — check before dispatching (this is the blocking precondition, not the env):** the live queue holds **3 non-terminal tasks** (`2 plan_ready` + `1 review`) against a coordinator limit of **3**. `plan_ready` **holds a slot and pausing does not release it** — only `DELETE` frees one; the truth is in the coordinator log on `capacity`, NOT in `activeTaskCount` (which reads 0 while the queue is full). A dispatch into a full queue sits in `backlog` **forever, silently** — the exact deceptive-signal class this umbrella exists to kill. Run `/aif-doctor` first; dispatch only once a slot is genuinely free, or use the paste-tab path instead.

**Why this exists (design §1 honest limitation, verified `coordinator.ts:398-476` + `reviewGate.ts`):** aif-handoff agents have **no mid-implementation "pause and ask" primitive**. They implement — _guessing_ on any ambiguity — then auto-review post-hoc. Auto-close fires when the review finds _no blocking findings_ — a bar that means "review found no blockers", NOT "a human is sure it's right". A genuine **design fork is not recognised as a question** — aif just picks and proceeds. So **without the levers below, aif decides forks wrong, silently.**

**Lever 1 — conservative aif config (set on the bridge/env BEFORE dispatch):**

```bash
export AGENT_MAX_REVIEW_ITERATIONS=1        # not converged in 1 pass → hand to human, don't keep guessing
export AGENT_AUTO_REVIEW_STRATEGY=closure_first
export AGENT_SKIP_REVIEW=false
```

**Lever 2 — park-don't-guess instruction (addressed to the aif agent, verbatim):**

> **aif agent — fork discipline (non-negotiable):** On ANY genuine fork or ambiguity (two defensible implementations, an undecided design choice, a missing spec detail that changes behaviour) — **do NOT pick.** Park it as a question (set the task to `manualReviewRequired` / `blocked_external` with the fork stated as «Option A → consequence X / Option B → consequence Y») and **stop that task.** Proceed only on the unambiguous parts. Guessing a fork to "keep moving" is the failure this whole loop exists to prevent.

**Umbrella-specific park triggers (do NOT guess these):** the message wording for S3's narrowed rules where `ruff.toml:9` and the astgrep message must stay consistent; S4's «what IS the consumer's real default branch» resolution when the repo has no remote; S5's migration-offer text format. Each is a wording/UX fork the spec does not fix — park, do not invent.

**Lever 3 — the resolve loop reviews aif's DECISIONS, not only its open questions:** the maintainer Approves / Requests-changes each completed task, so "questions" = open forks **+** aif's autonomous decisions awaiting verification (trust-but-verify).

**Pre-dispatch gate (run before `dispatch.ts`):**

```bash
grep -qi 'park it as a question' .claude/orchestrator-prompts/getff-honest-signals-meta-launch/kickoff.md && echo "LEVER-2 OK"
[ -n "$AGENT_MAX_REVIEW_ITERATIONS" ] && echo "LEVER-1 OK"
[ -n "$RUNTIME_BRIDGE_AIF_PROJECT_ID" ] && echo "PROJECT-ID OK"
```

Any of the three missing → STOP; do not dispatch autonomously.

**Egress gate (mandatory after `status=done` or `status=verified`):** aif does NOT push or open PRs by design.

```bash
npx tsx packages/runtime-bridge/src/cli/harvest.ts <taskId> --base staging
```

Anti-pattern `#autonomous-done-no-harvest`: skipping this leaves the work permanently in the container.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue.

**Active canonical traps for getff-honest-signals:** **T3, T7, T14, T15, T19** (carried forward verbatim from the umbrella kickoff §3), plus **T21** added at meta-launch level.

- **T3 (plausible finding without verification)** — every anchor re-verified live at stage time; quoted tool output mandatory. Already fired once at generation time: DRIFT-2 above (`pre-push.ts:1273` → live `:1318` / `:1438`). Assume the other anchors drift too by the time your stage runs.
- **T7 (following the prompt literally instead of reasoning adversarially)** — the umbrella's THEME is skip-reported-as-green; do not reproduce it in your own fixtures. A fixture that "passes" because the check silently skipped IS the defect.
- **T14 (conflating «no findings» with «no problem»)** — same shape as T7 here: a green run at low coverage is «coverage insufficient to conclude», not «fixed».
- **T15 (self-application skipped)** — S1's fixed runner MUST be run against the framework's own generated rules as the self-application check, and the result reported.
- **T19 (handoff without own cold-QA)** — own adversarial cold-review of each diff before handoff; CI green ≠ design review.
- **T21 (backward-check restates instead of sweeping)** — see §4b warning. Delegate the sweep to `agents/backward-sweep-auditor.md` handing it only the change CLASS, never the diff.

**Domain-specific traps (getff-honest-signals-specific, NOT in canonical catalogue):**

- **T-HS-A (from the umbrella kickoff)** — asserting on message TEXT instead of polarity/exit-code: a fixture that greps the new wording but not the exit code passes even if the gate still exits 0. Counter: every fixture asserts the EXIT CODE first, wording second.
- **T-HSM-B (meta-launch)** — **baseline-regen collision treated as a merge nuisance.** S3 and S4 both run `SNAPSHOT_MODE=capture`. The tempting move when S4 conflicts is to hand-merge the fingerprint hunks. Those files are generated; hand-merged content is authoritative-looking garbage. Counter: take either side, regenerate, verify with `SNAPSHOT_MODE=compare` ([git-conflict-merge-forward.md §4](../../rules/git-conflict-merge-forward.md)).
- **T-HSM-C (meta-launch)** — **stage-gate skipped because «S1 and S6 are small».** Volume=S is an argument about LOC, never about the gate. Six sequential stages create six chances to dispatch N+1 before N merged; the two smallest stages are where the rationalization lands. Counter: §3 gates are real `gh pr list` runs, evaluated every time.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation. Kickoff authors MUST enumerate T-numbers AND add ≥1 domain-specific trap.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass `principle 12` (AI-laziness-traps format validation):

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

Second-order self-application (the umbrella's own theme): **this kickoff's stage gates must not skip-and-report-green.** If a `gh pr list` gate returns empty because `gh` errored rather than because nothing merged, that is the umbrella's defect class occurring inside its own orchestration — treat a gate error as HALT, never as "no results, proceed".

---

## §7 Phase -1 reviewer dispatch (between stages)

Before dispatching Stage N+1, invoke:

```text
Agent: Phase -1 cold-review for getff-honest-signals Stage N
Role: reviewer
Skill: requesting-code-review
```

Reviewer verifies, at minimum: (a) the fixture was genuinely RED pre-fix — the PR body quotes a failing run, not an assertion that it would fail; (b) the fixture asserts the exit code (T-HS-A); (c) for S3/S4, the baseline regen is in the same PR as the template change.

Reviewer discipline: surfaces strategy forks as DECISION-NEEDED (option A → consequence X / option B → consequence Y). Does NOT pick strategy. See `reviewer-discipline.md §2`.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 In plain words` block whose CONTENT follows the orchestrator-checkpoint substance shape. Substance spec: see SKILL.md §10.3a.

**Anti-duplication discipline:** the block content names stages / fixture verdicts / file:line / PR numbers. It does NOT restate per-turn personal-reasoning prose — that is the end-of-turn hook's job (`#two-prompts-drift` per `.claude/rules/dual-implementation-discipline.md §4`).

---

## §8 Stop conditions

- Any `gh` command returns an error while evaluating a stage gate → HALT (see §6 second-order note). An errored gate is NOT an empty gate.
- Stage gate returns empty AND Stage N was expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Any stage returns a BLOCKER finding → halt the stage; surface before proceeding.
- `getff-any-stack-trace` appears in-flight on a python-lane-touching branch while S3/S4 are pending → halt (umbrella kickoff §0 ordering rule).
- `SNAPSHOT_MODE=compare` fails on clean `origin/staging` after S3 → halt before S4.
- Any temptation to edit `~/.claude/skills/orchestrator/` → violation; stop.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this kickoff.
- Do NOT collapse stages — one stage = one PR (umbrella kickoff §1, binding).
- Do NOT fix DRIFT-1 (`wave-sequencing-plan.md §0` reconcile) inside any stage PR — separate concern, separate PR ([CLAUDE.md `PR strategy`](../../../CLAUDE.md)).
- Do NOT pull W5.7 (foreign-dir scan) in — carved out to `getff-foreign-scan-triage` (Tier 2).
- Do NOT finalize project strategy — surface genuine forks to maintainer.
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per no-paid-llm-in-ci.md + BFR-default).

---

## §10 See also

- Spec (BINDING): `docs/superpowers/specs/2026-07-23-getff-any-stack-closure-design.md` §8, §10.
- `.claude/orchestrator-prompts/getff-honest-signals/kickoff.md` — the umbrella kickoff.
- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source (STALE, DRIFT-1).
- `.claude/skills/pipeline/SKILL.md` — generating skill.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/git-conflict-merge-forward.md §4` — generated-vs-semantic conflict triage (T-HSM-B).
- `.claude/rules/evidence-regeneration.md` — baseline regen on template edit (S3/S4).
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
