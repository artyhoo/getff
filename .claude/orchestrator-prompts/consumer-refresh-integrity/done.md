# consumer-refresh-integrity — DONE
- Final PR: #1509 (R3; stage chain R1 #1503 → R2 #1505 → R4 #1504 → R3 #1509, kickoff PR #1489)
- Closed: 2026-08-21
- Summary: the four consumer-delivery integrity defects from the 2026-08-19 issue sweep are closed — R1: refresh-baseline manifest + warn/preserve divergence guard in refresh_safe/copy_safe (issue 1481); R2: generate_eslint_barrel preserves consumer-added barrel entries (1481 casualty 2); R3: check-ask-files.sh + run-local-ci-sweep.sh delivered per RI-4 measured breadth with do_refresh mirrors, T-CRI-C acceptance arms, snapshot regen and full wiring (1482, 1485); R4: consumer-aware digest — inject-session-bootstrap.sh existence-checks every rendered path citation at render time, degrades to rule NAME, framework tree byte-identical, plugin twin regenerated (1484 + absorbed 1486). Host-verify fence complete (8/8 incl. R2 backfill — R2's session had skipped its §5 append; backfilled dispatcher-authorized in #1509).
- Follow-ups (parked, out of scope by kickoff decisions): (1) RI-2 consequence — a consumer that vendored a script BEFORE its delivery has no manifest entry, so the first --refresh silently overwrites (fresh install silently skips); measured + pinned in deliver-gate-scripts.test.sh arm (b2), candidate issue. (2) npm-tarball ships zero root scripts/ entries (packages/core package.json files allowlist) — tarball installs get neither gate script; packaging fix vs relocation fork parked per RI-4. (3) Pre-existing copy_safe --force clobber class (setup.d/lib.sh:359-376) widened by the new deliveries. (4) An alternative R3 delivery breadth analysis (env|factory via 40-configs, arguing the pre-push ask section is maintainer-only) from a parallel session is preserved verbatim at r3-parallel-session-40configs-wip.diff in this (gitignored) umbrella dir — unadopted, re-opens RI-4.
