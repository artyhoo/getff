# KICKOFF — launch-preannounce-track (meta-launch)

> **Type:** multi-stage execution umbrella (S1–S6), generated by `/pipeline launch-preannounce-track` on 2026-07-11.
> **Origin:** plan-currency check + launch-table from `/pipeline` skill.
> **Deliverable:** consumer-matrix acceptance gate (S2) + pre-push structural owner-split (S3) + cargo honest demo (S4) + zcode probe (S5) + npm pivot-lite PREP (S6), calibrated by a foreign-repo run (S1).
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

This meta-launch is a routing/launch-table overlay. The **binding scope statement is the umbrella kickoff already on staging**: `.claude/orchestrator-prompts/launch-preannounce-track/kickoff.md`. Read in this order:

1. This meta-kickoff (launch-table + stage gates + dispatch 1-liners).
2. `.claude/orchestrator-prompts/launch-preannounce-track/kickoff.md` — the binding scope (S1–S6 definitions, §0 S.0 re-verify rule, §6 AI-traps).
3. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.

**The one rule that overrides everything (umbrella kickoff §0):** the P0 hotfix track already landed on staging (#923 #932 #939 #942 #943 #946 #947 #949 #951 #957). Do NOT redo it. Every stage starts with the **S.0 rule**: re-verify each premise against fresh `origin/staging` before coding; if already fixed, record + skip forward (trap T-LPT-B).

---

## §1 Inputs

- **Plan-currency check:** CURRENT (as of 2026-07-11 e69a0cc2f). P0 landed; S1–S6 pending; no in-flight collisions on any stage surface; umbrella open (no `done.md`).
- **Umbrella kickoff:** `.claude/orchestrator-prompts/launch-preannounce-track/kickoff.md` (on staging, PR #958 5de61f0a7).
- **Stale items:** None. The 10 `POTENTIAL_DUPE` signals from `/pipeline` dup-detect map exactly onto the already-landed P0 PRs (#957 #951 #949 #947 #946 #943 #942 #939 #932 #923) — expected, not a conflict. (Count note, Phase -1 Finding 4: the umbrella kickoff §0 enumerates 9 of these; #957 — ultrareview P0.4b, merged 2026-07-10 — appears there only as the §0 baseline re-check reference. The 10-PR list here is the complete P0 set.)
- **In-flight probe:** zero open PRs / branches / worktrees for any S1–S6 surface. Only unrelated open PR on staging: #954 (identity rename, separate umbrella).

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| S1 — foreign-repo calibration | manual-liveness / calibration | Mode A inline | No | 1 (gates S2) | S3, S4, S5, S6(1-2) | S |
| S2 — consumer-matrix acceptance gate | execution-build | Mode A + SDD | Yes | 2 (needs S1 + S3 contract) | S4, S5 | L |
| S3 — pre-push structural owner-split | execution-build (load-bearing) | PAIR (Mode SDD) | borderline | 2∥ (startable now) | S2, S4, S5 | M |
| S4 — cargo honest demo (F2a) | execution-build | Mode A | No | 2∥ (startable now) | S2, S3, S5 | M |
| S5 — zcode probe (F3) | manual-liveness | Session-bound (operator machine) | No | 2∥ (startable now) | S2, S3, S4 | S |
| S6 — npm pivot-lite PREP | execution-build (load-bearing) | PAIR | borderline | 1 (steps 1-2) / 3 (step 3 gated) | S1 | M |

**Dependency reality (from umbrella kickoff §3):**

- **S1** is the ONLY Stage-1 gate — S2's fixtures are calibrated by S1's foreign-repo findings.
- **S3, S4, S5, S6(steps 1-2)** have file/worktree surfaces disjoint from S1 and from each other → **startable immediately** in separate worktrees. The umbrella kickoff frames S3/S4/S5 as "parallel to S2"; reading them as startable-now is an authored extension of umbrella §3 (Phase -1 Finding 3) — safe because S1's only consumer is S2. "Disjoint" holds at the file level, NOT the contract level — see the S3→S2 coupling below.
- **S2** starts once S1's calibration report is merged to staging AND the S3 push-channel contract is decided (Option B per Phase -1 Finding 2): umbrella kickoff §2 S3 item 3 ↔ S2 asserts (d)/(i) — S2 encodes whichever contract S3 chooses, so authoring (d)/(i) before the contract is decided risks reworking the core stage. If S3 is BLOCKED, S2 may start the fixture + asserts (a)(b)(c)(e)(f)(g)(h) and HOLDS (d)/(i) until the contract lands.
- **S6 step 3** (dependency-delivery default-flip) is gated on **S2 start cell green AND U10 published** (U10 = the irreversible `npm publish` node, owned externally by `getff-to-prod-meta-launch`). Steps 1-2 (narrow exports, `npm pack` audit) are reversible and start now.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → Stage 2 (S2 waits for S1)

S2 dispatch waits for S1's calibration report to be merged to `staging`. Branch name is executor-chosen, so gate on the **deliverable on staging**, not a fixed head branch:

```bash
git fetch origin staging -q && \
git ls-tree -r origin/staging --name-only .claude/orchestrator-prompts/launch-preannounce-track/ \
  | grep -iE 'calibration|s1-report' \
  || echo "S1 calibration report NOT on staging — do NOT dispatch S2"
```

Expected: at least one calibration-report file path. If empty → halt. Do NOT dispatch S2.

Additionally (Option B — Phase -1 Finding 2), verify the S3 push-channel contract is decided before S2 authors asserts (d)/(i):

```bash
gh pr list --search "is:merged base:staging owner-split in:title" --json number,title --limit 5
# fallback: the S3 stage report / contract note on staging
git ls-tree -r origin/staging --name-only .claude/orchestrator-prompts/launch-preannounce-track/ | grep -iE 's3|owner-split|push-channel'
```

If neither shows the contract → S2 starts fixture + asserts (a)(b)(c)(e)(f)(g)(h) only; (d)/(i) HOLD until the S3 contract lands.

### Stage 2 → Stage 3 (S6 step-3 default-flip)

S6 step 3 is gated on TWO conditions, both must hold:

```bash
# (a) S2 start cell present + green on staging:
gh pr list --search "is:merged base:staging consumer-matrix in:title" --json number,title,mergedAt --limit 5
# (b) U10 published (external — getff-to-prod-meta-launch owns the irreversible publish):
npm view @getff/core version 2>/dev/null || echo "U10 not published — S6 step-3 default-flip BLOCKED (steps 1-2 still OK)"
```

If (a) empty OR (b) unpublished → S6 step 3 stays parked; steps 1-2 proceed independently.

**Phase -1 cold-review before Stage 2 admission (S1→S2) is MANDATORY** (see §7). S3/S4/S5/S6(1-2) are independent single-stage sessions — each gets its own pre-handoff cold-QA (T19), not an inter-stage Phase -1.

---

## §4 Sub-wave dispatch instructions

Each stage = one executor session (single-owner-per-stage, per umbrella kickoff §0). Run the CLAUDE.md pre-dispatch in-flight probe before dispatching any stage. Every session's FIRST action is the **S.0 re-verify** against fresh `origin/staging`.

- **S1 (Mode A inline):** open a fresh CC session → `/orchestrator launch-preannounce-track §S1`. Run `./setup --full` (consumer profile) against 2–3 foreign OSS repos (≥1 pnpm workspace monorepo, ≥1 flat npm app), record every failure verbatim, open a GitHub issue per NEW defect class (check the tracker first — many are known/closed). Output: calibration report committed under the umbrella dir. **This gates S2** — merge it before S2 dispatch.
- **S2 (Mode A + SDD):** after S1 merges → fresh session → `/orchestrator launch-preannounce-track §S2`. Build the SINGLE start cell first (pnpm monorepo · Node 22 · ubuntu), all asserts paired (RED before GREEN, T15). SDD roles: implementer + spec-reviewer + code-quality-reviewer. Wire merge-blocking via `ci-success needs:` (verify each added job is GREEN on staging first — wiring a red job deadlocks merges).
- **S3 (PAIR — startable now):** fresh worktree → `/orchestrator launch-preannounce-track §S3`. Owner-tag every pre-push section + principle test for untagged sections + consumer-topology smoke + push-channel contract. **§4b PR-body mandate applies** (touches `packages/core/principles/**`).
- **S4 (Mode A — startable now):** fresh worktree → `/orchestrator launch-preannounce-track §S4`. clippy.toml writer + example crate + `demo:cargo` target + severity projection (`[lints.clippy] <lint> = "deny"`) + wording sweep (clippy present-tense-demo, cargo-deny explicitly roadmap).
- **S5 (Session-bound — operator machine, startable now):** must run on the operator's Mac (zcode installed there). Live-probe each zcode cell of `.ai-factory/rule-channel-capabilities.json`; add `axis: operator|shipped` field; downgrade unbacked cells to `reference`. Half-day hard timebox.
- **S6 steps 1-2 (PAIR — startable now):** fresh worktree → `/orchestrator launch-preannounce-track §S6`. Narrow `packages/core` exports to honestly-supportable surface + publish-readiness audit (`npm pack` byte-inspection, files/bin/README/license). Step 3 (default-flip) waits for the Stage-2 gate above. Record a handoff note to U10 in the umbrella dir.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered — self-evaluate per stage)

Workers whose target file matches ANY of `.claude/rules/**`, `packages/core/principles/**`, `docs/meta-factory/EXECUTION-PLAN.md`, `docs/meta-factory/prior-art-evaluations.md`, `CLAUDE.md`, `packages/core/templates/**`, `.claude/skills/**`, `agents/**` MUST include in the **PR body** (not just commit message):

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked, with file:line evidence>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the change's scope, with file:line evidence>
```

**Hard rules (each has historically failed the gate):** H3 headings (`### §1.7 … applied`) — NOT H2; the word «applied» is required; ≥40 non-whitespace chars per section body; ≥1 `file.ext:line` citation per section. Escape hatch for pure-structural PRs: `### §1.7 Skipped: <rationale ≥60 chars>`.

Pre-flight grep before `gh pr create`:

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'                  # must be ≥2
```

**Likely triggered:** S3 (new principle test under `packages/core/principles/**`). **S4 — self-evaluate explicitly:** the core render/demo surface is outside the list, but the wording sweep may touch `CLAUDE.md` / `packages/core/templates/**` / `.claude/skills/**` — §4b then applies (Phase -1 Finding 5). **Likely NOT triggered:** S1 (report + issues), S5 (json cells), S2/S6 (CI workflows, fixtures, package.json, core exports — outside the path list) — but each Worker self-evaluates its actual touched paths and applies §4b if any match.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue. This umbrella's traps are declared in the umbrella kickoff §6 and carried here verbatim.

**Active canonical traps for launch-preannounce-track:** T1, T2, T3, T5, T15, T19, T20, T21.

- **T1** — sampling floor ≥5 (S1 foreign-repo runs: 2-3 repos is the minimum, not a stop-at-first-clean).
- **T2** — designing ≠ running (S2/S4/S5: run the real installer / real `cargo clippy` / real zcode probe, not "would fire").
- **T3** — every finding carries command+output or file:line (S1 failures recorded verbatim with rc).
- **T5** — no source edits during S1 calibration (R-phase output is a report; fixes land in S2+).
- **T15** — self-application skipped (canonical per ai-laziness-traps.md §2 T15): the discipline must also run on this umbrella's own artefacts — §6 below is the practiced instance. (The umbrella kickoff's «per the project's own T15 discipline» phrase labels the RED-before-GREEN paired-assert obligation "T15" — a mis-citation surfaced by Phase -1 Finding 1; that obligation is real and binding for every S2 assert, but its correct name is the paired-negative discipline, not T15.)
- **T19** — own adversarial cold-QA before handoff; CI ≠ design review.
- **T20** — no verdict without an evidence-bearing tool call in the same turn.
- **T21** — backward-check enumerates sibling surfaces, does not restate the PR.

**Domain-specific traps (launch-preannounce-track-specific, from umbrella kickoff §6):**

- **T-LPT-A** — validating the matrix through the framework's own substrate (symlinking framework `node_modules`, hoisting tsx at framework root, in-process ESLint Linter API instead of `npx eslint .` + real git hooks). Any cell that takes the shortcut is VOID.
- **T-LPT-B** — declaring an item still-broken from a stale snapshot. Counter: the §0 S.0 rule — re-verify every premise on fresh `origin/staging` before coding.
- **T-LPT-C** — green-cell-by-construction: authoring the fixture around what the installer already handles instead of around S1's foreign-repo findings. Counter: every S2 assert needs an observed RED arm (paired-negative discipline); fixture shapes trace to the S1 report.

> Blanket «see ai-laziness-traps.md» without this enumeration = T7 violation. The enumeration above + domain traps satisfy the §3 obligation.

---

## §6 Recursive self-application

This meta-kickoff was generated by `/pipeline` and must pass `principle 12` (AI-laziness-traps format validation). Verify:

```bash
npx vitest run packages/core/principles/12-ai-laziness-traps.test.ts | tail -5
```

---

## §7 Phase -1 reviewer dispatch (S1 → S2 transition)

Before dispatching S2, invoke a cold-review of S1's calibration output:

```text
Agent: Phase -1 cold-review for launch-preannounce-track Stage S1
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

The reviewer reads the S1 calibration report + the umbrella kickoff §2 S1 acceptance, emits GO / REVISE / STOP with BLOCKER/MAJOR/MINOR classification, and surfaces any strategy fork as `DECISION-NEEDED: <one-line>. Option A → X. Option B → Y.` — it does NOT pick strategy, edit files, or approve on the maintainer's behalf (reviewer-discipline.md §2). S3/S4/S5/S6 are independent single-stage sessions → each gets its own pre-handoff cold-QA (T19), not an inter-stage Phase -1.

---

## §8 Stop conditions

- Stage-1→2 gate command returns empty (no S1 report on staging) AND S2 about to dispatch → HALT.
- Phase -1 reviewer returns STOP → halt S2 dispatch, surface to maintainer.
- Any sub-wave returns a BLOCKER finding → halt that stage, surface before proceeding.
- Any S2 cell takes the T-LPT-A substrate shortcut → the cell is VOID; STOP and rebuild through real channels.
- Any premise re-verified as already-fixed on staging (T-LPT-B) → record in the stage report + skip forward (this is expected, not an error).
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT redo P0 (#923…#957) — the umbrella kickoff §0 lists it; re-implementing is the T-LPT-B trap.
- Do NOT run the irreversible `npm publish` / drop `private:true` — that is U10, owned by `getff-to-prod-meta-launch`. S6 is reversible-prep only.
- Do NOT drift into out-of-scope surfaces (umbrella kickoff §4): full delivery manifest (P2), the 7-subsystem SCC untangle, new stacks, per-harness emission, go-to-market (owned by `public-launch` U12).
- Do NOT open a drive-by PR for a systemic find mid-stage — surface it as an observation in the stage report (CLAUDE.md PR strategy).
- Do NOT add npm deps beyond what S6 explicitly scopes; substrate stays bash + markdown + CC primitives + `gh`.

---

## §10 See also

- `.claude/orchestrator-prompts/launch-preannounce-track/kickoff.md` — the BINDING umbrella scope (S1–S6, S.0 rule, AI-traps).
- `.claude/orchestrator-prompts/getff-to-prod-meta-launch/kickoff.md` — U10 (irreversible publish) + U9/U11 gates that S6 hands off to.
- `.claude/orchestrator-prompts/public-launch/kickoff.md` — U12 go-to-market (this umbrella is its engineering prerequisite).
- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/parallel-subwave-isolation.md §1` — worktree isolation (S3/S4/S6 parallel sessions).
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries (§7).

<!-- host-verify: none — legacy closed umbrella (done.md): work already accepted; no live host acceptance to declare — retro-marked 2026-08-21 -->
