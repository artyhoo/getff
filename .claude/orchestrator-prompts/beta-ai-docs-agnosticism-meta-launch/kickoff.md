# KICKOFF — beta-ai-docs-agnosticism (meta-launch)

> **Type:** execution-build, multi-stage (generated by `/pipeline beta-ai-docs-agnosticism` on 2026-08-08).
> **Origin:** plan-currency check + launch-table from the `/pipeline` skill.
> **Deliverable:** staged dispatch of track-2 umbrella C — AGENTS.md layer + AI Usage Guide,
> a skills-agnosticism probe class, the self-generating-docs sweep, and context7/DeepWiki
> discoverability.
> **Base branch:** staging (NOT main — main is prod, manual promote only).
> **Tier:** 2 per [CLAUDE.md «Task-tier routing»](../../../CLAUDE.md) — **NO `bridge-profile`
> marker on any stage dispatch.** Every stage carries real design decisions (probe population
> design, doc-class judgments, ownership carve-out calls), so the top tier plans each stage and
> the executor tier implements + reviews from below. Binding from the umbrella kickoff header;
> do not re-litigate per-stage.

<!-- host-verify: none — routing/dispatch input only; this file ships no executable deliverable. Its own acceptance is principle 12 (§6), which each stage kickoff re-declares with its real host commands. -->

---

## §0 Cold-start context

Read in this order:

1. This kickoff (dispatch scope + stage gates + preflight verdicts).
2. [`.claude/orchestrator-prompts/beta-ai-docs-agnosticism/kickoff.md`](../beta-ai-docs-agnosticism/kickoff.md) — the umbrella kickoff (binding stage table §2, AI traps §4).
3. [`docs/superpowers/specs/2026-07-23-beta-program-design.md`](../../../docs/superpowers/specs/2026-07-23-beta-program-design.md) — the **design SSOT** (§6 umbrella C = C1-C5; §2 decisions D4/D7/D8; §8 integration contract). On any divergence between this kickoff and the spec, **the spec wins** — surface it, never improvise past a binding decision.
4. [`packages/core/templates/shared/tier-home.md`](../../../packages/core/templates/shared/tier-home.md) — the tier SSOT that C1's AGENTS.md layer points at (A-S3 deliverable).

---

## §1 Inputs

- **Plan-currency check:** CURRENT (as of 2026-08-08, `6f11473720`). No DRIFT items against the
  umbrella's own dependency claims; `wave-sequencing-plan.md` carries **zero** rows for the beta
  program (`grep -nE 'beta|umbrella C|track.2'` → no matches), so there is nothing for the beta
  track to have drifted *from* — the umbrella kickoff's own §0 gates are the live authority, and
  all three are resolved mechanically in §1.1.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/beta-ai-docs-agnosticism/kickoff.md` — on
  staging, verified `git ls-tree origin/staging` (per [kickoff-staging-placement.md §1](../../rules/kickoff-staging-placement.md)).
- **Binding design spec:** `docs/superpowers/specs/2026-07-23-beta-program-design.md` — on staging, verified.
- **Stale items:** none.

### §1.1 §0 preflight resolution (all three gates, mechanical evidence)

| Preflight (umbrella kickoff §0) | Verdict | Evidence |
|---|---|---|
| 1a. C3/C5 gated on A-S3 (tier-home doc exists) | **PASS** | PR #1272 MERGED 2026-08-08T00:40:31Z; `git ls-tree origin/staging packages/core/templates/shared/tier-home.md` returns the blob |
| 1b. C1 template edits gated on A-S1 (profile names final) | **PASS** | PR #1173 MERGED 2026-08-01T17:59:29Z (`core` / `env` / `factory` shipped) |
| 2. Pre-dispatch in-flight probe (track-1 umbrellas + umbrella A stages, shared surfaces) | **PASS — no live collision** | see §1.2 |
| 3. Anchors re-verified live at stage entry (spec §9 preflight 2) | **carried into each stage AC** + spot-verified now | see §1.3 |

### §1.2 In-flight probe — the two matcher hits are false positives

`inflight-check.sh beta-ai-docs-agnosticism` returned three `INFLIGHT:` lines. All three are
resolved; **none** is a live parallel session on umbrella C's surfaces:

| Signal | Verdict | Evidence |
|---|---|---|
| branch `claude/beta-docs-showcase-defer-to-factory` | **MERGED** — stale local ref, umbrella **B** not C | `gh pr list --head … --state all` → `#1105 MERGED` |
| branch `claude/beta-docs-showcase-kickoff-d0dcc3` | **MERGED** — stale local ref, umbrella **B** not C | `gh pr list --head … --state all` → `#1104 MERGED` |
| worktree `…/beta-ai-docs-agnosticism-3dc5d6` | **self** — this `/pipeline` session | `git log origin/staging..HEAD` → empty (ahead=0) |

Direct probes on the umbrella's own slug: **zero** open PRs (`gh pr list --state all --search
'beta-ai-docs-agnosticism'` returns only unrelated historical PRs), **zero** branches beyond this
session's own (`git branch -a --list '*beta-ai-docs*'`), **no** `done.md`.

**Shared-surface check against umbrella A's live stages** (kickoff §0 gate 2 names
`packages/core/templates/shared/AGENTS.md.template`, `INSTALL-FOR-AI.md`,
`.claude/rules/zcode-parity-doctrine.md`): A-S4 is in flight as aif task `92bf0019` and its
kickoff **does** name `INSTALL-FOR-AI.md` (`kickoff-s4.md:56,232,251` — the one-button install
step). This is a **contended surface, not a blocker**: C1's INSTALL-FOR-AI work is the
skill-dir-naming split + Cursor-story reconciliation, disjoint sections from A-S4's one-button
step. **Binding on S1:** re-run the probe at stage entry and, if A-S4 has not yet harvested,
sequence the `INSTALL-FOR-AI.md` edit *after* it merges rather than racing the file.

### §1.3 Anchor spot-verification (live, this invocation)

| Anchor | State |
|---|---|
| `packages/core/templates/shared/AGENTS.md.template` | exists (13489 B) |
| `INSTALL-FOR-AI.md` | exists (35986 B) |
| root `AGENTS.md` | **107 lines** — the ≤150 gate has 43 lines of headroom for C1 |
| principle 21 (`21-agnosticism-conformance.test.ts`) | enumerates `agents/*.md` only (`AGENTS_DIR`, `:266-281`) |
| C3's negative-existence claim («today NO probe enumerates `.claude/skills`») | **RE-CONFIRMED** — the only agnosticism probe walks `agents/`, not `.claude/skills` |

---

## §2 Launch-table

Launch table — beta-ai-docs-agnosticism (as of `6f11473720`):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| S1 — AGENTS.md layer + AI Usage Guide (C1) | execution-build | Mode A (PAIR / Mode-SDD) | Yes | 1 | — | M |
| S2 — Skills agnosticism probe (C3) | execution-build | Mode B × 2 (PAIR / Mode-SDD) | Yes | 2 | S3 | M |
| S3 — Self-generating docs sweep (C5) | execution-build | Mode B × 2 (PAIR / Mode-SDD) | Yes | 2 | S2 | L |
| S4 — Discoverability (C4) | wiring | Mode A (SOLO / Mode-A) | No | 3 | — | S |

**Routing derivation (`/pipeline` §2.5 Step 5-6).** Umbrella-level classify:
`TYPE=I-phase-large, SURFACES=13` → `scope_decided=TRUE` (spec §6 C1-C5 binding),
`parallel_safe=TRUE` for the declared S2 ∥ S3 pair → **DECOMPOSE / Mode-B** at the umbrella.
Per stage: S1/S2/S3 each carry ≥2 internally-coupled surfaces (`parallel_safe=FALSE` *within*
a stage) and touch `packages/core/principles/**` / `packages/core/templates/**` /
`.claude/skills/**` / `agents/**` (`review_required=TRUE`) → **PAIR / Mode-SDD**. S4 is thin
CI/config wiring with no principle-09 header surface → **SOLO / Mode-A**.

**Parallel-sibling provenance:** the S2 ∥ S3 edge is declared by the umbrella kickoff §2
(«Order: S1 → S2 ∥ S3 → S4»), not inferred. S2 touches `packages/core/principles/**` +
`.claude/skills/**` conformance declarations; S3 touches `docs/**` generated sections +
`agents/claims-conformance-auditor.md`. **ATTN:** both stages may want to edit
`.claude/skills/night-mode/SKILL.md` (S2 for its degradation declaration, S3 if that file holds
derivable prose). Stage 2 dispatch MUST assign that file to exactly one worker in the stage
kickoffs — a shared file across two worktrees is `#shared-workdir-parallel` with extra steps.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → Stage 2

```bash
gh pr list --search "is:merged head:beta-c-s1-agents-layer base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: non-empty JSON. If empty → halt. Do NOT dispatch Stage 2.

### Stage 2 → Stage 3

```bash
gh pr list --search "is:merged head:beta-c-s2-skills-probe base:staging" --json number,title,mergedAt,headRefName --limit 10
gh pr list --search "is:merged head:beta-c-s3-selfgen-docs base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: **both** non-empty (Stage 2 is a two-worker fan-out; one merged sibling is not a
cleared stage). If either is empty → halt. Do NOT dispatch Stage 3.

### Umbrella closure

```bash
gh pr list --search "is:merged head:beta-c-s4-discoverability base:staging" --json number,title,mergedAt,headRefName --limit 10
```

On green: the merging session writes `.claude/orchestrator-prompts/beta-ai-docs-agnosticism/done.md`
per the umbrella-closure convention.

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7 below).

---

## §4 Sub-wave dispatch instructions

Every stage: **one PR onto `staging`**, branch name as in §3, top tier plans / executor tier
implements + reviews, **no `bridge-profile` marker**. Every stage re-verifies its spec file:line
anchors live at entry (preflight 3) and re-runs the in-flight probe (preflight 2). Each stage's
own kickoff is authored as a separate staging PR **before** its dispatch (per
[kickoff-staging-placement.md §1](../../rules/kickoff-staging-placement.md); precedent: umbrella A's
`kickoff-s1.md` shipped as PR #1161, after the meta-launch PR #1122).

**Cross-cutting, binding on EVERY stage (umbrella kickoff §2, spec §6 C2):** every new or
edited doc artifact ships per the ai-doc standard — channel selection FIRST
([rule-enforcement-channel-selection.md](../../rules/rule-enforcement-channel-selection.md)),
doc-authority Class / Authoritative-for headers (principle 09 is the mechanical floor, the
standard is wider), hot/cold split, D8 memory/context hygiene. C2 is not a stage precisely
because it must not fall between stages.

### Stage 1 — S1 AGENTS.md layer + AI Usage Guide (PAIR / Mode-SDD, ~M)

- **Scope (spec §6 C1):** extend `packages/core/templates/shared/AGENTS.md.template` with the
  environment layer + degradation pointers, pointing at the A-S3 tier-home doc as SSOT (never
  restating its criteria — that is `#two-prompts-drift`); author the new **AI Usage Guide**
  (First Steps → daily cycle → presets → parks → degradations); refresh `INSTALL-FOR-AI.md`
  (skill-dir naming split, Cursor-story reconciliation).
- **ONE source of truth, two renders:** the AI Usage Guide's First Steps and umbrella B's
  human First Steps are the **same** steps list rendered twice. Ship the fixture that asserts
  parity — a hand-synced pair is a lying doc waiting for its first edit.
- **Sequencing constraint (§1.2):** if A-S4 (aif task `92bf0019`) has not harvested at stage
  entry, do the `INSTALL-FOR-AI.md` edit last, after it merges.
- **AC:** doc-claims + rules-autoload probes green (command + output, not prose); the
  First-Steps parity fixture passes; root `AGENTS.md` still ≤150 lines.

### Stage 2 — parallel fan-out (read the §2 ATTN first)

**S2 — Skills agnosticism probe (C3) — PAIR / Mode-SDD, ~M**

- New probe class enumerating `.claude/skills` — verified-negative live this invocation
  (principle 21 walks `agents/*.md` only, `:266-281`). Widen principle 21's population, or add
  a sibling principle; the channel choice is the stage's first decision, not an afterthought.
- Night-mode conformance treatment; `/arch` + glm-handoff **explicit** degradation declarations.
- **AC:** principle 21 green over the widened population; a **seeded-break paired-negative**
  for the new probe (harness-self pattern) — a probe with no paired-negative is unfalsifiable.

**S3 — Self-generating docs sweep (C5) — PAIR / Mode-SDD, ~L**

- **T10 is binding: population enumeration BEFORE sampling.** Inventory ALL derivable-prose
  classes first; only then migrate to `getff:begin` generated sections with drift gates.
- **D7 ownership carve-out (spec §2):** maintainer-owned files and `.claude/rules/*` get
  **patch-proposal PRs flagged for sign-off**, never direct edits. The zcode-doctrine §3 sync
  is the first such proposal (its deferral trigger fired; Wave B merged #1043-#1047).
- Author `agents/claims-conformance-auditor.md` — the spec §8 assembly-gate cold auditor.
- **AC:** every migrated section has a regen check; owner-gated changes are flagged PRs.

### Stage 3 — S4 Discoverability (SOLO / Mode-A, ~S)

- `context7.json` (`excludeFolders`; the `rules` field carries the discipline rules) + a GitHub
  Action re-indexing per push; DeepWiki acknowledged **secondary** (slow refresh — never wire a
  freshness-critical claim to it).
- **llms.txt-for-repo is explicitly NOT added** (crawler-log evidence, spec C4). Record the
  decision; do not re-litigate it.
- **Maintainer-gated leg:** the context7 *submission* is an outward-facing action on an external
  service — the worker prepares it and **surfaces it for the maintainer to perform**, never
  submits autonomously.
- **AC:** `resolve-library-id` returns this repo; the Action runs green; workflow pins tool
  versions per [ci-tool-pinning.md](../../rules/ci-tool-pinning.md) and adds zero paid-LLM calls
  per [no-paid-llm-in-ci.md](../../rules/no-paid-llm-in-ci.md).

---

## §4b §1.7 PR-body authoring mandate (paths-triggered — applies to S1, S2, S3)

S1 (`packages/core/templates/**`), S2 (`packages/core/principles/**`, `.claude/skills/**`) and
S3 (`agents/**`, `.claude/rules/**` proposals) all match the mandate path list. S4 does **not**
(`.github/workflows/**` is outside it) — S4 may skip §4b.

**Required PR body sections (verbatim shape — the CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- `doc-authority-hierarchy.md` — <what was checked>. file:line evidence: `<path>:<line>`
- `phase-research-coverage.md §1.12` — <what was checked>. file:line evidence: `<path>:<line>`
- `build-first-reuse-default.md` — <verdict + rationale>
- `no-paid-llm-in-ci.md` — <verdict>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the change's scope>

- `<existing-artefact-name>` — <supersedes / extends / orthogonal>. file:line evidence: `<path>:<line>`
```

**Hard rules (each has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2.** The gate's awk regex matches `^### §1\.7 …`.
2. **The word «applied» is required.**
3. **≥40 non-whitespace chars in EACH section body.**
4. **≥1 `file.ext:line` citation per section.**
5. Escape hatch for purely structural PRs: `### §1.7 Skipped: <rationale ≥60 chars>`.

**Pre-flight grep — run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c   # ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # ≥2
```

**T21 warning, load-bearing for S3:** the backward-check must **sweep siblings**, not restate the
PR. S3 migrates prose to generated sections across many docs — the tempting backward-check is
«all migrated sections carry regen checks», which names only the diff's own files. Delegate the
sweep to [`agents/backward-sweep-auditor.md`](../../../agents/backward-sweep-auditor.md), handing it
the change *class* only.

---

## §4c Autonomous aif-handoff dispatch — park-don't-guess contract

**Status for this umbrella: autonomous dispatch is OPEN (both legs verified 2026-08-08).**

| Leg | Result | Evidence |
|---|---|---|
| Bridge reachable | **UP** | `RUNTIME_BRIDGE_MODE=aif-handoff`; `GET /runtime-profiles` → `200`, `GET /health` → `200` |
| Lever-1 review-iteration ceiling | **VERIFIED = 4** | `docker exec aif-handoff-api-1 … AGENT_MAX_REVIEW_ITERATIONS` → `4`; same on `aif-handoff-agent-1`; `GET /settings` → `maxReviewIterations: 4` |

**Where the ceiling must live, and why the obvious placement is inert.**
`autoReviewHandler.ts:125` reads `refreshedTask.maxReviewIterations ?? env.AGENT_MAX_REVIEW_ITERATIONS`
— **the task row wins** — and `api/schemas.ts:79` stamps that row at creation from the **api**
container's env (`.default(getEnv().AGENT_MAX_REVIEW_ITERATIONS)`). So setting the variable on the
`agent` service alone is **inert for every task created through the API**: measured on 2026-08-08,
the five most recent rows (`GET /tasks`) all carried `mri=3` while the agent container reported
its own value. The ceiling is therefore set on **both** services in
`~/code/aif-handoff/docker-compose.override.yml`, and the probe that proves it is `GET /settings`
plus a fresh task's row — not the agent-side `docker exec` alone. (Same failure class as that
file's documented `COORDINATOR_MAX_CONCURRENT_TASKS` incident: a value set where nothing reads it.)
A host-side `export` is not forwarded in any case — no path in `packages/runtime-bridge/src/**`,
no compose key.

**Ceiling semantics + scope honesty (operator decision 2026-08-08).** 4 rounds of
fix-and-re-review before handing to a human — a cheap executor seat should keep fixing its own
review findings rather than parking them. This is a **global** default, not a GLM-only one:
nothing pins a task to the GLM seat unless its kickoff carries `<!-- bridge-profile: <name> -->`
(`runtime-bridge/src/kickoff.ts:30` → `AifHandoffBackend.ts:221`), and **Tier-2 kickoffs
deliberately omit that marker** (this umbrella's header; verified absent in all three kickoffs by
running the extractor's own regex). So 4 rounds bill whatever seat aif picks —
`resolvedDefaultTaskRuntimeProfileId` is `null` today, and every existing task row carries
`profile=null`. Per-task `maxReviewIterations` (api `schemas.ts:79`) is the override when a stage
wants a different ceiling. **Open fork for the maintainer, deliberately not decided here:** pinning
the executor seat to GLM requires a `bridge-profile` marker, which contradicts the Tier-2 «no
marker» rule — resolving that is a tier-routing decision, not a stage one.

**Every autonomously dispatched stage kickoff MUST carry this verbatim:**

> **aif agent — fork discipline (non-negotiable):** On ANY genuine fork or ambiguity (two
> defensible implementations, an undecided design choice, a missing spec detail that changes
> behaviour) — **do NOT pick.** Park it as a question (set the task to `manualReviewRequired` /
> `blocked_external` with the fork stated as «Option A → consequence X / Option B → consequence
> Y») and **stop that task.** Proceed only on the unambiguous parts.

**Egress gate (mandatory after `status=done`):** aif does not push or open PRs by design —
`npx tsx packages/runtime-bridge/src/cli/harvest.ts <taskId> --base staging`. Skipping it is
`#autonomous-done-no-harvest`: the work stays in the container forever.

**Park-record contract (binding on every stage kickoff §2 allowlist):** recording a fired PARK is
not a file write — it lands in the park payload + the PR's `## Parked questions` section, and its
correction lands as a **separate owner commit** on the spec / ADR / kickoff, never inside the
stage PR. Stage kickoffs state this in §2 instead of widening the allowlist.

---

## §5 AI-traps active (per [`.claude/rules/ai-laziness-traps.md §3`](../../rules/ai-laziness-traps.md))

See [.claude/rules/ai-laziness-traps.md §2](../../rules/ai-laziness-traps.md) for the full catalogue.

**Active canonical traps for beta-ai-docs-agnosticism:** **T3**, **T10**, **T14**, **T15**,
**T16**, **T20** (inherited verbatim from the umbrella kickoff §4), plus **T2**, **T13** and
**T21** added by this meta-launch for the dispatch layer.

- **T3** — probe/doc claims need command + output, not prose. «Probe green» = a pasted run.
- **T10** — S3 starts with population enumeration of ALL derivable-prose classes, never sample-first.
- **T14** — a clean probe over low coverage is «coverage insufficient», not «agnostic».
- **T15** — self-application is the point: the docs discipline applies to OUR docs first.
- **T16** — AGENTS.md/LF and context7 are ADOPTED standards. Verify OUR problem-class fits
  (root ≤150 lines, nested precedence) instead of cargo-culting the format.
- **T20** — every verdict carries file:line evidence.
- **T2** *(added here)* — S2 designs a probe class; designing ≠ auditing. The stage is not done
  when the probe exists, it is done when the probe has been **run over the real population** and
  its findings reported.
- **T13** *(added here)* — the LF AGENTS.md standard and context7's schema are ADOPTED. Confirm
  the upstream had evidence for the pattern; do not treat «it's a standard» as zero-work.
- **T21** *(added here)* — see the §4b warning: S3's backward-check must enumerate sibling
  surfaces, not restate its own migrations.

**Domain-specific traps (umbrella-C-specific, NOT in the canonical catalogue):**

- **T-BAD-A** *(from the umbrella kickoff)* — «the generated section works because the generator
  ran once». Every `getff:begin` migration must ship its DRIFT check (seeded-break test), else it
  is a new lying doc with extra steps.
- **T-BAD-B** *(added here)* — **«agnostic because the probe passes on the harness we run»**. C3's
  whole claim is cross-harness portability, and the probe executes under exactly one harness. A
  green probe under Claude Code says nothing about Cursor/Aider/Codex. Every agnosticism verdict
  must name the harnesses actually exercised and mark the rest `INCONCLUSIVE-needs-human`, never
  generalise from the one seat that ran.
- **T-BAD-C** *(added here)* — **«pointed at the SSOT» when the doc actually restates it**. C1
  points the AGENTS.md layer at the tier-home doc; the tempting shortcut is to paste the tier
  criteria inline «for convenience». That is `#two-prompts-drift`: two copies, one edit, silent
  divergence. Falsifier: if the tier criteria are readable without opening tier-home.md, C1 has
  forked the SSOT.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without the enumeration above is a
> T7 violation (pattern-matching the prompt instead of reasoning against it). Every stage kickoff
> generated under this meta-launch inherits this list and MUST add ≥1 trap of its own.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass principle 12
([`packages/core/principles/12-ai-laziness-traps.test.ts`](../../../packages/core/principles/12-ai-laziness-traps.test.ts)):

```bash
npx vitest run packages/core/principles/12-ai-laziness-traps.test.ts
```

The umbrella's own goal is recursive by construction: the discipline it ships (AI docs that
orient a cold harness cheaply, claims that are probes rather than prose) applies to **this repo's**
docs first — that is T15, and it is the acceptance bar, not a footnote.

---

## §7 Phase -1 reviewer dispatch (between stages)

Before dispatching Stage N+1:

```text
Agent: Phase -1 cold-review for beta-ai-docs-agnosticism Stage N
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

The reviewer reads `git diff staging...<stage-N-head>` + this kickoff's Stage N AC, emits
GO / REVISE / STOP with BLOCKER/MAJOR/MINOR classification, and surfaces strategy forks as
«DECISION-NEEDED: … Option A → consequence X. Option B → consequence Y.» — it does **not** pick
strategy, edit files, approve on the maintainer's behalf, or accept «CI is green» as design
review ([reviewer-discipline.md §2](../../rules/reviewer-discipline.md), T19).

**Verdict routing:** GO → dispatch Stage N+1 · REVISE → worker fixes, repeat Phase -1 ·
STOP → escalate, halt.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict,
mid-stage status, final handoff) ends with a `## 🟢 In plain words` block whose content follows
the orchestrator-checkpoint substance shape — it names sub-waves, AC items, file:line,
REPORT-traces, dispatch state. It does **not** restate the per-turn generic reasoning the
end-of-turn hook already enforces; content verbatim-copyable from `end-of-turn-reminder.sh` is
`#two-prompts-drift`.

---

## §8 Stop conditions

- Any `gh` command errors while Stage 2's parallel fan-out is live → halt, escalate.
- A stage gate returns empty while Stage N was expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface.
- Any sub-wave returns a BLOCKER finding → halt the stage before proceeding.
- S3 is about to edit a maintainer-owned file or `.claude/rules/*` directly → STOP; the D7
  carve-out permits patch-proposal PRs only.
- Divergence from spec D7/D8 → STOP + surface (umbrella kickoff §5).
- Any temptation to edit `~/.claude/skills/orchestrator/` → violation; stop.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this kickoff.
- Do NOT finalize project strategy — surface genuine forks to the maintainer.
- Human-facing site content is umbrella **B**; tier-home *authoring* is A-S3 (C1 only points at
  it); killer-layer doc semantics are track 1. Out of scope here.
- Do NOT autonomously rewrite maintainer-owned or `.claude/rules/*` files (spec D7).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate purity per no-paid-llm-in-ci.md + BFR-default).

---

## §10 See also

- [`.claude/skills/pipeline/SKILL.md`](../../skills/pipeline/SKILL.md) — the generating skill.
- [`.claude/rules/ai-laziness-traps.md §2`](../../rules/ai-laziness-traps.md) — canonical trap catalogue.
- [`.claude/rules/parallel-subwave-isolation.md §1`](../../rules/parallel-subwave-isolation.md) — Stage 2 worktree isolation.
- [`.claude/rules/reviewer-discipline.md §2`](../../rules/reviewer-discipline.md) — reviewer role boundaries.
- [`.claude/rules/kickoff-staging-placement.md §1`](../../rules/kickoff-staging-placement.md) — merge stage kickoffs to staging before dispatch.
- [`.claude/rules/destination-environment-verification.md §1`](../../rules/destination-environment-verification.md) — each stage kickoff declares its own `host-verify` contract.
- [`state.md`](state.md) — this meta-launch's session state log.
