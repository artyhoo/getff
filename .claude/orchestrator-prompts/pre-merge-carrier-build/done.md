# pre-merge-carrier-build — DONE
- Final PR: #1492 (B2; stage chain B1 #1479 → B2 #1492, kickoff PR #1478, F5 ratified in #1480)
- Closed: 2026-09-01
- Summary: the opt-in pre-merge carrier ships at every npm-lane install — B1: `packages/core/audit-self/pre-merge-local.sh` (throwaway-worktree merge, three-sha report, exit-code table with F2 semantics, ts-server lane) + `ci-available-probe.sh` (#1465 riders) + delivery via `setup.d/40-configs.sh` copy_safe (ungated by profile per F1) + refresh pair-list entries + CI wiring inside existing `audit-self.yml` jobs + fixture self-tests; B2: the remaining six shipped lane runners (python/go/cargo + react-next/react-spa/react-native) with per-lane gate derivation, pin checks, cache isolations, and the F5 claim narrowed to npm-lane installs in `INSTALL-FOR-AI.md` + `AI-USAGE-GUIDE.md`.
- B3 is NOT a build stage: it is the promotion-to-default decision, trigger-gated (kickoff.md:72 — fires on the recorded §f trigger «first live catch on a non-timeliner consumer» OR on falsifier α «usage ≈ zero after N weeks»). It is an operator fork by construction, so the umbrella closes at B2; B3 re-opens as its own decision record when the trigger fires.
- Follow-ups (parked by kickoff decisions): (1) F5 — delivering the carrier into the thin python/go/cargo install paths is trigger-gated on first live demand (kickoff.md:51); (2) the aif dispatch `62462592` that built B2 was harvested 2026-08-20 with the Task-10 deviation recorded in kickoff.md:185.
