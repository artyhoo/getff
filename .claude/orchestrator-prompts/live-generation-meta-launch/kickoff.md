# KICKOFF — live-generation (meta-launch)

> **Type:** I-phase-small — thin-closure stage LG-S4 only (generated by `/pipeline live-generation` on 2026-07-17).
> **Origin:** plan-currency check + launch-table from `/pipeline` skill.
> **Deliverable:** LG-S4 thin closure — INSTALL-FOR-AI.md + README draft diff + documented `ecosystem-python.ts` gap + this umbrella's `done.md`, in ONE PR onto staging.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

> **Status snapshot (binding, verified against `origin/staging` @ `4a9b508a9` 2026-07-17):** LG-S1/S2/S3 are ALL MERGED (#1005/#1006/#1010, 2026-07-13). OWNER-FORK-2 RESOLVED in #1006 (leave ESLint-direct; converge later via add-alongside). The python-delivery-v0 S3 prerequisite gate is SATISFIED (#997 merged). The `deps-hash-multistack` extraction (#1016 kickoff + #1017 scope pointer) is OUT of LG-S4 scope. **The single remaining work unit is LG-S4 — a thin confirmatory closure PR, NOT an I-phase implementation stage.**

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + stage gates).
2. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.
3. Per-sub-wave kickoff at `.claude/orchestrator-prompts/live-generation/kickoff.md` (AUTHORITATIVE = the version on `origin/staging` post-#1021; **NOT the stale working-tree pre-#1021 copy** — see §1 DRIFT-1).

---

## §1 Inputs

- **Plan-currency check:** **DRIFT FOUND — 1 item (resolved inline below)** (as of 2026-07-17 `4a9b508a9`).
- **Umbrella kickoff:** `.claude/orchestrator-prompts/live-generation/kickoff.md`
- **Stale items:**
  - **DRIFT-1 (resolved by using authoritative source):** the working-tree kickoff on `feat/deps-hash-multistack-dh-s1` is the PRE-#1021 version (still marks LG-S1/S2/S3 as future, gate as unsatisfied, OWNER-FORK-2 as open). The AUTHORITATIVE version on `origin/staging` (PR #1021, merged 2026-07-16T22:20:49Z) correctly reflects: LG-S1/S2/S3 merged, gate satisfied, FORK-2 resolved, LG-S4 re-framed as thin closure. **This meta-launch was built from the `origin/staging` authoritative copy.** No kickoff edit is required — the stale working-tree copy is on an unrelated branch (`feat/deps-hash-multistack-dh-s1`) and will be superseded on next staging checkout. **Mechanical evidence:** `git diff origin/staging..HEAD -- .claude/orchestrator-prompts/live-generation/kickoff.md` = non-empty; `gh pr list --search 'is:merged base:staging live-generation'` = #1005/#1006/#1010/#1017/#1021 all merged.

---

## §2 Launch-table

```text
Launch table — live-generation (as of 4a9b508a9):

| Sub-wave | Type             | Mode    | SDD? | Stage | Parallel sibling | Volume |
|----------|------------------|---------|------|-------|------------------|--------|
| LG-S4    | I-phase-small    | Mode A  | No   | 1     | —                | S      |
```

**Column rationale:**

- **Sub-wave: LG-S4** — the ONLY remaining stage. LG-S1/S2/S3 are merged (#1005/#1006/#1010).
- **Type: I-phase-small** — kickoff §2 (authoritative) explicitly states «thin-closure stage — a small confirmatory PR + doc draft, NOT an I-phase implementation stage». Three small deliverables (doc gap + INSTALL-FOR-AI + done.md), all docs/markers, no new machinery.
- **Mode: Mode A (single inline Opus session)** — §5 dispatch table: a single execution-build sub-wave → Mode A. No parallel siblings.
- **SDD?: No** — §3 threshold: SDD review overhead roughly equals catch-rate at 1–2 tasks. LG-S4 is a single PR with 3 tightly-coupled doc/marker edits under the same acceptance criterion (umbrella closure). The cold-review surface here is doc-accuracy, not multi-task design review — Phase -1 cold-review (§7) is the right gate, not a 3-role SDD loop.
- **Stage: 1** — single stage; no Stage 2 follows (LG-S4 is the final stage; writing `done.md` CLOSES the umbrella).
- **Parallel sibling: —** — single sub-wave; `deps-hash-multistack` was EXTRACTED (#1017 scope pointer) and is NOT a sibling (separate umbrella, separate dispatch).
- **Volume: S (<100 LOC)** — INSTALL-FOR-AI.md additions + README draft diff (in PR body, maintainer-owned) + a `done.md` (~30 lines) + a one-line gap marker in `research/allowlist.ts` comment or `INSTALL-FOR-AI.md`. No code change beyond an optional comment.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

LG-S4 is the FINAL stage (Stage 1). There is no Stage 2 to gate. The pre-dispatch gate (already SATISFIED per §1) is:

```bash
# Pre-dispatch gate — LG-S1/S2/S3 must be merged to staging before LG-S4 dispatch.
gh pr list --search "is:merged base:staging head:claude/live-generation-s1-python-a52650" --json number,mergedAt --limit 5
gh pr list --search "is:merged base:staging head:claude/live-generation-s2-js-confirm" --json number,mergedAt --limit 5
gh pr list --search "is:merged base:staging head:claude/live-generation-s3-rust" --json number,mergedAt --limit 5
```

Verified 2026-07-17: all three return non-empty (#1005 2026-07-13T07:25:40Z / #1006 2026-07-13T07:38:19Z / #1010 2026-07-13T09:17:03Z). Gate is GREEN. Proceed to LG-S4 dispatch.

**Post-LG-S4 closure gate (umbrella CLOSES, not stage-gate):** after the LG-S4 PR merges, verify the umbrella is closable:

```bash
git cat-file -e origin/staging:.claude/orchestrator-prompts/live-generation/done.md && echo "CLOSED" || echo "STILL OPEN"
```

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7 below). For LG-S4 this means: the LG-S4 PR diff gets a Phase -1 cold-review BEFORE merge (not after — there is no Stage 2 to gate).

---

## §4 Sub-wave dispatch instructions

**LG-S4 — thin-closure Worker session (Mode A inline).**

**1-liner (paste into a new CC tab):**

```text
/orchestrator live-generation §LG-S4 — thin closure (Mode A, doc-only): (1) document ecosystem-python.ts Tier-1 gap in research/allowlist.ts comment + INSTALL-FOR-AI.md (default = documented gap, owner-trigger; ecosystem-python.ts stays ABSENT — do NOT build it); (2) UPDATE the existing repo-root INSTALL-FOR-AI.md (445 lines, already on staging): correct the stale claim at lines 234-235 ("rule-research live adapter for Python are out of scope") to reflect LG-S1/S2/S3 SHIPPED, and add the 3-stack live-generation workflow cross-linked to the shipped adapters; (3) prepare README diff AS A DRAFT IN THE PR BODY (README is maintainer-owned — do NOT edit README.md directly, Artifact Ownership Contract); (4) write live-generation/done.md in THIS PR with stage-by-stage closure referencing #1005/#1006/#1010. Branch from staging (current checkout is clean staging). §1.7 Forward+Backward in PR body (conditional — see §4b). Rest in kickoff: .claude/orchestrator-prompts/live-generation/kickoff.md (working tree == authoritative origin/staging post-#1021).
```

**Worker acceptance criteria (LG-S4, binding — copied from authoritative kickoff §2):**

1. `ecosystem-python.ts` Tier-1 adapter — **OWNER-FORK: triggers ONLY on explicit owner signal (a confirmed consumer with a non-Tier-0 python source); otherwise the LG-S4 executor RECORDS it as a documented gap and proceeds to closure** (default = documented gap — `allowlist-resolver.ts` already degrades gracefully). Do NOT implement `ecosystem-python.ts` in this PR unless the owner has signalled the trigger.
2. INSTALL-FOR-AI.md (**UPDATE the existing 445-line file** — correct the stale Python-live-adapter-out-of-scope claim at lines 234-235 + add the 3-stack live workflow) + README updates (README maintainer-owned → **PREPARED DRAFT DIFF in the PR body**, Artifact Ownership Contract — do NOT edit README.md directly).
3. `done.md` for this umbrella, written **by the LG-S4 executor in the same PR — not created earlier, not deferred to a later session**. Stages referenced: LG-S1 #1005, LG-S2 #1006, LG-S3 #1010, LG-S4 = this PR.

**STOP lines that bind LG-S4 (from authoritative kickoff §2 STOP lines):**

- `done.md` ONLY at the LG-S4 final-PR merge — LG-S4 is that PR.
- NO new `ConventionNode` fields (IR is frozen; OWNER-FORK-1 still OPEN and is a separate MT-plane umbrella, NOT LG-S4).
- Do NOT re-build the shared core (owner directive).
- Do NOT re-implement the js/ts adapter (SHIPPED #805).
- A researched practice NOT single-pattern-expressible → research-only finding, NEVER an inert rule (`#MAJOR-1` / discipline-theatre STOP).
- NO render-from-research in CI (principle 17 / no-paid-llm-in-ci.md).
- **`deps-hash-multistack` is OUT of LG-S4 scope** (#1017 scope pointer) — do NOT re-implement the per-manifest deps-hash staleness warning in LG-S4.

**Discipline (from authoritative kickoff §3):**

- Branch per stage, base `staging`. Run `npm --prefix packages/core run test:principles`, `test:backends`, `test:composition` (LG-S4 is doc-only — these are expected green trivially; do not skip, they catch doc-drift).
- `Prior-art:` trailer on the capability commit (LG-S4 carries SSOT rows 218/219 if not already landed — re-grep the SSOT tail at commit time, T20).
- Backward-check per ai-laziness-traps.md T21 (LG-S4: the sibling surface is the OTHER two stacks' adapters — does the closure PR's framing hold for the js ESLint lane + the rust clippy lane + the shared-core planes?).
- §1.7 Forward/Backward in PR body — see §4b below.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to ALL Workers below — self-evaluate)

Workers whose target file matches ANY of the following paths MUST include in the **PR body** (not just commit message):

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

**LG-S4 paths-triggered self-eval:** `done.md` lives under `.claude/orchestrator-prompts/**` (NOT in the path list above); INSTALL-FOR-AI.md is repo-root (NOT in the list). **LG-S4 therefore is NOT §1.7-path-triggered** — UNLESS the Worker edits SSOT (`docs/meta-factory/prior-art-evaluations.md` for rows 218/219) or `agents/rule-researcher.md` (if the per-stack ConventionNode contract is extended). If either is touched → §1.7 applies; otherwise use `### §1.7 Skipped: <rationale ≥60 chars>` (mechanical-maintenance escape hatch).

**Required PR body sections (verbatim shape — CI substance gate is strict):** see `/Users/art/code/rules-as-tests-aif/.zcode/skills/pipeline/templates/meta-kickoff.template.md §4b` (full H3-heading + ≥40-char + file:line-citation rules + pre-flight grep). **Hard rules (each historically failed the gate):** heading depth = H3 (`###`) NOT H2; word «applied» required; ≥40 non-whitespace chars per section body; ≥1 file:line citation per section.

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # must be ≥2 (one per section minimum)
```

**If your sub-wave target file does NOT match the path list above:** ignore §4b. The mandate is path-scoped.

---

## §4c Autonomous aif-handoff dispatch — park-don't-guess contract

> **INERT for this invocation:** the §10 report did NOT offer autonomous dispatch (runtime-bridge probe: `RUNTIME_BRIDGE_MODE` unset → maintainer-paste is the ONLY dispatch channel for LG-S4). Skip §4c. It activates only if a Worker is dispatched via `tsx packages/runtime-bridge/src/cli/dispatch.ts`.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue.

**Active canonical traps for live-generation LG-S4** (per authoritative kickoff §4):

- **T3** — every claim about what LG-S1/S2/S3 shipped carries the merged-PR evidence (#1005/#1006/#1010 + `gh pr list --search 'is:merged'`); the LG-S4 closure PR cites them, not just names them.
- **T11** — any «no upstream generator» or «core not reusable» claim in LG-S4 done.md has the BFR §3 evidence behind it (already run in the R-phase).
- **T12** — the LG-S4 done.md does NOT re-pin ast-grep/ruff/cargo from this doc; it cites the R-phase patch + python-delivery-v0 §0 pins.
- **T15** — LG-S4 is consumer-rule generation closure across three stacks; the `#recursive-self-application-gap` (framework's own rules self-generating) is restated in done.md as an EXPLICIT non-goal, not silently assumed.
- **T16** — name-match check: «LG-S4 ships ecosystem-python.ts» is FALSE by default (Tier-1 is documented gap, not built); the closure PR does NOT claim python Tier-1 works.
- **T20** — the closure verdict (umbrella DONE) carries the §Qe firing probe evidence + the merged PRs in the same turn.
- **T21** — the backward-check enumerates the sibling stacks + shared-core planes, does not restate the LG-S4 diff.

**Domain-specific traps (live-generation-specific, per authoritative kickoff §4):**

- **T-LG-A — «the shared core exists, so just wire it»** — counter: LG-S4 is CLOSURE; if the closure PR starts adding machinery, STOP — that's a different umbrella.
- **T-LG-C — «re-implement the js adapter / re-prove LG-S2»** — counter: #805/#852/#1006 are SHIPPED; LG-S4 done.md cites them, does not re-do them.
- **T-LG-D — «unfreeze the IR for the closure»** — counter: OWNER-FORK-1 is OPEN and is a SEPARATE MT-plane umbrella; LG-S4 does NOT touch `ir/types.ts`.
- **T-LG-E — «pad done.md to look productive»** — counter: done.md is a tight closure record (stages + PRs + non-goals), NOT a marketing doc.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation. This kickoff enumerates T-numbers AND carries ≥1 domain-specific trap.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass `principle 12` (AI-laziness-traps format validation, `packages/core/principles/12-ai-laziness-traps.test.ts`). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer dispatch (between stages)

LG-S4 is the only stage. There is no stage-transition to gate. The Phase -1 cold-review applies to the **LG-S4 PR diff BEFORE merge**:

```text
Agent: Phase -1 cold-review for live-generation LG-S4 (final closure)
Role: reviewer
Skill: requesting-code-review
```

Reviewer discipline: surfaces strategy forks as DECISION-NEEDED (option A → consequence X / option B → consequence Y). Does NOT pick strategy. See `reviewer-discipline.md §2` for the pattern.

**LG-S4 reviewer focus (specific to thin-closure):**

1. Does `done.md` accurately reflect what shipped in #1005/#1006/#1010 (no over-claim, no under-claim)?
2. Is `ecosystem-python.ts` recorded as a documented gap with the owner-trigger condition (NOT silently skipped, NOT silently built)?
3. Does the INSTALL-FOR-AI.md addition cross-link the three stacks' shipped adapters correctly?
4. Is the README draft a DRAFT IN THE PR BODY (not an edit to README.md — Artifact Ownership Contract)?
5. Does the closure PR accidentally re-scope `deps-hash-multistack` back into LG-S4? (STOP line — #1017 extracted it.)

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 In plain words` block whose CONTENT follows the orchestrator-checkpoint substance shape. Substance spec: SKILL.md §10.3a table.

**Anti-duplication discipline:** the block content names sub-waves / AC items / file:line / REPORT-traces / dispatch-state. It does NOT restate per-turn personal-reasoning prose — that is the end-of-turn hook's job.

---

## §8 Stop conditions

- Any `gh` command returns error → halt, escalate.
- Pre-dispatch gate (§3) returns empty for LG-S1/S2/S3 → halt (would contradict the 2026-07-17 verification — re-check).
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- LG-S4 Worker starts writing machinery (code beyond a comment) → STOP (T-LG-A); thin closure = docs only.
- LG-S4 Worker edits README.md directly → STOP (Artifact Ownership Contract; README draft goes in PR body).
- LG-S4 Worker re-implements deps-hash warning → STOP (#1017 scope extraction).
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this kickoff.
- Do NOT finalize project strategy — surface genuine forks to maintainer.
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per no-paid-llm-in-ci.md + BFR-default).
- Do NOT dispatch LG-S4 from the `feat/deps-hash-multistack-dh-s1` branch — LG-S4 branches from `staging`.

---

## §10 See also

- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source.
- `.claude/skills/pipeline/SKILL.md` — generating skill.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/parallel-subwave-isolation.md §1` — worktree isolation.
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
- `.claude/orchestrator-prompts/live-generation/kickoff.md` — AUTHORITATIVE = the `origin/staging` version (post-#1021).
- `.claude/orchestrator-prompts/deps-hash-multistack/kickoff.md` — the EXTRACTED sibling umbrella (NOT LG-S4 scope).
