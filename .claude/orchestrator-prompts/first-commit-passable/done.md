# first-commit-passable — DONE

- Final PR: #1555
- Closed: 2026-09-02
- Summary: Single stage F1 closed the three verified beta BLOCKERs on the shipped first-commit path — issue 1528 (`.gitignore` seed via `copy_safe`, dotless template source because `npm pack` drops dotted files), issue 1529 (`--no-warn-ignored` on both eslint lines of the shipped `.lintstagedrc.json`, `--max-warnings=0` bar unchanged), issue 1530 (`tests/setup.ts` shipped per stack, greenfield tsconfig `include` covers `tests/**/*`, brownfield delivery gated by a tsconfig-coverage predicate in `setup.d/40-configs.sh` with one ⚠ note, `setup.d/lib.sh` untouched). Regression arms: `tests/install-sh/first-commit-passable.test.sh` (21 checks) + the `framework-fresh-install-validate` e2e replay (real pre-commit commit + `npx vitest run` on all four stacks). Executed as aif task `7097ed61`; one hygiene REVISE round (prettier on shipped templates, SC2034, `md5_of` hardening); host-side merge-forward onto staging (8 npm fingerprints regenerated); cold fidelity GO Round 1 (`17d5d238b5`) + narrow delta GO Round 2 (`49b63d51ec`).
- Kickoff-text defect for the concept holder (fidelity `KICKOFF-AMBIGUOUS`, not graded): FC-4 forbids «any other file» in the snapshot diff while its own mandatory `SNAPSHOT_MODE=capture` re-hashes the derived `.ai-factory/refresh-baseline.json` by construction. Resolution taken by the executor: derivative-of-construction, not a leak. FC-4 (and any future payload kickoff) needs an explicit carve-out for install-time-generated manifests, else every payload/format change trips a false «park».
- Notes-lane residue (no failure scenario, recorded for a follow-up if ever material): the `md5_of` no-hasher hard-fail in `tests/install-sh/first-commit-passable.test.sh` runs inside `$(…)` call sites under `set -uo pipefail` without `-e`, so a host with none of md5sum/shasum/sha256sum would still see the byte-identity arms pass vacuously (no CI/acceptance host lacks all three); `INSTALL-FOR-AI.md` sits at 599/600 lines after the merge-forward fold.
