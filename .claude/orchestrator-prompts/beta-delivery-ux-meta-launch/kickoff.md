# KICKOFF — beta-delivery-ux (meta-launch)

> **Type:** execution-build, multi-stage (generated by `/pipeline beta-delivery-ux` on 2026-07-24).
> **Origin:** plan-currency check + launch-table from the `/pipeline` skill.
> **Deliverable:** staged dispatch of umbrella A (track-2 beta program) — install depth
> profiles, pipeline presets/status/workspace command, tier-home doc, GLM one-button,
> contour-skill shipping, npm release mechanics.
> **Base branch:** staging (NOT main — main is prod, manual promote only).
> **Tier:** 2 per [CLAUDE.md «Task-tier routing»](../../../CLAUDE.md) — **NO `bridge-profile`
> marker on any stage dispatch.** Every stage carries real design decisions (profile
> payloads, preset data model, aif API shapes), so the top tier plans each stage and the
> executor tier implements + reviews from below. This is binding from the umbrella kickoff
> header; do not re-litigate it per-stage.

---

## §0 Cold-start context

Read in this order:

1. This kickoff (dispatch scope + stage gates + preflight verdicts).
2. `.claude/orchestrator-prompts/beta-delivery-ux/kickoff.md` — the umbrella kickoff (binding stage table §2, AI traps §4).
3. `docs/superpowers/specs/2026-07-23-beta-program-design.md` — the **design SSOT**. On any
   divergence between this kickoff and the spec, **the spec wins** — surface the divergence,
   never improvise past a binding decision (spec §2 D1-D9 carry falsifiers).
4. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.

---

## §1 Inputs

- **Plan-currency check:** CURRENT (as of 2026-07-24, `03949222c`). No DRIFT items against
  the umbrella's own dependency claims; every §0 preflight resolved mechanically (§1.1).
- **Umbrella kickoff:** `.claude/orchestrator-prompts/beta-delivery-ux/kickoff.md` (on staging, verified `git ls-tree origin/staging`).
- **Binding design spec:** `docs/superpowers/specs/2026-07-23-beta-program-design.md` (on staging).
- **Stale items:** none. Two **dormant residue branches** carry un-merged work on S1's
  surface — they are S1 scope (T17/T18 sweep), NOT a live collision (§1.2).

### §1.1 §0 preflight resolution (all four gates, mechanical evidence)

| Preflight (umbrella kickoff §0) | Verdict | Evidence |
|---|---|---|
| 1. meta-launch amendment (spec §2 D1) merged to staging | **PASS** | PR #1103 MERGED 2026-07-23T14:26:28Z; touched `getff-to-prod-meta-launch/kickoff.md` + the U3-U7 stub kickoffs (`modular-install-fullpack`, `install-hardening-finish`, `plugin-finish`, `generator-into-install`, …) |
| 2. pre-dispatch in-flight probe (track-1 umbrellas, aif queue, install-surface worktrees) | **PASS** | `pipeline-getff-honest-signals-0a1dc9` / `-stack-trace-2e2dd0` / `-foreign-scan-e27372` all `ahead=0` vs staging; open PRs = 1 (dependabot #1096, `fast-uri`, disjoint surface); aif bridge answers `/runtime-profiles` |
| 3. blocked-pending-neighbor stages | **3 of 4 UNBLOCKED, 1 still blocked** | see §1.3 |
| 4. every stage re-verifies its file:line anchors at stage entry | **carried into each stage AC** | spec anchors measured at `812268ac2`/`d534019b3`; staging is now `03949222c` |

### §1.2 U3 residue — dormant, S1-scoped, NOT a live collision

Two branches carry un-merged install-surface work. Both are **dormant** (≥28 days, zero open
PRs, no active session) and both are **inside S1's chartered scope** («absorbs U3 — run the
T17/T18 residue sweep of `mif-s3-integ` FIRST»). S1 MUST inspect them before writing anything
to `install.sh` / `setup.d/`:

| Branch | Ahead of staging | Last commit | Surface |
|---|---|---|---|
| `mif-s3-integ` (= `origin/mif-s3-revive-toolbootstrap`; worktree `…/hungry-kowalevski-92006e/.claude/worktrees/mif-s2s3`) | 5 commits | 2026-06-26 | tool-bootstrap layer revival, static stack column, `setup.d/05-mcp.sh` 5-field manifest read, `audit-self.yml` wiring |
| `feature/modular-install-fullpack-f6366e` (worktree `…/peaceful-swirles-c2b560`) | 2 commits | 2026-06-24 | `install.sh` → `setup.d/**` modularization + `PKG_ROOT` BASH_SOURCE fix; **likely already superseded by merged S1/S2 — verify, do not assume** |

**T17/T18 discipline (binding for S1):** preserve future-value residue BEFORE any
supersede/delete call; verify redundancy **empirically** (diff against staging), never from
the branch name or this table. Keeping is reversible; deleting is not.

### §1.3 blocked-pending-neighbor — re-evaluated at dispatch time

| Neighbor gate (umbrella §0.3) | Status | Evidence |
|---|---|---|
| S3 (A3) needs the acceptance-contour **CLAUDE.md** amendment | **UNBLOCKED** | `git show origin/staging:CLAUDE.md` line 116 carries the «Exception (acceptance-contour spec D1)» paragraph |
| S2 (A4) marker semantics need the **acceptance-contour spec** | **UNBLOCKED** | `docs/superpowers/specs/2026-07-23-acceptance-contour-design.md` on staging; its D1 **precondition** also holds — `fidelity-verdict-in-pr-body` IS a REQUIRED check in staging branch protection (`gh api …/branches/staging/protection`) |
| go-lane rows need **adapter-jig J3** | **STILL BLOCKED** | J1 #1087 + J2 #1094 merged; **J3 not merged** (`git log origin/staging` shows no J3). Spec §8: «J3 owns `setup.d/47-go.sh` — A1 profiles list it only after J3 merges» |

**Consequence (binding, narrow):** S1's per-profile payload inventory **descopes the go lane**
— js / python / cargo only. This is a row-level descope, not a stage block: S1, S2, S3, S4, S5
and R1 are ALL startable. When J3 merges, the go row is added by a follow-up commit (or by R1's
matrix cell if still open then). Any stage that finds itself needing `setup.d/47-go.sh` → STOP
and surface; do not create it here.

---

## §2 Launch-table

Launch table — beta-delivery-ux (as of `03949222c`, 2026-07-24):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| S1 — Install profiles (A1) | execution-build | Mode A (SOLO gate) | Yes | 1 | — (serialized gate) | L |
| S2 — Presets + status + workspace cmd (A4/A5/A9) | execution-build | Mode B (worktree) | Yes | 2 | S3, S4, S5 | L |
| S3 — Tier-home + degradation matrix (A3) | execution-build (doc + cross-owner pointer) | Mode B (worktree) | No | 2 | S2, S4, S5 | M |
| S4 — GLM one-button + aif companion install (A2) | execution-build | Mode B (worktree) | Yes | 2 | S2, S3, S5 | M |
| S5 — Contour skills + foreign dispatch (A8/A7) | wiring + execution-build | Mode B (worktree) | Yes | 2 | S2, S3, S4 | M |
| R1 — npm release mechanics (A6) | execution-build | Mode A | Yes | 3 | — | M |

**Routing derivation (§2.5):** `classify-work.sh` → `TYPE=I-phase-large`, `SURFACES=14`,
`DISPATCH=Mode-B`. Predicates: `scope_decided=TRUE` (spec §2 D1-D9 + §4 A1-A9 binding),
`SURFACES≥2=TRUE`, `parallel_safe=TRUE` (explicit `S2 ∥ S3 ∥ S4 ∥ S5` declaration in the
umbrella kickoff §2). → **ALIAS = DECOMPOSE / Mode-B**, with S1 and R1 as serialized
single-worker bookends around the Stage-2 fan-out.

**SDD=No rationale for S3 only:** the stage is one shipped doc + one pointer-ization commit
— under the ≥3-independent-task threshold, review overhead would exceed catch-rate. Its
cross-owner CLAUDE.md edit is instead gated by maintainer sign-off (§3 Stage 2→3 gate).

### §2.1 ATTN — Stage-2 shared-surface contention (read before dispatching the fan-out)

The four Stage-2 stages are declared parallel by the umbrella kickoff (binding), but they are
**not surface-disjoint**: S2 (ship `scripts/create-worktree.sh`), S3 (tier-home doc into the
`env`+`factory` payloads), S4 (`companions.manifest` aif row upgrade) and S5
(`setup.d/10-skills.sh` shipped-skill set) all append rows to the S1 payload machinery.
Semantically these are additive list edits — low conflict risk — but textually they WILL
collide on `setup.d/10-skills.sh` and the profile payload manifest.

**Binding mitigations for every Stage-2 worker:**

1. Each worker runs in its **own git worktree** (`bash scripts/create-worktree.sh <name>`) per
   [parallel-subwave-isolation.md §1](../../rules/parallel-subwave-isolation.md). Never the shared checkout.
2. Before push: `git fetch origin staging` + **merge-forward** (merge staging INTO the branch,
   plain fast-forward push). **Never `git rebase` a published PR branch** — force-push is
   classifier-blocked for agents in every form. Full recipe:
   [git-conflict-merge-forward.md §2](../../rules/git-conflict-merge-forward.md).
3. Payload-manifest edits are **append-only additions of that stage's own rows**. A worker that
   finds itself rewriting or reordering another stage's rows → STOP and surface.
4. If a merge-forward surfaces a conflict outside generated artefacts
   (`tests/install-sh/baselines/`, `plugin/hooks/`) → **semantic conflict** → STOP, park, surface.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → Stage 2

Stage 2 dispatch waits for the S1 PR to be merged to `staging`:

```bash
gh pr list --search "is:merged head:beta-a-s1-install-profiles base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: non-empty JSON. If empty → **halt**. Do NOT dispatch S2/S3/S4/S5 — every one of them
consumes S1's profile machinery and payload inventory.

### Stage 2 → Stage 3

R1 dispatch waits for ALL four Stage-2 PRs:

```bash
for b in beta-a-s2-presets-status-work beta-a-s3-tier-home beta-a-s4-glm-onebutton beta-a-s5-contour-skills; do
  echo "--- $b ---"
  gh pr list --search "is:merged head:$b base:staging" --json number,title,mergedAt,headRefName --limit 5
done
```

Expected: four non-empty results. Any empty → halt; R1's `files` allowlist + tarball matrix
cell must measure the FINAL shipped payload, not a partial one.

**Additional Stage 2→3 gate — maintainer sign-off on the S3 cross-owner edit.** S3's
CLAUDE.md «Task-tier routing» pointer-ization is a cross-owner edit under the
[Artifact Ownership Contract](../../../CLAUDE.md). It ships as a **separate atomic commit**
with its own rationale, and R1 does not dispatch until the maintainer has signed off on that
specific commit. Same treatment as the meta-launch amendment (spec §9, r3).

### Umbrella closure

After R1 merges, the merging session writes `.claude/orchestrator-prompts/beta-delivery-ux/done.md`
per the [CLAUDE.md umbrella-closure convention](../../../CLAUDE.md) (schema: `# <umbrella> — DONE` /
`- Final PR: #<num>` / `- Closed: <YYYY-MM-DD>` / `- Summary: <one-line>`).

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7).

---

## §4 Sub-wave dispatch instructions

Every stage: **one PR onto `staging`**, branch name as in §3, top tier plans / executor tier
implements + reviews, **no `bridge-profile` marker**. Every stage re-verifies its spec
file:line anchors live at entry (preflight 4) and re-runs the in-flight probe (preflight 2).

### Stage 1 — S1 Install profiles (A1)

- **Scope:** three named profiles (`core` / `env` / `factory`) over the existing flag machinery;
  one-line consequence text per profile (rustup vocabulary, ADOPTED as a *pattern* — verify
  against OUR stateless `--refresh` regen semantics, not rustup's additive components);
  `--profile` flag + TTY menu + INSTALL-FOR-AI dialog default; the **per-profile payload
  inventory**.
- **Do first (T17/T18):** the U3 residue sweep of `mif-s3-integ` and
  `feature/modular-install-fullpack-f6366e` (§1.2) — preserve, then supersede. Empirically.
- **Payload inventory is the deliverable's spine.** It explicitly includes the convenience +
  guard hook set already shipping (end-of-turn recap, ask-question reminder,
  inject-matching-rule, deps-hash staleness + the `.husky` pre-commit/pre-push chain) — no
  shipped comfort/shield may be lost between profiles. It **RE-TRIAGES** today's
  dogfood-vs-consumer hook split rather than inheriting it: at `env`/`factory` depth the
  consumer authors their own kickoffs and AI docs, so contour-guard hooks
  (`check-kickoff-traps`, doc-authority checks) become consumer-relevant shields there —
  **per-hook verdict recorded per profile, not assumed**.
- **Descope:** go lane (§1.3). Lanes covered: js / python / cargo.
- **AC:** fresh-machine smoke per profile (command + output, not prose); `--refresh` upgrade
  path proven; existing flag back-compat kept.

### Stage 2 — parallel fan-out (read §2.1 ATTN first)

**S2 — Presets + status + workspace one-command (A4/A5/A9)**

- 4 declarative presets (`aif` / `night` / `economy` / `sdd`) in pipeline `references/` as
  **data, not prose**; list verb; `--preset <name>` flag (flag/env first per clig.dev); TTY
  proposal row in the §3 launch-table. Menu-only UX is REJECTED (breaks agents/CI).
- `/pipeline status`: read-only, sectioned — in-factory (bridge REST) / parked questions
  (`questions.ts`) / ready-to-harvest + PR state (`gh pr list`) — ending with
  suggested-next-command lines. **NOT a dashboard.**
- A9 one-command: **REUSE `scripts/create-worktree.sh`** (do not rewrite it) + ship it +
  per-harness session start. CC → **defer entirely to the native worktree flow** (desktop app
  / `claude -w`), no wrapper involvement. Flag-first; non-TTY prints instead of launching.
- **Marker-emitting presets:** `economy` and `aif` emit the `bridge-profile` marker. Semantics
  per acceptance-contour D1 — both branches are now UNBLOCKED (§1.3). The marker **value must
  be the profile's full display name, UNIQUE** under the resolver's case-insensitive substring
  match — verify against the live list (`curl -s "$RUNTIME_BRIDGE_AIF_URL/runtime-profiles" | jq -r '.[].name'`)
  at authoring time; an ambiguous value aborts dispatch with `dispatch_failed` (3 prior
  recurrences — see [CLAUDE.md «Marker value rule»](../../../CLAUDE.md)).
- **AC:** preset activation works flag-only (non-TTY); status renders all three sections
  against live bricks; A9 smoke on CC **and** one non-CC harness.

**S3 — Tier-home + degradation matrix (A3)**

- Shipped, AI-agnostic doc carrying Tier 0/1/2 criteria + the full degradation table (no aif →
  in-session SDD; no GLM → tiers slide; no Fable → Opus tops; non-CC harness → per-artifact
  degradations). Payload home is **open fork F-A′** (`.ai-factory/` doc vs skill-context) —
  decide in-stage on which the C1 AGENTS.md pointer + non-CC harnesses read most cheaply;
  record the decision with its rationale.
- CLAUDE.md «Task-tier routing» becomes a **pointer** to the shipped SSOT → separate atomic
  commit, maintainer sign-off (§3).
- **AC:** doc ships in `env`+`factory` payloads; AGENTS.md template points at it (C1 contract).

**S4 — GLM one-button + aif companion install (A2)**

- aider-pattern flow: detect missing executor profile → ONE explanation (z.ai Coding Plan) →
  human pastes ONE key into an untracked env location → automation does the rest (create the
  aif runtime profile via REST, set per-mode defaults Plan→top / Task+Review→executor, run a
  validation ping, ship the glm-handoff skill).
- **The installer never handles the key value.** Executor of the flow is an AI session driven
  by an INSTALL-FOR-AI step + a bash helper — no GUI/wizard (BFR cost gate). REFERENCE
  claude-code-router / LiteLLM as mapping-layer precedents; we do not rebuild routing.
- **aif REST field shapes are designed-not-proven** (checkout dated 2026-05-26) — **verify live
  FIRST** against the running aif before building on them.
- `factory` also upgrades the aif-handoff `companions.manifest` row from detect+instruct to a
  **consented guided INSTALL** (official repo, docker compose, detect-first; decline → graceful
  `env`-level degradation). Opt-in, never a hard dependency
  ([companion-install-principle.md](../../rules/companion-install-principle.md)).
- **AC:** live end-to-end on a clean machine against a running aif. **Degrading to guided
  manual steps counts as an objective-3 MISS, not a pass** (spec §4 A2, r2).

**S5 — Contour skills + foreign dispatch (A8/A7)**

- A8: **wiring-only** additions of `/arch` + `claude-glm-executor-handoff` to
  `setup.d/10-skills.sh` (env/factory). **NO content edits to `/arch` or glm-handoff** — three
  parallel sessions touch them; rebase on whatever staging holds.
- A7: `factory` vendors the runtime-bridge subset (CLI entrypoints + dispatch hook,
  env-parameterized) into the consumer repo; dedup-log path becomes per-project. npm packaging
  of the bridge stays deferred (U9, post-announce).
- **AC:** agnosticism harness green over the widened shipped set; vendored dispatch smoke on a
  non-framework repo.

### Stage 3 — R1 npm release mechanics (A6)

- Name freeze `@getff` family, bin `getff` (absorbs U11 + its gate «имена заморожены ДО
  публикации»); `files` allowlist validated by a **real tarball matrix cell**; bin runnability
  decision (`.ts` bins: tsx dependency vs prebuild — fork F-C′, decided in-stage); package
  README/LICENSE/metadata; release notes via the existing release-drafter.
- **`npm publish` is NOT executed** — it stays an operator act in release-frame phase 2.
- Binding input: `.claude/orchestrator-prompts/launch-preannounce-track/s6-u10-handoff.md`
  (549-file over-ship; unpublish-is-not-rollback; publish-under-`@getff`-only).
- **AC:** `npm pack` tarball installs + runs in the matrix cell.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered — applies to EVERY stage below; self-evaluate)

**Every stage of this umbrella touches at least one triggering path** (`setup.d/**` +
`packages/core/templates/**` for S1; `.claude/skills/**` for S2/S5; `CLAUDE.md` +
`agents/**` for S3; `.claude/skills/**` for S4). Treat the mandate as ON for all six stages
unless your final diff provably touches none of:

- `.claude/rules/**` · `packages/core/principles/**` · `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md` · `CLAUDE.md` · `packages/core/templates/**`
- `.claude/skills/**` · `agents/**`

**Required PR body sections (verbatim shape — the CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- doc-authority-hierarchy.md — <what was checked>. file:line evidence: `<path>:<line>`
- phase-research-coverage.md §1.12 — <what was checked>. file:line evidence: `<path>:<line>`
- build-first-reuse-default.md — <verdict + rationale>
- no-paid-llm-in-ci.md — <verdict>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under this change's scope>

- <existing-artefact-name> — <how this PR interacts; supersedes / extends / orthogonal>. file:line evidence: `<path>:<line>`
- <repeat for each swept sibling surface>
```

**Hard rules (each has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2.** The awk regex matches `^### §1\.7 …`; H2 silently
   fails while rendering plausibly. (6+ recurrences.)
2. **The word «applied» is required.**
3. **≥40 non-whitespace chars in EACH section body** (awk-counted, post-strip).
4. **≥1 file:line citation per section**, matching `[^[:space:]]+\.[a-z]+:[0-9]+`.
5. **Escape hatch:** `### §1.7 Skipped: <rationale ≥60 chars>` — purely structural PRs only
   (snapshot regen, rename). Must state *why* there is no discipline surface.

**T21 discipline (binding here — the Backward-check is where this umbrella will fail):** your
Backward-check must **enumerate sibling surfaces the diff did NOT touch** and verdict each
(`SWEPT-CLEAN(evidence)` / `GAP-FOUND(action)`). For this umbrella the class-surfaces are:
`install.sh`, `setup.d/**`, `INSTALL-FOR-AI.md`, the plugin channel (`plugin/hooks/**`), the
zcode twins, and `packages/core/templates/**`. A Backward-check whose surface list equals your
own diff's file list is **non-conformant by format**. Prefer delegating the sweep to the cold
[`agents/backward-sweep-auditor.md`](../../../agents/backward-sweep-auditor.md) — hand it only the
change's *class*, never the diff.

**Pre-flight grep — run BEFORE `gh pr create` (compose `$PR_BODY` first, then check):**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'   # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c   # ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c   # ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'   # ≥2
```

Use `--body-file` (not `--body`) so the local git-safety hook can validate the §1.7 sections
reliably. If the hook blocks — fix the body per its message; never route around it via the API.

### §4b.1 `## Fidelity verdict` — REQUIRED on every stage PR (acceptance-contour D3)

**Discovered live during this meta-launch (PR #1122, 2026-07-24):**
`fidelity-verdict-in-pr-body` is a **REQUIRED status check** on `staging` — it is the same
gate whose registration unblocks S2's marker-emitting presets (§1.3). Every `base=staging` PR
must carry exactly ONE `## Fidelity verdict` section with exactly ONE `FIDELITY:` line.
Enforcement: [`packages/core/hooks/checks/pr-body-fidelity.ts`](../../../packages/core/hooks/checks/pr-body-fidelity.ts)
via [`.github/workflows/pr-body-fidelity.yml`](../../../.github/workflows/pr-body-fidelity.yml).

**`FIDELITY: skipped` is NOT available to a stage PR.** The checker's `declaresProvenance()`
reads the PR body's `## Provenance` section: any non-placeholder, non-`n/a` content there makes
the PR a stage PR, and a stage PR MUST carry a real verdict produced by
[`agents/fidelity-auditor.md`](../../../agents/fidelity-auditor.md). **All six stages of this
umbrella are stage PRs** — plan for the GO block, not the escape hatch.

**Required GO grammar (all four, case-sensitive tokens):**

```markdown
## Fidelity verdict

FIDELITY: GO
Basis: .claude/orchestrator-prompts/beta-delivery-ux/kickoff.md
Round: 1
Audited-SHA: <PR head SHA, 12-40 hex — MUST equal the head at merge time>

<≥1 file:line evidence reference on a line OTHER than `Basis:`>
```

**Failure modes that will bite (each is an explicit fail-closed branch in the checker):**

1. **`Audited-SHA` must match the CURRENT PR head.** Any push after the audit invalidates it —
   re-run the audit and update the SHA. This is the most likely repeat failure on a stage that
   takes review rounds.
2. **A rework round REPLACES the block, never appends.** Two `## Fidelity verdict` sections, or
   two `FIDELITY:` lines in one section, is a hard fail — so an appended `GO` cannot shadow a
   round-1 `REVISE`.
3. **A recorded `FIDELITY: REVISE` / `STOP` is terminal** — resolve the rework loop and replace
   the block before merge.
4. **Evidence cannot be borrowed.** ANY heading closes the section, so a `file:line` living in a
   neighbouring `### §1.7 …` block does not count; and the `Basis:` path itself is excluded from
   satisfying the evidence requirement.
5. **HTML comments are stripped before parsing** — commented-out template text never passes.

---

## §4c Autonomous aif-handoff dispatch — park-don't-guess contract

> **Live only when a stage is dispatched via the runtime-bridge instead of a maintainer-paste
> tab.** If every stage runs in a pasted CC tab, §4c is inert. **If any stage goes through
> `dispatch.ts`, §4c is MANDATORY.**

**Why:** aif-handoff agents have **no mid-implementation «pause and ask» primitive**. They
implement — *guessing* on any ambiguity — then auto-review post-hoc. Auto-close fires when the
review finds no blocking findings, which means «review found no blockers», NOT «a human is sure
it's right». A genuine design fork is **not recognised as a question** — aif picks and proceeds.
This umbrella is dense with genuine forks (F-A′ payload home, F-C′ bin runnability, per-hook
profile verdicts, aif REST field shapes), so without the levers below aif will decide them
wrong, silently.

**Lever 1 — conservative aif config (set BEFORE dispatch):**

```bash
export AGENT_MAX_REVIEW_ITERATIONS=1
export AGENT_AUTO_REVIEW_STRATEGY=closure_first
export AGENT_SKIP_REVIEW=false
```

**Lever 2 — park-don't-guess instruction (carried verbatim to the aif agent):**

> **aif agent — fork discipline (non-negotiable):** On ANY genuine fork or ambiguity (two
> defensible implementations, an undecided design choice, a missing spec detail that changes
> behaviour) — **do NOT pick.** Park it as a question (set the task to `manualReviewRequired` /
> `blocked_external` with the fork stated as «Option A → consequence X / Option B → consequence
> Y») and **stop that task.** Proceed only on the unambiguous parts. Guessing a fork to "keep
> moving" is the failure this whole loop exists to prevent.

**Lever 3 — the resolve loop reviews aif's DECISIONS, not only its open questions.** Approve /
request-changes each completed task; «questions» = open forks **+** autonomous decisions
awaiting verification.

**Pre-dispatch gate:** `grep -qi 'park it as a question' <this-kickoff>` **and**
`[ -n "$AGENT_MAX_REVIEW_ITERATIONS" ]`. Either missing → STOP; do not dispatch autonomously.

**Egress gate (mandatory after `status=done`):** aif does NOT push or open PRs by design.

```bash
npx tsx packages/runtime-bridge/src/cli/harvest.ts <taskId> --base staging
```

Anti-pattern `#autonomous-done-no-harvest`: work reaching `done` without harvest stays in the
container forever.

---

## §5 AI-traps active (per [ai-laziness-traps.md §3](../../rules/ai-laziness-traps.md))

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue.

**Active canonical traps for beta-delivery-ux:** **T3, T7, T16, T17, T19, T20, T21** (inherited
verbatim from the umbrella kickoff §4) **+ T10, T13** added by this meta-launch.

- **T3** — every «works» claim carries command + output. Fresh-machine smokes, live aif pings,
  `npm pack` runs — not prose. No prose-only findings.
- **T7** — do not pattern-match the spec's stage table into checkbox theater. The gates are
  live-fired evidence, not section headers.
- **T10** — *(added here)* report completeness against what EXISTS, not what you looked at. S1's
  payload inventory must **enumerate the shipped hook/skill/template population first**, then
  verdict each per profile. An inventory built from the hooks you happened to open is a T10
  violation dressed as a deliverable.
- **T13** — *(added here)* ADOPTED ≠ zero-work. rustup profiles, the aider onboarding pattern,
  clig.dev flag-first, `scripts/create-worktree.sh` — each is adopted for OUR problem class;
  confirm the upstream evidence transfers, or escalate to own-build audit depth.
- **T16** — rustup / aider are ADOPTED **patterns**, not code. Verify our regen-upgrade and
  one-key flow against OUR install semantics. Write the explicit line: «Upstream problem class:
  X. Our problem class: Y. Match? evidence: …».
- **T17** — the U3 residue sweep happens BEFORE supersession (destructive-delegation guard).
  Preservation is the dispatcher's job, not the worker's — §1.2 is that preservation record.
- **T19** — own adversarial cold-QA of every stage diff before handoff. CI ≠ design review.
- **T20** — verdicts (F-A′ home, F-C′ bin strategy, per-hook profile calls) carry file:line or
  command-output evidence in the same turn they are issued.
- **T21** — Backward-checks enumerate **sibling surfaces**, not the diff. See §4b.

**Domain-specific traps (beta-delivery-ux-specific, NOT in the canonical catalogue):**

- **T-BDU-A — «profile payload looks complete because the happy stack installed».** *(inherited
  from the umbrella kickoff §4)* Payload completeness is judged against the per-profile
  inventory — including the convenience + guard hook set — on ALL covered lanes
  (js / python / cargo; go descoped per §1.3), never against one demo run.
- **T-BDU-B — «degradation counted as a pass».** *(added here)* Every stage here has a
  degrade-to-manual escape (A2 guided manual, A9 print-instead-of-launch, `factory` decline →
  `env`). Only A9's print-fallback is a designed success path. A2 degrading to guided manual is
  an **objective-3 MISS** (spec §4 A2, r2); a `factory` aif install that silently lands at `env`
  depth without recording the decline is a MISS too. Report degradations as misses, never as
  neutral fallbacks.
- **T-BDU-C — «the neighbor gate is probably clear by now».** *(added here)* §1.3 is a snapshot
  taken at `03949222c`. Each stage RE-CHECKS its own neighbor gate at entry — especially the go
  lane / J3 row. «It was unblocked in the meta-kickoff» is not evidence at stage-entry time; nor
  is the reverse (a stage refusing to start on a gate that has since cleared).

> **Anti-pattern warning:** a blanket «see ai-laziness-traps.md» without the enumeration above
> is itself a T7 violation. Kickoff authors MUST enumerate T-numbers AND add ≥1 domain-specific
> trap.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass principle 12 (AI-laziness-traps
format validation):

```bash
npx vitest run packages/core/principles/12-ai-laziness-traps.test.ts | tail -5
```

It must also pass principle 29 (worker-dispatch-channel) — this kickoff dispatches Workers to
maintainer-pasted CC tabs or the runtime bridge, **never via the Agent tool**. The Agent tool is
reserved for read-only Phase -1 review (§7).

---

## §7 Phase -1 reviewer dispatch (between stages)

Before dispatching Stage N+1, invoke:

```text
Agent: Phase -1 cold-review for beta-delivery-ux Stage N
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

The reviewer reads `git diff staging...<stage-N-head>` + this kickoff's Stage-N AC, then emits
**GO / REVISE / STOP** with BLOCKER / MAJOR / MINOR classification. On any finding requiring a
strategy choice it emits «DECISION-NEEDED: <one-line>. Option A → consequence X. Option B →
consequence Y. Maintainer decides.» — it does **not** pick the strategy, does not edit files,
does not approve on the maintainer's behalf, and does not skip surface review because CI is
green (T19).

**Verdict routing:** GO → Stage N+1 dispatch. REVISE → surface findings, worker fixes, repeat
Phase -1. STOP → escalate, halt.

**Stage-2 note:** the fan-out produces four diffs. Review them **per-stage as each lands**, not
as one bundle — a bundled review over four disjoint concerns is where the T21 restatement
failure mode concentrates.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/pipeline/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict,
mid-stage status, final handoff) ends with a `## 🟢 In plain words` block whose CONTENT follows
the orchestrator-checkpoint substance shape — it names stages, AC items, file:line evidence,
REPORT traces, dispatch state. It does NOT restate per-turn personal-reasoning prose; that is
the end-of-turn hook's job. If your block is verbatim-copyable from `end-of-turn-reminder.sh`,
you have drifted into `#two-prompts-drift` — replace it with orchestrator-specific substance.

Skip allowed only for one-line acknowledgements and raw `gh`/`git` output handed back without
commentary.

---

## §8 Stop conditions

- Any `gh` command errors during a Stage-2 parallel transition → halt, escalate.
- A stage gate (§3) returns empty while Stage N was expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface.
- Any stage returns a BLOCKER finding → halt that Stage; surface before proceeding.
- **A stage needs `setup.d/47-go.sh` or a go-lane row** → halt (J3 not merged, §1.3).
- **A stage's design decision would diverge from the binding spec** → STOP and surface the
  divergence; never improvise past a binding decision (umbrella kickoff §5).
- **A Stage-2 merge-forward surfaces a conflict outside generated artefacts** → semantic
  conflict; park and surface (§2.1 item 4).
- **S4 finds the aif REST field shapes differ from the spec's designed-not-proven sketch** →
  that is expected; record the live shapes and proceed, but do NOT silently redesign the flow.
- Any temptation to edit `~/.claude/skills/orchestrator/` → violation; stop.

---

## §9 Anti-scope

- Do NOT write stage implementation code in this kickoff.
- Do NOT finalize project strategy — surface genuine forks (F-A′, F-C′) to the maintainer.
- Do NOT edit `/arch` or `claude-glm-executor-handoff` **content** (A8 is wiring-only).
- Do NOT touch killer-layer code (generation, trust threading, freshness engines — track 1).
- Do NOT touch aif-handoff internals (acceptance walls stand; REST-consumer automation only).
- Do NOT do U9 repo split or bridge npm packaging (post-announce).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps beyond what a stage's binding spec section requires.
- Do NOT execute `npm publish` (operator act, release-frame phase 2).

---

## §10 See also

- `.claude/orchestrator-prompts/beta-delivery-ux/kickoff.md` — the umbrella kickoff (binding stage table).
- `docs/superpowers/specs/2026-07-23-beta-program-design.md` — design SSOT (§2 D1-D9, §4 A1-A9, §8 Lego map, §9 gates).
- `docs/meta-factory/research-patches/2026-07-23-beta-program-decisions.md` — decision record.
- `.claude/orchestrator-prompts/launch-preannounce-track/s6-u10-handoff.md` — R1 binding input.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/parallel-subwave-isolation.md §1` — Stage-2 worktree isolation.
- `.claude/rules/git-conflict-merge-forward.md §2` — the merge-forward recipe (§2.1 item 2).
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
- `.claude/rules/companion-install-principle.md` — S4 `factory` aif install consent model.

<!-- host-verify: none — legacy closed umbrella (done.md): work already accepted; no live host acceptance to declare — retro-marked 2026-08-21 -->
