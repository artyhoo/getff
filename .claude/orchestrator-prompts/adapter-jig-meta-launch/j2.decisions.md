# J2 decisions log — night-mode autonomous run (2026-07-22)

> Per night-mode delta item 1: technical forks resolved autonomously with recorded rationale;
> genuine owner forks would be logged WITHOUT deciding. All entries below are TECHNICAL
> (determinable on the project's merits) — zero owner forks so far.

## Resolved technical forks

| # | Fork | Resolution | Rationale (evidence) |
|---|---|---|---|
| 1 | DN-J1 drift probe: extract shared delivery-cell lib vs per-lane glue | **per-lane glue (trivial verdict)** | Measured (diff + function-by-function structural compare): shared F7/F8 cells are faithful name-swap mirrors (`_log`/`_copy_or_refresh`/tail-guard identical post-normalization); 49% divergence = §2.2 FREE-row lane specialization a library cannot absorb; 100% of W2-W5 MAJORs lived in the per-lane corner bodies that stay hand-written either way; does not clear the 2B-standardize bar (85% byte-identical there vs ~51% here). Matches the spec's recorded default. |
| 2 | Registry placement + slot | **`packages/core/principles/33-adapter-jig-arm-registry.{ts,test.ts}`** | Slot 33 = lowest free (verified `ls`, 01-32 contiguous). Mirrors principle-31 data+test split idiom. Inside EXISTING principles suite → §8 «no new runners» holds literally. |
| 3 | Registry growth strategy | **append-per-increment; count===22 asserted in final increment only** | Keeps branch green after every commit (night-mode item 6: reviewers read committed state); a 22-row registry landed upfront with zero markers would be RED from increment 1. |
| 4 | A2 scope: detector-level only vs detector + adapter-selection seam | **both** | The production seam A2 judges is `resolveCtxForRoot` (spec §3.1); detector-only coverage leaves the adapter-selection precedence (npm beats pip/cargo under skipAif) unpinned — recon confirmed zero polyglot fixtures at that seam. |
| 5 | B1 «production entrypoint» reading: function-level vs process-level | **function-level (`validateResearchPlan` + FF2011) with the cli.ts:70→107 wiring cited** | FF2011 lives at `allowlist-resolver.ts:313`; process-level CLI spawn adds cost without adding discrimination — the wiring line is a stable grep-able seam. |
| 6 | C1 target: extend `cargo-entry-lane.test.sh` vs new file | **extend** | Spec-preferred «inside existing suites»; python precedent (`python-delivery.test.sh`) already models the cell-matrix shape. |
| 7 | C3 realization: behavioral RED-proof vs mechanical meta-grep | **behavioral** (mutate delivered file → compare run → assert RED) | A meta-grep over exclusion lists is `#discipline-theatre`-adjacent; the behavioral proof is exactly the arm's RED-proof column in spec §3.3. |
| 8 | C4 closing depth: sweep vs loud-report | **loud-report minimum; sweep only if genuinely cheap** | Spec C4 row: «swept (or loudly reported), never silently left active» — both compliant; loud-report is the minimal-risk overnight change; full sweep touchpoints recorded for J3+/follow-up if executor finds it non-cheap. |
| 9 | G3 protected surfaces: 2 of 3 don't exist in this repo | **prefix-based diff-scope guard, absent-path tolerant** | `.claude/skills/rule-tests/` + `agents/rule-test-author.md` are rule-tests-surface S1 FUTURE artifacts (adapter-jig kickoff §0 names them as its STOP lines). A prefix guard is valid for nonexistent paths — creating them in a wiring PR is also an intersection and still REDs. |
| 10 | H3 realization: extend unwired-debt.test.ts vs new shared module | **co-locate H1+H3 in `ecosystem-unwired-debt.test.ts`; extract census regexes only if the executor needs them in both files** | Both arms guard the same census; one file = one seam (recon recommendation). |
| 11 | P1 scope | **getff-SHIPPED-and-pinned tools only (ast-grep, ruff)** | Recon: parity arm must not claim tools the framework does not pin on both surfaces; wider scope would fabricate an invariant F10 does not state. |
| 12 | E3 population definition | **enumerate firing lanes by presence of the capability-matrix `checkToolchainFreshness` wiring (executor confirms exact sentinel from recon-group-E.json)** | Population-by-artifact-presence matches the H3/principle-21 census idiom; avoids a hand-maintained list. |
| 13 | Increment order | REG → A → B → H → G → D → E → C → P → FIN | vitest-only groups first; bash/setup.d groups (D/E/C) later so snapshot regens batch; FIN = CI-wiring verify + DN-J1 research-patch + count===22 + full local gate run. |
| 14 | D1 lane scope: python-only vs add a cargo regenerate-on-refresh positive | **python-only** | Recon group-D recommendation: the D1 incident class (W3 flag-path stale + W5 plain-pass-join stale) lives entirely in the python join path; cargo achieves never-stale structurally by always-write (46-cargo.sh `cat > lock`, no skip guard) — there is no guard to judge, and a cargo "refresh regenerates" positive would assert copy semantics already covered by cargo-entry-lane (4)/(5b). Registered D1 = python arms 6 (pos) + 9/11 (neg). |
| 15 | D3 cargo `ruleIds` content: `[]` vs derived clippy lint names | **`[]` with in-writer comment** | Recon group-D recommendation: F11 core parity is NAME-presence, not content; cargo's ban surface is clippy TOML lint config (disallowed-methods entries), not named ast-grep rule ids — deriving pseudo-ids would fabricate identifiers no other surface consumes. Secondary fingerprint value-format divergence (python bare 16-hex vs cargo algo-prefixed) left as-is: D3 set-compares names, not values; noted for a future normalization pass. |

## Owner forks (logged, NOT decided)

_(none so far)_
