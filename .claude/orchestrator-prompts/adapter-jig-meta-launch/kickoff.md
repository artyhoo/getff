# KICKOFF — adapter-jig (meta-launch)

> **Type:** execution-build, multi-stage (Tier 2 — top tier plans, executor implements + reviews). Generated by `/pipeline adapter-jig` on 2026-07-22.
> **Origin:** plan-currency check + launch-table from `/pipeline` skill.
> **Deliverable:** the adapter-factory conformance jig lands its 22-arm suite inside existing suites (J2, retrofit-run against npm/python/cargo + DN-J1 drift probe) and stamps the **go** family end-to-end THROUGH the jig (J3, atomic). J1 (frozen contract F1-F11 + cold-review agent + SSOT #226) is already merged (#1087).
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + stage gates).
2. `.claude/orchestrator-prompts/adapter-jig/kickoff.md` — the umbrella kickoff (BINDING scope; §0 dispatch gate, §1 stages, §5 STOP lines).
3. `docs/superpowers/specs/2026-07-22-adapter-jig-design.md` — the BINDING design spec (contract F1-F11, 22-arm suite §3, forks DN-J1/DN-J2 with recorded defaults, §9 J3 DoD).
4. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.

---

## §1 Inputs

- **Plan-currency check:** CURRENT (re-grounded against `origin/staging` @ `06174f286`, 2026-07-22). See §1.1 details below — the `priority-score.sh` `DONE done_pr=1082 basis=jaccard` signal is a **verified false-positive** (PR #1082 = `worktree-ecosystem-wiring-w5` "researched-python live path", unrelated to adapter-jig). Umbrella is IN FLIGHT with J1 merged, J2/J3 open.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/adapter-jig/kickoff.md` (on `origin/staging` @ #1083).
- **Merged progress:** J1 = #1087 (`worktree-adapter-jig-j1`) — frozen F1-F11 contract + `agents/adapter-jig-reviewer.md` + SSOT #226; umbrella kickoff + binding design = #1083.
- **In-flight probe (2026-07-22, 0 open PRs):** `ecosystem-wiring` DONE (#1086 done.md — no open lane-touching W-stage PR; J2 retrofit-run may safely consume merged lane state) · `ir-unfreeze` DONE (#1084 done.md) · `rule-tests-surface` S4 NOT in flight (§3 serialization edge dormant — carry the rule).
- **Stale items:** local worktree `claude/pipeline-ecosystem-wiring-696c01` is 16 commits behind `origin/staging`; all planning re-grounded against `origin/staging`. The meta-launch kickoff MUST land on `origin/staging` before J2 dispatch ([kickoff-staging-placement.md §1](../../rules/kickoff-staging-placement.md)).

### §1.1 Plan-currency detail (false-positive adjudication — T3)

`priority-score.sh` and `dup-detect.sh` reported `adapter-jig ... status=DONE done_pr=1082 basis=jaccard score=100%` + four `POTENTIAL_DUPE` xrefs (#1082/#1080/#1078/#1076). All are **fuzzy-layer false-positives** — same class the prior `ecosystem-wiring` session flagged for #1070/#1058. Mechanical adjudication (deterministic layer wins): (a) no `.claude/orchestrator-prompts/adapter-jig/done.md`; (b) `gh pr view 1082` → title "researched-python live path", head `worktree-ecosystem-wiring-w5` — zero adapter-jig overlap; (c) git log shows adapter-jig work actively landing (#1083, #1087) with J2/J3 unstarted. Verdict: umbrella **NOT DONE**; jaccard/xref bases are non-load-bearing.

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| ~~J1~~ | contract extraction | — | — | — (DONE #1087) | — | — |
| J2 | execution-build (conformance-suite assembly, no new runner) | PAIR / Mode-SDD | Yes | 1 | `rule-tests-surface` umbrella-level PARALLEL-OK; **named lock-arms D1/D2/D3 SERIALIZE with S4** (§3) | L |
| J3 | execution-build (first stamped family: go, atomic) | PAIR / Mode-SDD | Yes | 2 (gated on J2 merge) | — | M |

**Routing rationale (§2.5 Step 5):** J2 = I-phase-large, `scope_decided=TRUE` (binding spec §3 fixes all 22 arms + DN-J1/DN-J2 recorded defaults), `SURFACES≥2` (vitest `packages/core/` + `tests/install-sh` bash) but **NOT `parallel_safe`** — the 22 arms land inside SHARED existing suite files, so parallel worktree sessions would collide → **PAIR** (single implementer + spec-reviewer + code-quality-reviewer). J3 = atomic by T-AJ-C (adapter + lane + CI arm + BASELINE move together in ONE PR), not decomposable → NOT `parallel_safe` → **PAIR**. Both are `review_required` (load-bearing: touches `packages/core/principles/**`, ships a CI arm, BASELINE lockstep) → dual-reviewer SDD.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 (J2) → Stage 2 (J3)

Stage 2 (J3) dispatch waits for the J2 PR to be merged to `staging`:

```bash
gh pr list --search "is:merged head:worktree-adapter-jig-j2 base:staging" --json number,title,mergedAt,headRefName --limit 10
```

Expected: non-empty JSON (the J2 conformance-suite PR merged). If empty → **HALT**. Do NOT dispatch J3.

### Cross-umbrella serialization gate (BINDING, per umbrella kickoff §3)

Before merging any J2 lock-shape-touching arm (D1/D2/D3) OR before dispatching if `rule-tests-surface` S4 has opened in the interim, re-probe:

```bash
gh pr list --search "is:open base:staging" --json number,title,headRefName --limit 40 | \
  grep -iE "rule-test|deps-hash|s4|surface" || echo "S4 not in flight — J2 lock-arms clear to proceed"
```

If S4 is in flight: the lock-touching arms of J2 (D1/D2/D3) and S4 **SERIALIZE** — whichever reaches merge second merge-forwards the other's landed state and re-fires its DoD against the CURRENT lock behaviour (merge-forward, never rebase — [git-conflict-merge-forward.md §2](../../rules/git-conflict-merge-forward.md)). Umbrella-level parallelism otherwise stands.

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7 below). Re-probe in-flight immediately after the Phase -1 review completes, before J3 dispatch (all historical collisions materialized inside the Phase -1 window).

---

## §4 Sub-wave dispatch instructions

### Stage 1 — J2 (conformance-suite assembly) — dispatch NOW (after this kickoff lands on staging)

- **Channel:** maintainer opens a fresh CC tab, pastes the §10 1-liner (Tier 2 default — no `bridge-profile` marker; top tier plans, executor implements + dual-reviews). Alternatively autonomous via runtime-bridge IF configured + §4c present (see §4c / §10 autonomous-offer).
- **Scope (binding — umbrella kickoff §1 J2 + spec §3):** land the 22 arms INSIDE existing suites (vitest `packages/core/` + `tests/install-sh` bash); pairing enforced by a meta-check in the EXISTING principles suite (NO new runner — STOP line §5). Retrofit-run against npm/python/cargo — each finding lands as fix + arm. The retrofit-run DOUBLES as the DN-J1 drift probe: MEASURE 45-python ↔ 46-cargo shared-grammar drift (diff stats / structural comparison — method recorded, T-AJ-B); non-trivial ⇒ extract shared delivery-cell library (2B-standardize precedent) in this stage; trivial ⇒ record measurement + keep per-lane glue.
- **"Works" (umbrella §2):** all 22 arms GREEN on the three wired lanes with RED-provability demonstrated per arm (inverted assertion or violating stub, quoted in PR body); drift-probe measurement recorded.
- **STOP lines (umbrella §5):** NO edits to `.claude/skills/rule-tests/`, `agents/rule-test-author.md`, `packages/core/ir/`; NO new test runner; NO new freshness/staleness ledger (deps-hash stays `rule-tests-surface` S4 territory); NO paid LLM in CI.

### Stage 2 — J3 (first stamped family: go) — dispatch AFTER J2 merges + Phase -1 GO

- **Channel:** fresh CC tab (same Tier 2 posture).
- **Scope (binding — umbrella kickoff §1 J3 + spec §9 J3):** stamp the go family end-to-end THROUGH the jig — adapter + delivery lane + pinned golangci-lint CI arm + scratch red/green pair. DoD: scratch consumer fresh dir + `go.mod` → install → plant violation → golangci-lint fires RED + clean control GREEN, CI arm green ON THE RUNNER (workflow run linked — T-EW-C posture), BASELINE lockstep in the SAME PR, zero skill/IR edits.
- **Atomicity (T-AJ-C, umbrella §5):** ONE atomic PR — any `install.sh`/`setup.d`/CI go artefact leaking into J1/J2 diff is a stage-discipline breach. Touching a frozen contract row ⇒ STOP and revise the spec first.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to ALL Workers below — self-evaluate)

Workers whose target file matches ANY of the following paths MUST include in the **PR body** (not just commit message) the §1.7 Forward-check + Backward-check sections. BOTH J2 and J3 match (`packages/core/principles/**`, `.claude/rules/**`, `agents/**`, `.github/workflows/**` for the go CI arm).

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

**Required PR body sections (verbatim shape — CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- `doc-authority-hierarchy.md` — <what was checked>. file:line evidence: `<path>:<line>`
- `phase-research-coverage.md §1.12` — <what was checked>. file:line evidence: `<path>:<line>`
- `build-first-reuse-default.md` — <verdict + rationale>
- `no-paid-llm-in-ci.md` — <verdict>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the change's scope — enumerate SIBLING surfaces (both lock writers, all delivery lanes, tripwire pair), never restate the diff (T21)>

- `<existing-artefact-name>` — <how this PR interacts; supersedes / extends / orthogonal>. file:line evidence: `<path>:<line>`
- <repeat for each swept sibling surface>
```

**Hard rules (each has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2 (`##`).** CI regex matches `^### §1\.7 ...`.
2. **Word «applied» is required.**
3. **≥40 non-whitespace chars in EACH section body.**
4. **≥1 file:line citation per section** matching `[^[:space:]]+\.[a-z]+:[0-9]+`.
5. **Mechanical-maintenance escape hatch:** `### §1.7 Skipped: <rationale ≥60 chars>` — only for purely structural PRs (NOT applicable to J2/J3, both carry discipline surface).

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # ≥2
```

---

## §4c Autonomous aif-handoff dispatch — park-don't-guess contract (applies ONLY if a Worker is dispatched to the runtime-bridge instead of a maintainer-paste tab)

> **When live:** the §10 report offered autonomous dispatch AND the maintainer accepted. If every Worker runs in a maintainer-pasted CC tab, §4c is inert — skip it.

**Lever 1 — conservative aif config (BEFORE dispatch):**

```bash
export AGENT_MAX_REVIEW_ITERATIONS=1
export AGENT_AUTO_REVIEW_STRATEGY=closure_first
export AGENT_SKIP_REVIEW=false
```

**Lever 2 — park-don't-guess instruction (the dispatched kickoff carries this verbatim, addressed to the aif agent):**

> **aif agent — fork discipline (non-negotiable):** On ANY genuine fork or ambiguity (two defensible implementations, an undecided design choice, a missing spec detail that changes behaviour) — **do NOT pick.** Park it as a question (set the task to `manualReviewRequired` / `blocked_external` with the fork stated as «Option A → consequence X / Option B → consequence Y») and **stop that task.** Proceed only on the unambiguous parts. For adapter-jig specifically: the DN-J1 drift-probe verdict (extract-vs-keep) and any frozen-row-touch (STOP → spec revision) are exactly the forks to PARK, never guess.

**Pre-dispatch gate:** `grep -qi 'park it as a question' <this-kickoff>` AND `echo "$AGENT_MAX_REVIEW_ITERATIONS"` non-empty. Either missing → STOP.

**Egress gate (mandatory after `status=done`):** `npx tsx packages/runtime-bridge/src/cli/harvest.ts <taskId> --base staging`.

---

## §5 AI-traps active (per `.claude/rules/ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue.

**Active canonical traps for adapter-jig (inherited verbatim from umbrella kickoff §4): T2, T3, T11, T14, T15, T19, T20, T21.**

- **T2 / T20** — «arm exists» ≠ «arm proven»: every arm needs its RED demonstrated (inverted assertion or violating stub), output quoted. A green-only arm is REFUSED by design (spec §3).
- **T3** — spec census citations are design-time; re-verify live at each stage (staging has moved — W5/W6, ir-unfreeze all merged since authoring; the jaccard-DONE false-positive in §1.1 is the T3 lesson in action).
- **T11** — no new capability body without the companion-pool DeepWiki residual first (already discharged for J1's SSOT #226; J2/J3 introduce no new SSOT-class capability, but any drift-lib extraction in J2 re-triggers the consult).
- **T14** — the J2 retrofit-run reports coverage HONESTLY: a lane where an arm cannot run (tool absent) is «insufficient», never «clean».
- **T15** — the jig self-applies: the suite's own arms must survive their own RED-provability rule; the retrofit-run is the jig auditing itself.
- **T19** — own adversarial cold-review (the J1-shipped `agents/adapter-jig-reviewer.md` protocol) before every handoff; CI ≠ design review.
- **T21** — backward-checks ENUMERATE sibling surfaces (both lock writers, all three delivery lanes, the tripwire pair), never restate the diff.

**Domain-specific traps (adapter-jig-specific, NOT in canonical catalogue — inherited from umbrella §4):**

- **T-AJ-A — «arm passes because it tests the fixture, not the lane»**: an arm wired to a synthetic fixture only, never exercised against real lane artefacts, is theatre — each arm's retrofit-run MUST cite the REAL lane file/output it judged.
- **T-AJ-B — «drift probe eyeballed»**: DN-J1 resolves on a MEASURED drift (diff stats / structural comparison of the two lane scripts, method recorded), not «looks similar to me».
- **T-AJ-C — «go leaks ahead of its stage»**: J3 is its own atomic PR; any `install.sh` / `setup.d` / CI go artefact in the J2 diff is a stage-discipline breach (both-or-neither: the lane + its CI arm + BASELINE move together in J3).

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation. T-numbers enumerated + 3 domain-specific traps added per `ai-laziness-traps.md §3`.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass `principle 12` (AI-laziness-traps format validation). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

The umbrella itself is recursively self-applying (T15): the conformance jig's own 22 arms must survive their own RED-provability rule — the J2 retrofit-run is the jig auditing itself.

---

## §7 Phase -1 reviewer dispatch (between stages)

Before dispatching Stage 2 (J3), invoke:

```text
Agent: Phase -1 cold-review for adapter-jig Stage 1 (J2)
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

Reviewer discipline: surfaces strategy forks as DECISION-NEEDED (option A → consequence X / option B → consequence Y). Does NOT pick strategy, does NOT edit files, does NOT approve on behalf of the maintainer, does NOT skip surface review because «CI is green» (T19). See `.claude/rules/reviewer-discipline.md §2`. Additionally run the umbrella's own `agents/adapter-jig-reviewer.md` named cold-review protocol (T19 own-QA) before handoff of each stage.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/pipeline/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 In plain words` block whose CONTENT follows the orchestrator-checkpoint substance shape (names sub-waves / AC items / file:line / REPORT-traces / dispatch-state) — NOT a restatement of the per-turn generic reminder (else `#two-prompts-drift`).

---

## §8 Stop conditions

- Any `gh` command errors AND the umbrella has execution stages pending → halt, escalate.
- Stage gate command returns empty AND Stage N was expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Any sub-wave returns a BLOCKER finding → halt Stage; surface before proceeding.
- **J-stage-specific:** a J2/J3 diff would touch a FROZEN contract row (F1-F11) → STOP, revise the spec first (umbrella §5 / spec §9). A go artefact appears in the J2 diff → STOP (T-AJ-C breach).
- `rule-tests-surface` S4 opens while a J2 lock-arm (D1/D2/D3) is unmerged → serialize per §3, do NOT race.
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this kickoff.
- Do NOT finalize project strategy — surface genuine forks to maintainer.
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per no-paid-llm-in-ci.md + BFR-default).
- Do NOT dispatch J2/J3 Workers before the maintainer GO (this session's §0 confirmation gate).

---

## §10 See also

- `.claude/orchestrator-prompts/adapter-jig/kickoff.md` — the BINDING umbrella kickoff.
- `docs/superpowers/specs/2026-07-22-adapter-jig-design.md` — the BINDING design spec (contract, 22 arms, DN forks, J3 DoD).
- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source.
- `.claude/skills/pipeline/SKILL.md` — generating skill.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/parallel-subwave-isolation.md §1` — worktree isolation.
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
- `.claude/rules/git-conflict-merge-forward.md §2` — §3 serialization merge-forward recipe.
