# Research-source trust tiers — discipline rule

> **Class:** A — companion principle test shipped at [packages/core/principles/30-research-source-trust.test.ts](../../packages/core/principles/30-research-source-trust.test.ts).
> **Authoritative for:** the trust-tier discipline for rule-research provenance — §1 the tier model (pointer, not a copy), §2 the §4.5 re-tightening trigger, §3 anti-patterns, §4 ecosystem-prefix parsing (shipped in S4), §5 promotion / retirement.
> **NOT authoritative for:** project goal — see [README.md#why-this-exists](../../README.md#why-this-exists). The resolver implementation — see [packages/core/research/allowlist-resolver.ts](../../packages/core/research/allowlist-resolver.ts). Full tier architecture + threat model — see the umbrella kickoff (below).

> **Origin:** 2026-07-02, `rule-research-trust-tiers` umbrella ([.claude/orchestrator-prompts/rule-research-trust-tiers/kickoff.md](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md)). Live rule-research became the default delivery path (2026-06-29) while the documentation-source allowlist stayed a hardcoded 7-key const (`packages/core/research/allowlist.ts:8-17`) — front-end-skewed, no extension path for a consumer without forking. S1 shipped the resolver port + Tier 2; S2 shipped Tier-1 npm derivation + scope-lock + taint banner. This rule + [principles/30](../../packages/core/principles/30-research-source-trust.test.ts) are the S3 discipline layer for the already-shipped capability.

## §1 The tier model

The full tier architecture, threat model, and cross-tier invariants are specified in the umbrella kickoff — **this rule points to that spec, it does not copy it** (single source of truth per [dual-implementation-discipline.md §7](dual-implementation-discipline.md)):

- [`.claude/orchestrator-prompts/rule-research-trust-tiers/kickoff.md` §4](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) — target architecture: Tier 0 (builtin curated) → Tier 1 (derived from a direct dependency's local `homepage`/`repository` metadata, scope-locked, multi-tenant apexes ineligible) → Tier 2 (consumer-acked, `.ai-factory/research-allowlist.json`). First match wins.
- [`.claude/orchestrator-prompts/rule-research-trust-tiers/kickoff.md` §2](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) — the calibrated threat model (H1 bar-lowering, H2 shared-host whole-host trust) that motivates the containments Tier 1 ships with.
- [`packages/core/research/allowlist-resolver.ts`](../../packages/core/research/allowlist-resolver.ts) — the implementation: `resolveAllowedSources(ctx)` + `validateProvenance(p, resolved, opts?)`.

One-line summary for readers who only need the shape: a provenance URL is authorized if its host matches a builtin key (Tier 0), OR derives from a direct dependency's own metadata scope-locked to that package and not a shared apex (Tier 1), OR is explicitly acked by a human in a committed, PR-reviewed file (Tier 2). Anything else fails closed.

## §2 The §4.5 re-tightening trigger

[Kickoff §4.5](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) states, verbatim:

> The moment Path B (LLM code-gen) activates, Tier-1 auto-trust is downgraded to Tier-2-ack for any code-generating flow.

This is a **named, binding trigger**, not a suggestion: today's factory is deterministic and grammar-bounded (a wrong-but-well-formed lint rule is the worst case for a poisoned docs source, per [kickoff §2](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) row 1). Tier 1's author-controlled trust (H1) is calibrated against that bound. If Path B ever ships — the factory starts emitting arbitrary AST-plugin code from research output — the blast radius of a poisoned Tier-1 host changes class (wrong rule → arbitrary code), and Tier-1's auto-trust for that flow MUST be downgraded to require an explicit Tier-2 human ack. Implementing Path B without also downgrading Tier 1 for its code-generating flows is a rule violation, not an oversight to catch later.

## §3 Anti-patterns

- **`#trust-by-name-not-scope`** — authorizing a research host because it belongs to a familiar-sounding package, without the accompanying same-package scope-lock. This is the T16 (`#pattern-matching-on-name`, [ai-laziness-traps.md §2](ai-laziness-traps.md)) shape applied to provenance: a package name matching what the researcher expects is not the same as `provenance.packageName === entry.package` actually holding. The resolver's Tier-1 branch enforces the scope-lock mechanically (`T-RTT-A`, cross-package provenance fails); this anti-pattern names the *reasoning* failure a reviewer or future author could still make by hand-authoring a provenance entry that skips the check's intent (e.g. copy-pasting an `allowlistKey` from a different package's entry because "it's basically the same ecosystem"). Counter: never derive trust from a package's *name similarity* — only from `entry.package` being the trusted, plan-builder-sourced identity that the resolver checks against `provenance.packageName`.
- **`#allowlist-as-code-not-data`** — extending research-source trust by editing framework TypeScript source (the pattern this umbrella replaced, [kickoff §1](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md)) instead of the appropriate **data** surface: Tier 1 is derived automatically from a consumer's own `package.json` + `node_modules` (no edit needed for a direct dependency with a single-tenant host), and Tier 2 extends via the committed `.ai-factory/research-allowlist.json` ack file — never via a source-code diff to `allowlist.ts` or `allowlist-resolver.ts`. Before this umbrella, a consumer researching an uncurated framework (e.g. Drizzle) hit `unknown allowlistKey` and had no path forward short of forking the framework's own source. Counter: a new trust grant is always a **data change** (an installed dependency's own metadata, or an acked JSON entry) — a PR that adds a host to `ALLOWED_SOURCES` or hand-writes a `multi-tenant-hosts.json` exception for a single request is the violation this names.

## §4 Ecosystem-prefix parsing — SHIPPED (S4)

`Provenance.packageName` and `ResearchEntry.package` were bare package names through S1-S3 (implicit `npm:` ecosystem — the only adapter S1/S2 shipped). **S4 ships the ecosystem prefix** this section previously reserved as a forward-looking placeholder: [`packages/core/research/ecosystem-name.ts`](../../packages/core/research/ecosystem-name.ts) `parseEcosystemName(name)` parses an explicit `"<ecosystem>:<bareName>"` prefix (concrete scheme: `cargo:serde`, with unprefixed names defaulting to the implicit npm ecosystem for back-compat with every pre-S4 entry). An unrecognized prefix (e.g. a hypothetical `pip:requests` before a pip adapter exists) parses to `ecosystem:"unknown"` — fail-closed, never silently retried as a bare npm name. [`packages/core/research/allowlist-resolver.ts`](../../packages/core/research/allowlist-resolver.ts) `tier1For` wires the parse in: a prefix that does not match the wired `ctx.adapter.ecosystem` yields a Tier-1 miss (new diagnostic code `FF2016`, distinct from the generic `FF2009` no-eligible-host code); a matching prefix strips to the bare name before `listDirectDeps`/`readInstalledMeta`. The scope-lock (`packageName === entryPackage`) stays prefix-consistent by direct string comparison — both sides of a scope-locked pair carry the same prefixed-or-bare form. [Kickoff §5 S4](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) reserved the non-JS **adapters** (`{toolchain, stack}`) as a seam; this section's naming convention (the concrete prefix scheme) is this rule's own contribution, now implemented alongside the first non-JS adapter — [`packages/core/research/ecosystem-cargo.ts`](../../packages/core/research/ecosystem-cargo.ts) `cargoAdapter`, reading `Cargo.toml` `[package]` metadata with zero network access and zero `cargo`/`rustc` binary invocation (SSOT [#197](../../docs/meta-factory/prior-art-evaluations.md)).

## §5 Defence-in-depth from the S2 adversarial review (items 1 + 3 documented-not-coded; items 2 + 4 now coded)

The S2 adversarial review (round 2) surfaced four residual observations against the resolver, each re-adjudicated in a 2026-07-03 follow-up (adversarial re-verification of every reachability claim). Items 1 and 3 were accepted **by design** rather than fixed with a code change — recorded here so a future auditor does not re-open them as new findings without checking this list first. Item 1 is a prose-documented risk acceptance (not gated by principle 30 or any other CI mechanism), with an explicit harden-criterion; item 3 likewise. Items 2 and 4 have since been **coded** — each ships as a deterministic guard + paired-negative/tripwire test (see the entries below). Item 2's VALUE-traversal containment was itself found incomplete on re-review (a lexical-only check bypassed by an in-tree symlink) and was hardened a second time to a realpath-based check — see the 2nd BLOCKER note inside item 2 below.

**Parser robustness (adjacent, not from the S2 review — found alongside the 2nd BLOCKER fix):** the hand-rolled Cargo.toml subset parser (`parseCargoToml`) merged duplicate `[package]` tables last-write-wins, so a manifest with two conflicting `[package]` tables (e.g. two different `homepage` values) yielded a position-dependent host rather than failing closed. Fixed: `parseCargoToml` now counts `[package]` table headers and, if more than one is seen, discards all `[package]` fields entirely (`packageFields` stays empty) — `readInstalledMeta`'s existing `!packageFields.has('name')` fail-closed check then rejects the manifest, since a real Cargo.toml always declares `name`. Covered by a dedicated test in [`ecosystem-cargo.test.ts`](../../packages/core/research/ecosystem-cargo.test.ts) (two `[package]` tables with different `homepage` values → `readInstalledMeta` returns `null`, not either host).

1. **Union homepage ∪ repository trust (C).** Tier 1 trusts BOTH a package's `homepage` AND `repository` host — both enter the same `candidateFields` derivation in `resolveAllowedSources`'s `tier1For` and pass the *identical* filter chain (canonicalize → IP-literal → punycode → `isMultiTenantHost`) in one loop. By-design ([kickoff §4](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md)): a direct dependency is already trusted to run arbitrary install-time code on the consumer's machine, so trusting its self-declared hosts for *documentation research* is a strictly smaller grant than the trust already extended by `npm install`. **`repository` ⊆ `homepage` under the threat model:** both fields sit in the same attacker-controlled bucket (installed package metadata) and `extractHttpsHostFromMeta` resolves them identically — a poisoned `repository` and a poisoned `homepage` are the same attacker writing the same metadata, and a *benign* shared-repo `repository` (e.g. `github.com`) is already dropped by `isMultiTenantHost` — so splitting the two fields defends nothing under the current model. **Why C has no mechanical tripwire:** its harden-criterion fires on an *external-incident* observation, a fact about the world, not a change in this repo's code state — nothing (`candidateFields`, `multi-tenant-hosts.json`, the filter chain) changes value when it fires, so there is no RED→GREEN transition to pin; it stays KEEP-DOCUMENTED rather than being promoted to principle 30. The one mechanizable slice — a benign shared apex never leaking a shared host into Tier 1 — is *already* pinned by principle 30's paired-negative (`isOnMultiTenantList('github.com')` is caught; a single-tenant host is not), so a second test would be `#discipline-theatre`. **Harden-criterion:** if a real incident shows repository-host abuse distinct from homepage-host abuse, split the two fields and gate them independently. **Future-auditor re-check (in lieu of a tripwire):** re-verify [`multi-tenant-hosts.json`](../../packages/core/research/multi-tenant-hosts.json) *coverage* — a newly-popular shared-repo / docs-hosting apex absent from that list is the actual regression surface, not the union itself.
2. **Dep-name path-traversal (D) — CODED (S4); path-override/workspace-member/vendored VALUE traversal (BLOCKER ×2, found + fixed post-ship) — CODED.** `installedPkgJsonPath` (the npm `EcosystemAdapter`) previously did not reject `../`-segment dependency names in its `join`. The precondition that made this live — "a non-`package.json`-controlled dependency source is added to the ecosystem-adapter seam" — fired in S4: [`packages/core/research/ecosystem-cargo.ts`](../../packages/core/research/ecosystem-cargo.ts) `cargoAdapter` is a SECOND `EcosystemAdapter`, and its `readInstalledMeta` is directly reachable with an arbitrary name (path/vendor/workspace-member manifest resolution, not a `Set.has` membership gate first). Both adapters now carry an `isUnsafeDepName` guard (rejects any name containing `..` or an unexpected separator shape) BEFORE constructing a filesystem path from a dependency **NAME** — `ecosystem-npm.ts`'s `installedPkgJsonPath` and `ecosystem-cargo.ts`'s `resolveDepManifestPath`. **This guards the NAME surface only.**
   - **1st BLOCKER (lexical VALUE traversal) — fixed.** A post-ship adversarial pass found that `resolveDepManifestPath`'s path-override branch had a DEAD traversal check (`isUnsafeDepName(...)` computed and discarded in an empty `if` body) and the workspace-member branch had ZERO containment on the *VALUE* — a consumer-controlled `path = "../../../outside"` or `[workspace] members = ["../../evil"]` resolved and read an out-of-tree, attacker-controlled `Cargo.toml`, laundering its `homepage` into Tier-1 trust. This was first contained by `isWithinRoot` — a purely **lexical** string check on `path.resolve()` output.
   - **2nd BLOCKER (symlink escape past the lexical check) — fixed.** A re-review of the 1st fix found that `isWithinRoot` is lexical-only: `path.resolve()` never dereferences symlinks, while the manifest read (`existsSync`/`readFileSync`) DOES follow them. An **in-tree symlink pointing out-of-tree** therefore bypassed containment on ALL THREE resolution branches — including the **vendored** branch, which had NO `isWithinRoot` check at all (verified live: `vendor/<pkg>` symlinked to an outside dir authorized the outside `Cargo.toml`'s `homepage` as Tier-1). Fixed via `resolvedWithinRoot` (`ecosystem-cargo.ts`): containment is now checked on the **realpath-canonicalized** path — `realpathSync` is applied to BOTH the candidate AND `root` before comparing (canonicalizing only the candidate side, against a lexical `root`, would false-reject legitimate in-tree paths whenever `root` itself sits under a symlinked ancestor, e.g. macOS `/tmp` → `/private/tmp`). All three branches (vendored, path-override, workspace-member) now route through this single helper; `isWithinRoot` itself is retained as the low-level lexical primitive (still used for the cheap absolute-path/`..`-segment pre-check with no fs access) but is no longer the containment gate on its own.
   - **Summary: dep-NAME traversal → `isUnsafeDepName`; path-override/workspace-member/vendored VALUE traversal (lexical AND symlink-resolved) → `resolvedWithinRoot` (realpath both sides, within root). Two distinct guard families on two distinct surfaces (name vs. value), the value guard now robust to symlink escapes on all three branches.**
   - **Tripwire (re-armed, not retired):** [`ecosystem-adapter-precondition.test.ts`](../../packages/core/research/ecosystem-adapter-precondition.test.ts) no longer asserts "exactly one adapter" — it asserts the POSITIVE property (every `EcosystemAdapter` implementation file carries a textual traversal-guard signal), with a negative-control test proving the detector actually discriminates guarded from unguarded source, a behavioural test calling both adapters' `readInstalledMeta` with a traversal NAME end-to-end, and a behavioural end-to-end pair covering the VALUE surface (traversing path-override and workspace-member values against real out-of-tree manifests). [`ecosystem-cargo.test.ts`](../../packages/core/research/ecosystem-cargo.test.ts) carries the BLOCKER-exploit paired-negatives directly for both the lexical escape AND the symlink escape (RED pre-fix, GREEN post-fix) on all three branches, plus in-tree-still-works positive controls — including a dedicated positive control for an in-tree symlink whose TARGET is also within root (proving the realpath check does not over-reject legitimate in-tree symlink aliases). A future third adapter shipped without the NAME guard fails the precondition file again, the same way the second adapter flipped it before this hardening; the VALUE containment is adapter-local (`resolvedWithinRoot`) and must be re-implemented per adapter that resolves declared paths — the precondition file's textual signal does NOT currently cover it (documented gap, not a silent one).
   - **Residual (accepted — outside the threat model): TOCTOU.** `resolvedWithinRoot` canonicalizes and containment-checks the path, then a later `existsSync`/`readFileSync` reads it — a narrow time-of-check/time-of-use window. An attacker who can *race the local filesystem* (swap a symlink target between the `realpathSync` check and the read) could defeat it, but that capability — concurrent local-FS writes during a research run — is strictly stronger than the committed-file H1 actor this tier defends against ([kickoff §2](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md)), and implies the attacker already holds local code execution. Accepted by design, not gated (no realistic in-model exploit); recorded so a future auditor does not re-open it as new. **Harden-criterion:** if research is ever run against an untrusted / shared working tree with a concurrent writer, open the manifest via a file descriptor and canonicalize the *fd* (or read-then-canonicalize the read path), closing the check/use gap.
3. **`ackFilePath` no containment (F).** `ResolveCtx.ackFilePath` is set by calling code, never sourced from plan JSON — it is not reachable from untrusted input in the current call graph (both production `validateResearchPlan` callers construct `{ root, adapter }` only, and `research-plan.schema.json` is `additionalProperties:false`, so a plan cannot smuggle the key either). **Harden-criterion:** if a plan-controlled ack-file path is ever introduced (e.g. a per-monorepo-workspace ack file selected by plan content), add path-containment validation at that point. **Tripwire:** [`ackfilepath-plan-containment.test.ts`](../../packages/core/research/ackfilepath-plan-containment.test.ts) makes this mechanically detectable — GREEN while `ackFilePath` stays confined to its `allowlist-resolver.ts` type-def + read-site and no `validateResearchPlan` ctx literal threads it (the residual anonymous-spread shape is noted in that file); RED the moment either changes.
4. **Bare-TLD ack host (round-1 B) — CODED.** `loadAckFile` now rejects a single-label ack host (e.g. `hosts: ["com"]`) with `AckFileError`, inside the same per-host loop that rejects IP-literal hosts ([`allowlist-resolver.ts`](../../packages/core/research/allowlist-resolver.ts), `loadAckFile`); paired-negative in [`allowlist-resolver.test.ts`](../../packages/core/research/allowlist-resolver.test.ts) (`hosts: ['com']` → `toThrow(AckFileError)`, with a two-label positive control). The earlier defence-in-depth still holds — an ack entry only activates via a human-reviewed, human-merged PR ([DN #2](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) human-merge boundary, cargo-vet certify precedent, [SSOT #186](../../docs/meta-factory/prior-art-evaluations.md)) — but the guard now catches a hand-edit typo (`com` for `docs.com`, which via `hostMatches` would authorize every `*.com`) at load time regardless, closing the round-1 B observation at the earliest reachable channel rather than resting on human review alone. **Prior harden-criterion (now satisfied):** reject single-label hosts in `loadAckFile` if ack files ever bypass human review — implemented ahead of that trigger for consistency with the sibling IP-literal / dup-key guards in the same loop. **Both host-derivation surfaces:** the §1.7 backward-sweep of this change found the *same* single-label gap in **Tier-1** derivation (`tier1For`'s `candidateFields` filter, which rejected IP-literal / punycode / multi-tenant but not bare TLDs — a compromised direct dep could widen its scope-locked research trust to a whole TLD via `homepage: "https://com"`); the guard is therefore applied there too, so Tier-1 package-metadata hosts and Tier-2 ack hosts reject bare TLDs consistently (paired-negative `tier1.test.ts` S2-N8). **Scope (not over-claimed):** the guard rejects *single-label* hosts only; a multi-label public suffix (`co.uk`, `github.io`) still authorizes its whole space — that is a separate property left unchanged by DN #3's exact-host / no-PSL design, not what this guard addresses.

## §6 §1.7 self-reflexive note

- **Forward-check:** complies with [no-paid-llm-in-ci.md](no-paid-llm-in-ci.md) (principle 30 + the resolver it checks are deterministic vitest/TypeScript — zero API-billed calls, per `allowlist-resolver.ts:1-11` header comment recording the offline-determinism invariant); complies with [build-first-reuse-default.md](build-first-reuse-default.md) (ADAPT verdicts recorded at [prior-art-evaluations.md #186](../../docs/meta-factory/prior-art-evaluations.md) (cargo-vet), [#187](../../docs/meta-factory/prior-art-evaluations.md) (TUF vocabulary), [#188](../../docs/meta-factory/prior-art-evaluations.md) (registry-metadata trust source) — no BUILD-without-search violation); complies with [doc-authority-hierarchy.md §2-§3](doc-authority-hierarchy.md) (this file carries the Class-A header above and is registered in principle 09's `REQUIRED_HEADER_DOCS`, [packages/core/principles/09-doc-authority-hierarchy.ts:53](../../packages/core/principles/09-doc-authority-hierarchy.ts)); complies with [dual-implementation-discipline.md §2(iv)](dual-implementation-discipline.md) (the resolver is a TypeScript package capability under `packages/core/research/`, not a hook/agent/skill delivery-channel artefact — §2(iv) non-trigger, no portable-fallback triage needed for the resolver itself; this rule + agent/skill doc updates are the discipline layer, correctly dual where the agent protocol already is).
- **Backward-check:** codifies [kickoff §4](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) (Tier 0/1/2 model) and [§4.5](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) (re-tightening trigger) without copying their content — see §1 above. **Supersession note:** the kickoff's AC 4 prose claim ("Existing 7 Tier-0 keys: zero behavior change") is a *design intent* statement; the **authoritative record of what actually shipped** is the executable pinned-divergence test added in S1 (issue/PR `#851`, `test(research): pin Tier-0 §4 cross-tier divergences; correct byte-identical overclaim`), which documents that the one-arg wrapper is NOT byte-identical on every input — three specific divergences (trailing-dot FQDN normalization, IP-literal rejection reason, punycode rejection reason) are pinned as executable truth in `allowlist-resolver.test.ts`, per the header comment in [`allowlist-resolver.ts:230-238`](../../packages/core/research/allowlist-resolver.ts). This rule does **not** edit the merged kickoff to correct its AC 4 wording (kickoff is a closed artefact per its own umbrella discipline) — it records the supersession here instead, so a future reader consulting the kickoff for "did anything change for the 7 keys" is pointed at the test that has the precise, executable answer.

## See also

- [`.claude/orchestrator-prompts/rule-research-trust-tiers/kickoff.md`](../orchestrator-prompts/rule-research-trust-tiers/kickoff.md) — the umbrella kickoff; authoritative for the full tier architecture, threat model, and DECISION-NEEDED resolutions this rule points to.
- [`packages/core/research/allowlist-resolver.ts`](../../packages/core/research/allowlist-resolver.ts) — the resolver implementation (`resolveAllowedSources`, `validateProvenance`, `EcosystemAdapter` seam).
- [`packages/core/research/ecosystem-npm.ts`](../../packages/core/research/ecosystem-npm.ts) — the npm `EcosystemAdapter` (S2).
- [`packages/core/research/ecosystem-cargo.ts`](../../packages/core/research/ecosystem-cargo.ts) — the cargo `EcosystemAdapter` (S4; the first non-JS adapter, reading `Cargo.toml` `[package]` metadata).
- [`packages/core/research/ecosystem-name.ts`](../../packages/core/research/ecosystem-name.ts) — `parseEcosystemName` (S4 ecosystem-prefix parsing, §4 above).
- [`packages/core/principles/30-research-source-trust.test.ts`](../../packages/core/principles/30-research-source-trust.test.ts) — the companion Class-A principle test (store-wide discipline invariants, distinct from the resolver's own behavior suites).
- [`agents/rule-researcher.md`](../../agents/rule-researcher.md) — the AI-agnostic research protocol that authors provenance entries against this tier model.
- [`.claude/skills/rule-research/SKILL.md`](../skills/rule-research/SKILL.md) — the Claude Code trigger for the protocol above.
- [`docs/meta-factory/prior-art-evaluations.md` #186-#188](../../docs/meta-factory/prior-art-evaluations.md) — the SSOT entries this umbrella's BFR-default consult produced.
- [`ai-laziness-traps.md §2 T16`](ai-laziness-traps.md) — `#pattern-matching-on-name`, the trap `#trust-by-name-not-scope` (§3 above) specializes onto provenance authorship.
- [`ci-tool-pinning.md`](ci-tool-pinning.md) — peer Class-A rule this file's structure mirrors.
