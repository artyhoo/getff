<!-- scope:plugin-skills-generator -->

# Stage 0 re-verification — plugin-skills-generator (verdict matrix + fork adjudication)

> **Type:** research-patch — Stage 0 deliverable of `.claude/orchestrator-prompts/plugin-skills-generator/kickoff.md` §2 (operator directive 2026-09-11: re-verify kickoff + plan claims after compaction BEFORE any fix code).
> **Owner:** Stage 0 session, 2026-09-11 (branch `feature/plugin-skills-generator-466290`, base staging @ `b069c59328`).
> **Method:** every kickoff §2 row re-derived with its named probe on the live tree; command + output quoted per T3; full populations, no sampling (T9/T10 — all four CORE skills probed individually); R-phase only, zero source edits (T5 — transform probes ran on scratch copies, removed before commit).
> **Format note:** stage-deliverable patch (verdict matrix per the kickoff), not a ≤100-LOC gap-accumulator entry; Problem/Evidence/Prevention discipline of the folder README is applied throughout.

## §1 Verdict matrix (kickoff §2 rows 0.1–0.7)

| Row | Claim | Verdict | Probe → output (abridged) | Disposition for Stage 1–2 |
|---|---|---|---|---|
| 0.1 | No script derives `plugin/skills/`; gates are CI-tier only | **CONFIRMED** | `ls scripts/` → 60 entries; only `generate-plugin-twins.sh` is generator-shaped (hooks+agents, `:1-43`). `grep -rln "plugin/skills" scripts/ tests/ packages/core/` → exactly 4 files, all gates/tests: `tests/plugin/bootstrap.test.sh`, `tests/plugin/install-seam.test.sh`, `tests/plugin/opencode-adapter.test.sh`, `packages/core/principles/24-plugin-manifest-integrity.test.ts`. `grep -n "skills" .husky/pre-commit` → no match (twins arm `:220-241` = hooks+agents only). `scripts/check-skill-drift.sh:86` scans `.claude/skills agents skills` — `plugin/` absent | Build the generator; extend 24(g)/(h) feeding — **no parallel gate** (kickoff 0.1 fold-in arm) |
| 0.2 | Marketplace consumes `plugin/` live from the branch | **CONFIRMED** | `.claude-plugin/marketplace.json` → `"source": "./plugin"`, `"version": "0.2.0"` (both `metadata` and plugin entry) | Channel model unchanged; §0 stands |
| 0.3 | tool-bootstrapping `skills/` ≡ `plugin/` byte-identical; deep fork = `.claude/skills/` → `skills/` prose, un-gated | **CONFIRMED + REFINED** | `diff -rq skills/tool-bootstrapping plugin/skills/tool-bootstrapping` → empty. getff: `SKILL.md` byte-identical (source carries 1 blob URL, 0 relative `](../` links); `references/*.md` 5/5 raw-DIFF. Deep fork: `.claude/skills/tool-bootstrapping/SKILL.md` vs `skills/tool-bootstrapping/SKILL.md` → differ; **no gate compares `.claude/skills/` vs `skills/`** (24(g) compares `skills/` vs `plugin/` only, `:451`) | getff depth adjudicated in §2 (not a plain copy); prose-fork disposition recorded in §5; plugin side of tool-bootstrapping = byte-copy |
| 0.4 | The transform suffices per CORE-four skill (link-only) | **CONFIRMED — all four individually** | §3: ai-doc 2 hunks, rule-research 1, rule-tests 1, template-audit 2 — every hunk a link-TARGET rewrite; zero prose deltas; backtick prose survives untouched | **No fork** — derivation = copy + `transform_internal_refs` (15 arms, `setup.d/lib.sh:148-162`) |
| 0.5 | dist / baseline blast radius | **CONFIRMED — zero** | `grep -c "plugin/skills" packages/getff/MANIFEST.sha256` → 0; `ls tests/install-sh/baselines/` → 7 stacks (cargo go python react-native react-next react-spa ts-server), `grep -rl plugin tests/install-sh/baselines/` → no hits; `build-getff-dist.sh:42` PAYLOAD = `install.sh setup setup.d agents skills templates .claude/hooks .claude/skills …` — `plugin/` absent | No dist regen, no snapshot capture in Stage 2. Note: PAYLOAD **ships** top-level `skills/` — source edits there WOULD move the MANIFEST (one more reason Stage 0 edits nothing) |
| 0.6 | ZCode plugin update mechanics | **INCONCLUSIVE-needs-host** | `printenv \| grep -i zcode` → none; `ls ~/.zcode` → absent in this container; `bash scripts/probe-zcode-runtime.sh` → `SKIP: /Applications/ZCode.app/…/zcode.cjs not present — runtime probe is maintainer-machine only` (rc=0; a SKIP is not pass evidence). Repo-side pinned: 3-site 0.2.0 chain — `plugin/.claude-plugin/plugin.json:4`, `.claude-plugin/marketplace.json:6,13`, `plugin/install/fetch-and-wire.sh:32` (`RAT_PLUGIN_VERSION="0.2.0"`) | Version-pinned-cache hypothesis stays a hypothesis; host command list in §4 F4; Stage 3 executes on host |
| 0.7 | ai-doc's special refs | **ENUMERATED — 3 classes** | (i) cross-plugin `superpowers:writing-skills` (`SKILL.md:9,16,20`) — prose skill names, not markdown links; recorded FINE on the plugin channel (kickoff §7); inert text where superpowers is absent. (ii) sibling `anthropic-and-aif-residue.md` (`:8,18`) — travels with the directory, resolves in-payload; transform correctly leaves it (probe: zero hunks in that file). (iii) repo-internal links (README `:9`, `.claude/rules` `:9`, docs `:19`) — transform rewrites targets (probe hunks = exactly those lines) | No repo-only FILE link left dangling; no fork |

## §2 Fork adjudication — getff derivation depth (row 0.3, resolved empirically)

The plan's recon flagged the kickoff §1 claim «byte-identical — the plugin copies already are» as imprecise for getff. Stage 0 adjudicated the exact mechanism with a two-step probe (T2: ran, not «would»):

- **Probe A** — copy `skills/getff/` → apply the 15 `transform_internal_refs` sed arms verbatim (`setup.d/lib.sh:148-162`; `UPSTREAM_BLOB_URL` default = `lib.sh:47` = `https://github.com/artyhoo/getff/blob/main`) → `diff` vs `plugin/skills/getff/`: **5 references files still differ**, every residual hunk = link TEXT only. Example (`references/overview.md:6`): source `[../../../README.md#why-this-exists](https://github.com/artyhoo/getff/blob/main/README.md#why-this-exists)` vs plugin `[README.md#why-this-exists](https://github.com/artyhoo/getff/blob/main/README.md#why-this-exists)`. The transform rewrites TARGETS only; the hand-copy **also stripped the `../` ladder from link TEXT** — exactly what 24(g)'s comment records («link TEXT lost its leading ../ ladder», `24-plugin-manifest-integrity.test.ts:431`).
- **Probe B** — add one TEXT-ladder strip arm (`sed -E 's#\[((\.\./)+)#[#g'`) → `diff -rq` vs `plugin/skills/getff/` → **BYTE-IDENTICAL, all 6 files.**

**Verdict:** `plugin/skills/getff` ≡ copy(`skills/getff`) + `transform_internal_refs` + TEXT-ladder-strip. Derivation mode for getff = **`transform+textstrip`**. Kickoff §1's byte-identical claim was true only for tool-bootstrapping.

Design consequences (recorded here; Task 3 implements):

1. The blunt probe arm matches any `[../` sequence. Census (`grep -rn '\[\.\./' skills/`): ladder-shaped TEXT exists **only** in the 5 `skills/getff/references/*.md` README links; the 2 `skills/tool-bootstrapping/references/decision-format.md` hits are backtick prose + clean sibling links, unaffected. Task 3 should scope the arm to link-TEXT position (`[(../)+` immediately inside `[…](`) so non-link bracket text can never be corrupted.
2. REJECTED alternative — fix the source TEXT wart in `skills/getff/` so the pure transform suffices: touches dist-MANIFEST-covered sources (`build-getff-dist.sh:42` ships `skills`) for zero consumer gain. Recorded as an observation, not taken (T5; also out of umbrella scope).
3. tool-bootstrapping mode = byte-copy — today's bytes hold, no arm fires.

## §3 Ref classification per CORE-four skill (row 0.4 — full population, T9/T10)

Probe: copy each `.claude/skills/<name>/` → apply the 15 arms → `diff -r` source vs output.

| Skill | Files carried | Hunks | Every hunk is | Prose survivors (left verbatim, installer parity — backticks are not links) |
|---|---|---|---|---|
| ai-doc | `SKILL.md` + `anthropic-and-aif-residue.md` | 2 (`SKILL.md:9` README + rules links; `:19` docs link) | link-TARGET rewrite | `:3` description paths; `superpowers:` names |
| rule-research | `SKILL.md` | 1 (`:18` rules link) | link-TARGET rewrite | `:18` `` `agents/rule-researcher.md` ``, `:22` `` `packages/core/research/allowlist-resolver.ts` `` |
| rule-tests | `SKILL.md` | 1 (`:27` docs link) | link-TARGET rewrite | `:23` `` `packages/core/backends/*/capability-matrix.json` ``; `superpowers:`/`mattpocock:` names |
| template-audit | `SKILL.md` | 2 (`:11` README + packages links; `:45` docs link) | link-TARGET rewrite | `:20` npm command line |

**No skill needs prose adaptation ⇒ no 0.4 DECISION-NEEDED fork.** (h)-impact: blob URLs are skipped by the checker's URL arm (`24:293` `^(?:[a-z][a-z0-9+.-]*:|//|#)` → continue); sibling links resolve in-payload; `KNOWN_PAYLOAD_LINK_DEBT` is currently EMPTY (`:339`) — the four new entries must ship fully transformed, which is precisely what the generator guarantees.

## §4 Forks surfaced (kickoff §5 ATTN triggers) — recommendations + resolution owners

- **F1 — generator placement (extend twins script vs sibling).** Evidence: the twins generator runs two populations on one marker contract `@plugin-transform: absent|sed|manual` (`scripts/generate-plugin-twins.sh:4-11`), byte-identity obligations (`24(d)`, `:387`), and a skip-no-twin rule. Skills need a THIRD contract shape: per-entry derivation MODE (`byte-copy` / `transform` / `transform+textstrip`) × source-population SWITCH (`.claude/skills/` vs `skills/`) — not expressible as the twins marker without overloading it; the twin-depth problem (header `:20-33`, one level) differs from the skills depth problem (CORE four sit two levels deeper than `.claude/skills/`). **Recommendation: sibling `scripts/generate-plugin-skills.sh`** mirroring the twins conventions (LOG_LEVEL, idempotence, no-op-on-clean, exit-code contract), with 24(g)/(h) as the unchanged CI backstop. Resolution owner: Task 2 consult (SSOT + external search ≥3 phrasings) records the verdict in the capability commit. Surfaced for operator visibility per kickoff §5; proceed-on-recommendation unless overridden.
- **F2 — transform reuse (source `setup.d/lib.sh` vs extract vs reimplement).** Constraint: kickoff §6 non-goal «No installer changes» — `setup.d/lib.sh` is installer surface (shipped via PAYLOAD `setup.d`). Options: (a) generator sources `lib.sh` — pulls ~1800 lines of installer runtime into a pre-commit context (side-effect risk at source time; the LIB_ONLY export-leak class is documented); (b) extract the transform into a shared lib sourced by both — cleanest single-source, but IS an installer-surface change (non-goal breach; available only if the operator relaxes §6); (c) reimplement the 15 arms in the generator + a **paired parity gate** asserting arm-set equality against `lib.sh:148-162` (mechanized `dual-implementation-discipline.md` §7). **Recommendation: (c)** — non-goal-safe, and divergence between the two copies becomes mechanically impossible. Resolution owner: Task 2 records the verdict; (b) re-opens if §6 is relaxed.
- **F3 — getff derivation depth. RESOLVED in §2** — no operator decision needed: `transform+textstrip` reproduces today's plugin bytes exactly, so the Stage 1 «regeneration is a no-op on a clean tree» contract holds from the first run.
- **F4 — ZCode cache mechanics (0.6). INCONCLUSIVE-needs-host.** Host commands for Stage 3 (recorded so the host run needs no re-derivation): `ls ~/.zcode/cli/plugins/cache/getff/`; `find ~/.zcode/cli/plugins/cache/getff -maxdepth 2 -name plugin.json -exec grep -H version {} +`; live-session skill list showing the four; `/ai-doc` slash-invocation (mechanism: survey #1699 §11.1 — model-mediated Skill-tool resolution, live-corroborated); `bash scripts/probe-zcode-runtime.sh` → 17/17 expected. The 3-site version bump 0.2.0→0.3.0 remains the refresh-key hypothesis — **confirm-or-falsify on host before PR 3 merges** (plan Task 6). Plugin channel criticality is already live-proven: ZCode does not parse `.claude/settings.json` — the hook set reaches it only via `plugin/hooks/hooks.json` ([zcode-live-smoke §1](2026-09-10-zcode-live-smoke.md)), and plugin `skills/` is a recognized ZCode skill channel (survey #1699 §5).
- **F5 — DECISION-NEEDED: a recorded 2026-06-22/09-03 decision explicitly excludes `rule-research` + `rule-tests` from the plugin channel.** Surfaced by the cold backward sweep (#17/#18), verified verbatim this session. The record: `plugin/README.md:27-29` («The set is deliberately narrower… rule-authoring skills are bound to the installer by their own text… shipping them here would ship instructions their reader cannot execute»); spec `2026-06-22-cc-plugin-packaging-design.md:111` («stay OUT on a different ground — honesty, not cost»); decisions ledger `2026-06-22-cc-plugin-packaging.decisions.md:19-25` (Option A, «B is rejected on honesty»). The honesty ground is real text: `rule-research/SKILL.md:24` «then run `./setup --full` to synthesize the researched rules» (protocol ends in an installer command); `rule-tests/SKILL.md:23` framework-root reads «are NOT delivered to consumers» (self-scoped honestly). **Superseding evidence on the other side:** (a) operator GO 2026-09-11 (kickoff §0 + §7 «the CORE four are the extension set», do-not-re-litigate) postdates the 2026-09-03 ledger; (b) the installer's CORE tier — `setup.d/lib.sh:50-61`, assigned 2026-08-01 — already ships these EXACT four to consumers as «always shipped, consumer-facing, no aif-handoff runtime assumed», the same link-only adaptation depth; (c) the plugin bundles the installer itself (`plugin/README.md:21` `install/ — bundled install.sh + templates`), so the `./setup` path the skills name exists on-channel. **Recommendation: ship as-is (link-only, no prose adaptation — §3) + REQUIRED dated supersession notes** in `plugin/README.md:23-29`, spec `:111`, and the ledger (append-only pattern exists at ledger `:35`); Stage 2 PR body carries the explicit supersession rationale. Wrong-if: the operator weighs the June honesty ground above the 2026-09-11 GO — then descope `rule-research`/`rule-tests` and ship `ai-doc`+`template-audit` only (one-line override, payload M1_SET stays at four entries). Never decided silently — this fork goes to the operator with the PR.

## §5 Stage 1–2 design inputs (locked by evidence, not decided silently)

| `plugin/skills/` entry | Source population | Generator mode | 24(g) watch |
|---|---|---|---|
| `getff` | `skills/getff/` | copy + transform + textstrip (§2) | `skills/` normalised tier — unchanged (`:446-451`) |
| `tool-bootstrapping` | `skills/tool-bootstrapping/` | byte-copy | `skills/` byte tier — unchanged |
| `ai-doc` `rule-research` `rule-tests` `template-audit` | `.claude/skills/<name>/` | copy + transform (§3) | **EXTENSION**: at the `:451` `existsSync(skills/<name>)` skip, fall back to `.claude/skills/<name>` compared through `normaliseChannelLinks` (`:446` — it strips ALL link targets + TEXT ladders, so transform output normalises SAME as the raw source; the test embeds no transform). The `:451` skip stays **only** for true plugin-native entries |
| `installing-enforcement` `using-getff` | none — plugin-native | never touched; `M1_SET` membership unchanged | `:451` skip applies |

Gate-shape notes: pre-commit arm mirrors the twins arm (`.husky/pre-commit:220-241`): staged paths under the two source populations → run generator → `git add plugin/skills/`. `M1_SET` (`:418`) promotion of the CORE four is a recorded-membership decision — Stage 2 PR body states the need (kickoff §1 item 3). The clobber-guard loss class is on record in the (g) WHY-block (`:415-417`: the 10-week spec-table mismatch + the 6-of-6 getff drift). tool-bootstrapping `.claude→skills` prose fork: stays hand-maintained, disposition recorded with rationale at Task 4 (never silently deleted — kickoff Stage 1 item 5).

Sweep-derived obligations (cold backward sweep, §6 — each lands with the stage named):

1. **24(g) WHY-block `:413-415`** asserts «Nothing generates plugin/skills/ — no script writes it» — becomes FALSE at generator landing; update in the SAME change that adds the generator (Task 3/4), else the gate's own comment is a doc-gate lie.
2. **New `tests/plugin/*.test.sh` are CI-RED until wired** — principle 41 (`41-shell-test-ci-coverage.test.ts:19-28`) + `.github/workflows/audit-self.yml:573-596` (the `:570-577` comment records the identical twins incident); wire any new suite in the same PR (Task 4).
3. **`context7.json:17-18`** `excludeFolders` covers `plugin/skills/getff` only — the derived copies would double-publish into the docs corpus beside their sources (tool-bootstrapping duplication already exists); widen the exclusion with the derived set (Stage 2).
4. **`.claude/rules/git-conflict-merge-forward.md:25-57`** — the CONFLICTING-PR regen list covers plugin/hooks+agents twins only; `plugin/skills/` must join it or skills twins get hand-merged (the forbidden failure). **Maintainer-owned file** (.claude/rules, Artifact Ownership Contract) — coordinator surfaces at PR merge; agents do not edit.
5. **`extension.json` skills roster** (getff, tool-bootstrapping, template-audit, ai-doc, rule-research — no rule-tests) + `:19` «the plugin payload mirrors this set» — pre-existing roster gap that becomes load-bearing at 6 entries; update or record divergence (Stage 2).
6. **[2026-07-18-zcode-full-parity-census.md:120-124](2026-07-18-zcode-full-parity-census.md)** — «plugin/skills/ ships only getff, using-getff, installing-enforcement» is already stale (omits tool-bootstrapping) and grows staler at +4; dated correction note in Stage 2's doc pass (append-only patch discipline).

## §6 §1.7 self-review

### Forward-check applied

- **doc-authority:** research-patch per folder README (folder-scoped authority, no per-file header required); `<!-- scope: -->` marker present; no goal-bearing artefact touched (T5 held — zero source edits; probe scratch removed pre-commit).
- **build-vs-reuse:** this patch lands NO capability; the Stage 1 consult (SSOT + external search + `Prior-art:` trailer) is Task 2's deliverable and this patch supplies its inputs (F1/F2).
- **destination-environment-verification:** 0.6 labelled INCONCLUSIVE-needs-host; the probe SKIP is recorded as a SKIP, never as pass evidence.
- **effort-worthiness L1:** the kickoff's own §8 host-verify fence was re-run where sufficient; additional probes only where a claim was imprecise (getff TEXT ladder) — no extra rounds for form.
- **dual-implementation-discipline §7:** F2 recommendation mechanizes the equivalence instead of trusting two hand-kept copies.

### Backward-check applied

Class of this change = «a research patch recording plugin/skills derivability + gate-coverage claims».

Surfaces where this class occurs / could contradict, enumerated:

1. [2026-09-10-zcode-full-parity-rphase-survey.md §11](2026-09-10-zcode-full-parity-rphase-survey.md) (skills deep-audit) — different question (works-on-ZCode vs derivable-from-source); `§11.2` audits live-load of the project skills, never plugin-copy derivation — SWEPT-CLEAN, no verdict conflict.
2. [2026-09-01-s3-derivable-prose-inventory.md](2026-09-01-s3-derivable-prose-inventory.md) — derivable prose in repo docs (INSTALL-FOR-AI rosters); no `plugin/skills` row; its B1 cites the same `skills/` manifest as installer source, consistent with §2 — SWEPT-CLEAN.
3. `packages/core/principles/24-plugin-manifest-integrity.test.ts` — every gate claim in this patch re-verified against the live file (`:339`, `:387`, `:412`, `:418`, `:446`, `:451`, `:465`) — SWEPT-CLEAN.
4. `.claude/rules/zcode-parity-doctrine.md` + doctrine-adjacent claims — this patch cites survey #1699 §5 / live-smoke §1 instead of re-asserting doctrine rows; no doctrine row edited — SWEPT-CLEAN.
5. `scripts/generate-plugin-twins.sh` — cited for contract shape (`:1-43`), not modified; its populations (`plugin/hooks`, `plugin/agents`) are untouched by everything recorded here — SWEPT-CLEAN.
6. `setup.d/lib.sh` transform (`:144-165`) + `setup.d/10-skills.sh` (grep → no `plugin` refs; installer never writes `plugin/`) — replication recorded as F2(c) parity obligation; GAP-FOUND→ACTION: none for this patch (no code landed).
7. Consumer-facing shipped artefacts (`packages/core/templates/**`, `INSTALL-FOR-AI.md`) — Stage 0 makes no claim about them; untouched — SWEPT-CLEAN.

Cold sweep: the T21 cold backward-sweep-auditor ran over the change CLASS (never the diff) at patch-assembly time; its per-surface GAP/CLEAN report is appended below and joins the Stage PR body.

### Cold backward-sweep report (agents/backward-sweep-auditor.md, 2026-09-11)

Verbatim report from the cold agent (34 tool calls, ~10 min; handed ONLY the change class, never the diff). Highest-risk findings per the agent: #17/#18 (the F5 recorded rejection), #7 (gate WHY-comment becomes false), #22 (context7 double-publish), #28 (merge-forward carve-out). GAP-FOUND dispositions: F5 above + obligations 1-6 above; the remaining GAP-FOUND rows are the change's own targets (generator absence, pre-commit arm, (g) `:451`, wiring), already owned by Tasks 3-5.

<details>
<summary>Full per-surface verdicts (35 surfaces)</summary>

**Generators / drift checks (scripts/):** `generate-plugin-twins.sh` — GAP (header :2-11 declares TWO populations; whichever F1 fork lands, that claim changes). `check-skill-drift.sh` — GAP (scope :15-16 + finds :86,:149,:200,:267 never scan `plugin/`). `build-getff-dist.sh` — CLEAN (:42 ships `skills/` not `plugin/`; MANIFEST grep → 0, verified live).
**Hooks:** `.husky/pre-commit` — GAP (arm :220-241 re-stages only plugin/hooks+agents; no skills arm). `pre-push.ts:922-928` skillDriftSection — CLEAN (plugin/ outside its population; 24 is the plugin watcher). `pre-push.ts:1740-1809` §8 lychee — CLEAN (exclusion covers plugin/agents only; blob-URL output skipped by URL arms).
**Principle tests:** 24(g) — GAP/change-target (M1_SET :418; `:450` looks up sources ONLY under `skills/`; `:451` is the extension line; WHY-block `:413-415` becomes false; `KNOWN_PAYLOAD_LINK_DEBT :339` must stay empty). 24(a)/(b)/(d)/(e)/(f) — CLEAN (twins-only populations). Principle 41 :19-28 — GAP-obligation (new tests/plugin suites RED until wired; twins precedent audit-self.yml:573-577).
**tests/plugin/:** bootstrap — CLEAN (`:30-42` floor ≥2 + per-skill frontmatter, not set equality). install-seam :46 / opencode-adapter :20 / manifest-valid — CLEAN (plugin-native or directory-level). twin-generation/-clobber-guard/-emit — CLEAN now / obligation (if F1 = extend, these widen; if sibling, new wired test per #9).
**Installer:** `setup.d/10-skills.sh` — CLEAN (never writes plugin/; `_copy_tree_with_transform` :26/:41 is the adaptation-depth precedent). `setup.d/lib.sh` — CLEAN SSOT / standing obligation (`GETFF_SKILLS_CORE` :61 must equal the generator's entry list or record divergence; 15 arms :144-162 mirrored byte-for-byte per F2(c)).
**CI:** audit-self.yml:573-596 — GAP-obligation (new generator/suites uncovered unless wired; the :570-577 comment records the identical twins incident).
**Version chain:** marketplace.json :8/:16 + plugin.json :4 + fetch-and-wire.sh :32 — GAP-must-bump (3-site 0.2.0; parity enforced by 24 V2 :139-144 + 24(c) :372-379).
**Docs claiming what ships:** plugin/README.md :23/:27 — GAP (defines the set as FOUR; rejects rule-research/rule-tests → F5). spec 2026-06-22 :111 — GAP (strongest recorded prohibition → F5 supersession note). decisions ledger :19/:25 — GAP («no generator»; «getff quarantined» — both become false; append-only dated note per :35 pattern). census 2026-07-18 :120-124 — GAP (stale set + three «no twin» rows → obligation 6). extension.json :19 + roster — GAP (mirror claim + missing rule-tests → obligation 5). context7.json :17-18 — GAP (exclusion must widen → obligation 3). .opencode/INSTALL.md — CLEAN (directory-level, flows automatically; watched by opencode-adapter.test.sh:20). plans 2026-06-22 :128-136 — CLEAN (historical, reconciled). census 2026-06-28 :68 — CLEAN (dated closed patch). beta-release-night-morning-report.decisions :111 — CLEAN (historical, superseded by live 24(g) tier).
**Rules:** zcode-parity-doctrine.md — CLEAN (scope :5/:22 is plugin/hooks only). git-conflict-merge-forward.md :25-57 — GAP-obligation (regen list must add plugin/skills/ → obligation 4). ci-tool-pinning.md :9-12,:51 — CLEAN (population 2 auto-covers the new shell scripts). operational-conventions.md :80 — CLEAN (verified live: no disable-model-invocation on any CORE-four source). other rules — CLEAN (`grep -rn "plugin" .claude/rules/*.md` → hooks-twin/companion/census rows only).
**Kickoff + own records:** kickoff.md — CLEAN as class statement (§1.1 :42-44, §1.3 :82-86, §1.4 :88-91, Stage 2.2 :100-102 internally consistent). This patch — GAP at sweep time (untracked + probe residue on disk; both resolved before commit: scratch removed, file committed in PR 1).
**Writer census (negative existence):** repo-wide `grep -rln "plugin/skills"` over sh/mjs/ts/yml → gates/tests only; `.gitignore` → no plugin entry; no script/installer/hook/workflow writes plugin/skills today — confirms 0.1; sole payload producer is manual hand-copy, the state this change retires.

</details>

## Tags

`#claim-from-memory-not-source` — the kickoff §1 «byte-identical — the plugin copies already are» claim was session-cached prose; the re-derivation (§2) replaced it with a byte-exact derivation recipe. Prevention (folder-README form): before an umbrella kickoff asserts the byte-relationship of two populations in §1, its Stage 0 row must name the comparison probe per FILE (not per directory), because link-normalisation hides at file granularity (`diff -rq` per skill dir, not per population).
