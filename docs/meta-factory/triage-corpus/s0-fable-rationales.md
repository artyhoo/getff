# S0 blind rater record — Fable seat (2026-08-11)

Scope: verbatim verdict + rationale record of the blind Fable rater over the 32 S0 probe rows
(display order = `display_n` in [s0-raters.csv](s0-raters.csv)). Part of the S0 ground-truth
construction (see [README.md](README.md) §Truth construction). Blindness held: the seat ran with
zero tool uses (verified from the task usage record), judging only the provided row texts.

1. MATERIAL — The shipped paired-negative greps a string it just wrote, so the shipped suite stays green if the leak detector regresses; the only real discrimination proof lived in a non-shipped scratch mutation.
2. MATERIAL — Source dates feed the patch's dated-coverage predicate and freshness assessment in a fast-moving research area; wrong years mean the record's recency claims — and the confidence placed on them — were false.
3. MATERIAL — A meta-scanner whose declared population silently excludes a member reports coverage while missing a stub — exactly the silent-bypass class the repo's gates exist to close.
4. MATERIAL — With the fixture source unspecified, the parity check degrades to a file compared with itself, so the seeded-red can never fire — the executor could ship a gate structurally incapable of going red.
5. MATERIAL — An acceptance criterion with zero assertions anywhere is unenforced; regressions of criterion 7 would pass silently through both this diff and the standing suite.
6. IMMATERIAL — Stderr noise on an already-failed flag write in a hook's error path; no gate or decision consumes that output and the happy path is unchanged.
7. IMMATERIAL — A slug typo whose quoted content is verified verbatim at the corrected URL, in a doc explicitly excluded from the live link audit; nothing downstream reads or breaks on it.
8. MATERIAL — The regex could not cross the closing brace and bound the provenance source's tier instead of the rule's, so a lock exhibiting the exact conflation criterion 3 forbids passed the assertion.
9. MATERIAL — Asserting the capture is merely non-empty lets the guard-the-guard go green while the /chat request it exists to detect is absent — a vacuous check on an enforcement surface.
10. IMMATERIAL — The auditor re-ran the shipped pattern over real-separator shapes and got identical verdicts 7/7, so the one-byte fixture drift is verified non-load-bearing today; the flag is purely prophylactic.
11. MATERIAL — Three surviving mutants are untested behavior branches in an enforcement hook — those paths (`-1m` table, `1m-deep` tier, combined composition) could break silently under a green suite.
12. MATERIAL — The «strongest external anchor» behind the calibrated parameters measures retrieval, the very class the patch demotes; re-rating it NO MATCH removed a false justification from a decision-bearing section.
13. MATERIAL — An invented causal attribution absent from the cited source, sitting in a durable provenance record; a fabricated cause left standing is precisely the unverified-claim failure the source-trust discipline targets (the 18-vs-19-day slip alone would be a nit).
14. MATERIAL — The coverage recount — itself a prior round's repair — counted an undated row as «dated 2026», leaving a flatly false load-bearing coverage predicate in the record for the third recurrence of the shape.
15. MATERIAL — The arm's precondition never occurs on the live path, so every python rule ships `{"provenance":[],"tier":2}` — the provenance/tier mechanism is functionally inert in production.
16. MATERIAL — Kickoff executors here follow citations literally; a range truncated before the template precedent removes required input and plausibly changes what the junior produces for the header docs.
17. IMMATERIAL — The finding itself establishes the referent is recoverable from the correct enumeration and line numbers; a dropped qualifier in a parenthetical misstates nothing about any named package.
18. MATERIAL — A spec clause silently absent from the kickoff means the executor never implements «npm path once live» — silent scope loss that changes the delivered work.
19. MATERIAL — D1 directed relocation of a file owned by another stage and of one that is copy_safe-installed from preset packages; executing as written breaks the ownership contract and fights the install pipeline.
20. MATERIAL — The disposition label is a status signal consumed by downstream sessions; «PARTLY ANSWERED» overstated what one unreplicated source can carry and risked premature threshold consumption the contour explicitly forbids.
21. MATERIAL — The kickoff authorized a cross-owner grab of umbrella B's named-owner SSOT against B-D5 and its own park contract; execution would have breached the Artifact Ownership Contract.
22. MATERIAL — A falsifier imported across task classes without disclosure and with the direction inverted; anyone acting on «~600k ceiling» for mechanical tails would draw the boundary on the wrong side of the measured finding.
23. MATERIAL — Two evidence lines false at the very SHA the kickoff cites — the executor's factual basis is wrong from the start, and work built on it inherits the error.
24. MATERIAL — Unregenerated fingerprints make the snapshot gate deterministically red, blocking the PR; the fix flips CI outcome, so behavior changed by construction.
25. IMMATERIAL — The finding concedes the compressed wording's substance is supported by v3 §4; no actual scope or behavior diverges between the two phrasings.
26. IMMATERIAL — The substantive repair is correct and durable; the leaked audit-round narrative in a shipped comment is context noise that changes no behavior and no consumer decision.
27. MATERIAL — `length > 100` while printing «loaded» is vacuous verification — a truncated bundle ships green through the consumer-matrix cell, a silent pass exactly where the gate must catch corruption.
28. MATERIAL — The zero-usage gate fails open on non-numeric totalTokens — it passes precisely when its input is malformed, a silent bypass of an enforcement step.
29. IMMATERIAL — The numbers stand; the fix only names the 18.5% baseline model precisely, and no parameter or decision in the patch hinged on the «predecessor» label.
30. IMMATERIAL — A header commit predating its addendum is a provenance-metadata nit; no gate consumes that header and none of the addendum's claims are invalidated by it.
31. MATERIAL — A binding invariant (the 6-item checklist on negative-existence claims) was skipped on four claims, and actually running it falsified two — the document's substantive claims changed.
32. IMMATERIAL — A line-number correction plus completing the citation list; nothing indicates the added presets overturned any claim the record makes.

SUMMARY: 23 MATERIAL / 9 IMMATERIAL / 0 ?
