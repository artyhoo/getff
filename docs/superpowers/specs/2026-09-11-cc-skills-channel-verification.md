# CC verification requirements — plugin skills channel (Stage 4, owner-deferred)

> **Status:** DEFERRED by operator directive 2026-09-11 (umbrella kickoff §7: «для клод кода потом проверим и исправим если что» — CC verification is documented here for the owner to schedule; it is explicitly NOT executor work in the plugin-skills-generator umbrella, and ZCode acceptance (Stage 3) does not depend on it).
> **Scope:** the five verification items kickoff §2 Stage 4 defines for the Claude Code consumer of the plugin skills channel, each as command + expected observable. **No CC fix belongs in that umbrella** (§6 non-goal) — a failing item below becomes a NEW fork/kickoff, never a drive-by patch.
> **Channel context:** the plugin is live-consumed via `.claude-plugin/marketplace.json` → `"source": "./plugin"`; the CORE four (`ai-doc`, `rule-research`, `rule-tests`, `template-audit`) ship generated from `.claude/skills/` link-only (Stage 0 §3, probe-proven per skill); version `0.3.0` at all three locked sites (Stage 2). Install commands per `plugin/README.md:9-13`.
> **Owner-facing starting state (probed 2026-09-11 through the aif container's read-only `~/.claude/plugins` mount):** the only marketplace registered on the operator host is `claude-plugins-official` (`known_marketplaces.json`) — `artyhoo/getff` is NOT yet registered, so V1 is a genuine first run, not an update.

## V1 — Install path + the four skills live

Commands (fresh CC session, consumer-shaped dir):

```text
/plugin marketplace add artyhoo/getff
/plugin install getff@getff
```

**Expected observable:** install completes clean (the plugin entry is `"strict": true` — schema errors surface loudly); the session skill list shows the plugin's skills INCLUDING `getff:ai-doc`, `getff:rule-research`, `getff:rule-tests`, `getff:template-audit`, alongside the pre-existing `getff:using-getff`, `getff:installing-enforcement`, `getff:tool-bootstrapping`, `getff:getff`; asking the session to use one (e.g. «use the ai-doc skill») resolves via the Skill tool.

**On failure:** capture the listing + invocation transcript verbatim; do not hand-fix in-session (scope fence above).

## V2 — Namespacing coexistence (plugin copy vs installer copy on ONE consumer)

**Setup:** the same consumer has the installer's copy (`.claude/skills/ai-doc` etc., via the bundled `install.sh` or `/getff:install-enforcement`) AND the plugin installed.

**Expected observable:** BOTH the workspace entry (`ai-doc`) and the plugin entry (`getff:ai-doc`) appear in the skill list, and session start raises no shadowing/conflict error. Content legitimately DIFFERS between the two (installer copy is link-adapted for in-repo use; plugin copy uses blob URLs) — coexistence, not equality, is the criterion.

## V3 — tool-bootstrapping disposition visible

The plugin entry must be the CONSUMER fork — generated byte-copy from `skills/tool-bootstrapping/` (the recorded Stage 1 disposition; generator contract `scripts/generate-plugin-skills.sh:1-48`) — never the operator fork. Probe the installed cache copy:

```bash
grep -c "@harness-posture" <cache>/skills/tool-bootstrapping/SKILL.md                 # → 0
grep -c "consumer projects that install this skill via" <cache>/skills/tool-bootstrapping/SKILL.md   # → ≥1
```

Source-truth for those counts (verified 2026-09-11 on the repo tree): `.claude/skills/tool-bootstrapping/SKILL.md` → 1 / 0; `skills/tool-bootstrapping/SKILL.md` → 0 / 1. If the installed copy shows the operator values, the derivation regressed to the wrong source population — surface, don't patch.

## V4 — Marketplace cache picks the bumped version

```bash
find ~/.claude/plugins/cache -maxdepth 4 -type d -name "getff"
# then, at the discovered root:
find <cache-root>/getff -name plugin.json -exec grep -H '"version"' {} +
```

**Expected observable:** the cache holds `0.3.0` (cache layout per precedent: `~/.claude/plugins/cache/<marketplace-or-org>/<plugin>/<version>/` — `2026-08-17-arch-prep-skill-stack-harmonization.md:23-24`). If an older version persists after `/plugin marketplace update` + reinstall, record the actual refresh mechanism — same hypothesis class as the ZCode refresh key (Stage 0 §4 F4: confirm-or-falsify, never assume).

## V5 — dist + install-sh baselines stable on CC-shaped CI (confirm-only)

```bash
grep -c "plugin/skills" packages/getff/MANIFEST.sha256     # → 0
grep -rl plugin tests/install-sh/baselines/                # → no hits
```

**Expected observable:** zero hits on both — the plugin channel sits OUTSIDE the dist payload and every install-sh baseline (Stage 0 row 0.5, re-verified 2026-09-11), so CC-shaped CI needs NO snapshot capture and NO MANIFEST churn. Any hit is a packaging regression to surface, not to absorb.

## Aftercare

- A failing item → NEW fork/kickoff (this umbrella's §6: «No CC fix pass inside this umbrella»).
- Record outputs (commands + verbatim observables) in the owning PR/retro per T3 discipline — no prose-only findings.
- Cross-refs: install commands `plugin/README.md:9-13`; CORE-four supersession rationale `2026-06-22-cc-plugin-packaging-design.md:113` + ledger Decision 2 (`2026-06-22-cc-plugin-packaging.decisions.md:37`); adjudication + per-skill link-only proof `docs/meta-factory/research-patches/2026-09-11-plugin-skills-generator-stage0-reverif.md` §3/§4-F5; ZCode-side counterpart `docs/meta-factory/research-patches/2026-09-11-plugin-skills-stage3-zcode-acceptance.md`.

## §1.7 self-review

### Forward-check applied

- **doc-authority-hierarchy:** internal spec-dir verification record, sibling-formatted to `2026-07-31-arch-v2-s-a-acceptance.md` (metadata blockquote header, no goal-bearing artefact touched); English throughout (language-discipline §1 cat 1).
- **build-vs-reuse:** documentation only — no capability introduced (doc files are an explicit capability-commit carve-out); no `Prior-art:` obligation.
- **destination-environment-verification:** the doc's load-bearing claim («operator host has no getff marketplace yet») is itself probe-backed, not assumed (header blockquote, dated).
- **effort-worthiness L0/L1:** `build-and-verify` — five reversible doc items; every command is runnable by the owner without re-derivation.

### Backward-check applied

Class of this change = «CC-side verification runbooks for the plugin skills channel».

Surfaces where this class occurs or could contradict, enumerated:

1. `docs/superpowers/plans/2026-06-22-cc-plugin-packaging.md:89,137` — the historical install-smoke steps for the SAME channel (marketplace add + install + Skill-tool invocation); V1 extends them to the six-skill set — SWEPT-CLEAN (aligned and cited, not superseded).
2. `plugin/README.md:9-13` — install commands; V1 quotes them verbatim — SWEPT-CLEAN.
3. `2026-06-22-cc-plugin-packaging-design.md:113` + `2026-06-22-cc-plugin-packaging.decisions.md:37-49` — the CORE-four supersession records this doc relies on; no contradiction, falsifier preserved on their side — SWEPT-CLEAN.
4. `INSTALL-FOR-AI.md` — grep for `plugin` → zero hits; the installer-channel consumer doc makes no plugin-skills claim this doc could contradict — SWEPT-CLEAN.
5. `docs/meta-factory/research-patches/2026-09-11-plugin-skills-stage3-zcode-acceptance.md` — the ZCode-side sibling evidence record; V4's confirm-or-falsify framing mirrors its H1 so the two harnesses record refresh mechanics symmetrically — SWEPT-CLEAN (cross-linked both directions).
6. `scripts/probe-zcode-runtime.sh` — CC has no equivalent probe; this doc deliberately does NOT claim one (no invented command) — SWEPT-CLEAN (absence recorded honestly).
