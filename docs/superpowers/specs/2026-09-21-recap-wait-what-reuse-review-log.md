# Recap hook reuses `/wait-what` + `CONTEXT.md` — cold review log

> **Authoritative for:** the per-finding dispositions of the `/arch` §2 cold reviews of
> [2026-09-21-recap-wait-what-reuse-design.md](2026-09-21-recap-wait-what-reuse-design.md).
> **NOT authoritative for:** the design itself — the spec; project goal —
> [README.md#why-this-exists](../../../README.md#why-this-exists).

Disposition vocabulary per [arch/SKILL.md §2](../../../.claude/skills/arch/SKILL.md):
`ACCEPTED | DISSOLVED | ESCALATED | FIXED`. Seats: top-down TD, bottom-up BU (mid tier, artifact
paths only — the spec, the handoff, and the repo at `origin/staging`).

## Round 1 — 2026-09-21 (TD REVISE, BU REVISE)

Both seats re-ran the measurement script independently and every table cell reproduced inside
transcript drift; the bottom-up seat audited every `path:NN` — 16 EXACT, one benign off-by-two
(fixed), zero wrong. The reports are presented per seat, never merged: each verdict stands on its
own axis. Every reviewer-supplied citation adopted into the spec was re-read on
`origin/staging` by the author first; one was off by one (`lang/ru.sh:101` → `:100`).

### Top-down

- TD-F1 MAJOR — D3's «corrected until learned» cannot converge: the retry Stop exits at the
  `stop_hook_active` guard before any counter runs, and the draft dropped the one-shot flag —
  **FIXED** (D3: per-term per-session flag, own prefix; undercount stated; R-9; measured 64
  unbounded blocks vs 29 bounded demands).
- TD-F2 MAJOR — the R-1 price omitted D3; the union is about half of armed turns — **FIXED**
  («The R-1 fork, priced»: per-N table with D3's bounded share; unbounded union 82 / 192 reported
  in Measurements).
- TD-F3 MAJOR — «disjoint files against S5» is false: D5c edits the Stop hook — **DISSOLVED**.
  Parent spec `:340` places D5c's eleven sites in `aif-doctor/SKILL.md`, not the hook, and
  `kickoff-s5.md` names neither the hook nor the packs (grep count 0, command in D7 step 1); the
  bottom-up seat reached the same reading. The «D-H» label is the kickoff's own title. D7 step 1
  now carries the evidence instead of the bare word.
- TD-F4 MAJOR — R-7 inverted the exposure: `--full` consumers are ARMED, so declining to arm
  here ships never-exercised arms live — **FIXED** (R-7 and D7 step 3: arming agreement is a
  merge precondition; declined → D1 only, arms dropped).
- TD-F5 MAJOR — handoff §5 Q1 half-answered: no false-positive evidence, card region not
  excluded — **FIXED** (script gained `--skip-card`, `--dedup`, `--band`; the 31-35 band read:
  0 structural false positives of 28; all numbers re-derived on the exempt scope).
- TD-F6 ESCALATED — 30 is looser than the STE bar the design imports, and the draft's
  «Russian is shorter» argument pointed the other way — **ESCALATED** to the operator as the
  R-1 fork; the faulty argument is removed and the trade is stated as a value trade.
- TD-F7 MINOR — contract text and checker can disagree on N under a garbage override —
  **FIXED** (D2: sanitize once before the packs run; the inherited `MAX_LINES` twin of the defect
  named and folded into the same slice).
- TD-F8 MINOR — «same region rule» named an obligation, not a seam — **FIXED** (D2:
  `_eot_recap_without_cards` text-emitting helper consumed by both).
- TD-F9 MINOR — a locked counter write inside a `$(…)`-consumed function — **FIXED** (D3: runs
  before the call, descriptors redirected, one hand-over variable).
- TD note — 15 % duplicate blocks from resumed sessions — **ACCEPTED** (`--dedup` is now the
  primary run; the raw run is reported beside it).

### Bottom-up

- BU-F1 MAJOR — «twins regenerated by pre-commit» is false for the lang packs — **FIXED** (D7
  step 2: hand `cp`, parity blind spot, armed-twin fixture).
- BU-F2 MAJOR — `lib/glossary.sh` never reaches `install.sh` / `setup.d` / plugin consumers —
  **FIXED** (D7 step 2: by-name delivery lines + snapshot; plugin channel D3-inert by rule; D3
  «inert» now covers an absent lib and forbids an abort).
- BU-F3 MAJOR — D3 matched terms inside code spans; 17 of 91 blocks were paths — **FIXED** (D3
  «prose only»; script strips code spans, URLs and link targets; re-measured 64 of 78).
- BU-F4 MINOR — same as TD-F8 — **FIXED** (one disposition, D2).
- BU notes — `avoid_phrase_blocks` counted hits, not blocks — **FIXED** in the script; the 7-day
  re-ask fraction slides — **ACCEPTED** (pinned to its run date, re-run value recorded); the
  script is absent on `staging` until this PR — **ACCEPTED** (said so in Measurements); a bare
  `»` or `…` token counts as a word — **ACCEPTED** as a known sub-percent limit.

Round 2 was not run: every MAJOR is FIXED or DISSOLVED with evidence and the one open item is
the operator's fork, which no cold seat can close
([effort-worthiness.md](../../../.claude/rules/effort-worthiness.md): what breaks if a fix is
wrong is a spec line the implementing slice's own review re-reads, at the cost of one edit).
