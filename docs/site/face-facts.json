{
  "schema": "getff.face-facts/v1",
  "_note": [
    "Derived, not asserted — `scripts/render-face-facts.mjs --write`; `--check` fails on any hand",
    "edit (umbrella D42; face-pages spec §7, seams FS1/FS2). Sources per family are cited in each",
    "family's `source` as path:line — never permalinks (D34: a pre-commit renderer cannot know the",
    "SHA of the commit it is creating).",
    "The landing build consumes this file as pinned data and stamps `url`, `verified-at: <pinned",
    "framework sha>` and resolved permalinks at build time (D42 falsifier (b): a URL-dependent",
    "value is a landing-side stamp, not a face fact). The landing runs no framework script (D35).",
    "Fence-region checking on the face pages (FS1 page arm) activates at S1 when the pages land",
    "(D24b byte-identity seam); today the check is this manifest's own byte-identity.",
    "Counts: CLAIMS-LEDGER.md counts are landing-side (the file is not in this repo). npm section:",
    "absent until the package publishes ≥ 0.1.0 (§7 \"absent until then\")."
  ],
  "maturity": {
    "source": "packages/core/manifest/maturity.json",
    "layers": {
      "rules": {
        "label": "beta",
        "definition": "One-command install; a rule provably fires on your code (`npm run lint` goes RED on a planted violation); rule docs are generated from the delivered rule set.",
        "caveat": "Two runtime rules (R7/R8) ship deferred behind AIF_STRICT_RUNTIME=1 — a default lint run stays green on their triggers.",
        "verified-at": "2026-09-14"
      },
      "factory": {
        "label": "experimental",
        "definition": "Stack scaffold + templates; a dedicated rule-pack is not yet shipped.",
        "caveat": "The factory depth clones a preset whose rule-packs are uneven — the per-stack rows govern.",
        "verified-at": "2026-09-14"
      }
    },
    "stacks": {
      "ts-server": {
        "label": "beta",
        "definition": "One-command install; a rule provably fires on your code (`npm run lint` goes RED on a planted violation); rule docs are generated from the delivered rule set.",
        "caveat": "Default TS lane; it shares the manifest-rendered multi-stack rule pack with react-next — no ts-server-specific rules beyond it.",
        "generation": "generated",
        "verified-at": "2026-09-14"
      },
      "react-next": {
        "label": "beta",
        "definition": "One-command install; a rule provably fires on your code (`npm run lint` goes RED on a planted violation); rule docs are generated from the delivered rule set.",
        "caveat": "Wires three custom rules by default (two more behind AIF_STRICT_RUNTIME=1) on top of the shared manifest-rendered rule pack.",
        "generation": "generated",
        "verified-at": "2026-09-14"
      },
      "react-spa": {
        "label": "early",
        "definition": "Installs one-command and wires real rules, but the rule-pack is still growing.",
        "caveat": "Wires two custom rules by default (two more behind AIF_STRICT_RUNTIME=1); the rule-pack is still growing.",
        "generation": "static",
        "verified-at": "2026-09-14"
      },
      "react-native": {
        "label": "experimental",
        "definition": "Stack scaffold + templates; a dedicated rule-pack is not yet shipped.",
        "caveat": "Ships stack scaffold + templates; no custom rule is wired yet.",
        "generation": "static",
        "verified-at": "2026-09-14"
      },
      "python": {
        "label": "alpha",
        "definition": "Installs one-command and fires on verified fixtures; rule generation is partial or deferred.",
        "caveat": "Non-npm lane — ast-grep + ruff only; the npm custom-rule engine does not apply.",
        "generation": "generated",
        "verified-at": "2026-09-14"
      },
      "cargo": {
        "label": "alpha",
        "definition": "Installs one-command and fires on verified fixtures; rule generation is partial or deferred.",
        "caveat": "cargo-deny ships as a starter config no workflow runs; the clippy bans are the enforced lane (CI-failing when violated).",
        "generation": "deferred",
        "verified-at": "2026-09-14"
      },
      "go": {
        "label": "alpha",
        "definition": "Installs one-command and fires on verified fixtures; rule generation is partial or deferred.",
        "caveat": "The forbidigo ban ships as .golangci.yml on a fresh repo, or as getff-golangci.yml when you already have one (inert locally until you opt in); the delivered CI workflow runs it build-failing in both cells; rule generation is deferred (operator, 2026-09-09).",
        "generation": "deferred",
        "verified-at": "2026-09-14"
      }
    }
  },
  "installCommands": {
    "source": {
      "positionals": "setup:67",
      "laneTable": "install.sh:281",
      "flags": "README.md:147"
    },
    "stacks": [
      "ts-server",
      "react-next",
      "react-spa",
      "react-native"
    ],
    "lanes": [
      {
        "lane": "python",
        "label": "Python",
        "detect": "pyproject.toml"
      },
      {
        "lane": "cargo",
        "label": "Rust/cargo",
        "detect": "Cargo.toml"
      },
      {
        "lane": "go",
        "label": "Go",
        "detect": "go.mod"
      }
    ],
    "flags": [
      {
        "flag": "--yes",
        "line": 149
      },
      {
        "flag": "--all",
        "line": 150
      },
      {
        "flag": "--dry-run",
        "line": 151
      }
    ]
  },
  "firstSteps": {
    "source": "packages/core/templates/shared/first-steps.source.json",
    "renders": [
      "packages/core/templates/shared/AI-USAGE-GUIDE.md — AI render (C1, this repo; parity-gated)",
      "landing site /docs/installation/ — human render (getff-ai-site S1; the first-steps-* quick-start slugs are stubs per face spec §4, so the render target is the Installation page — BU-F14 re-point, S0a)"
    ],
    "sequences": {
      "core": {
        "goal": "install -> a rule provably fires on your code",
        "profileFlag": "--profile core (core is never the default — install.sh:648 makes the non-interactive default `env`)",
        "steps": [
          {
            "id": "install",
            "title": "Install at core depth",
            "action": "From your project root run `bash <getff>/setup -y --profile core <stack>` — `core` must be named, because `-y` on its own resolves to `env` (install.sh:648). (stacks: ts-server, react-next, react-spa, react-native; `python`, `cargo` and `go` are separate non-npm toolchain lanes, each an explicit positional — `./setup cargo` or `bash install.sh cargo`, the wrapper forwards lanes verbatim). Omit the stack to auto-detect.",
            "evidence": "install.sh; setup.d/30-templates.sh"
          },
          {
            "id": "verify-payload",
            "title": "Verify the payload landed",
            "action": "`ls AGENTS.md .ai-factory/ scripts/` — you should see AGENTS.md, `.ai-factory/{DESCRIPTION.md,ARCHITECTURE.md,RULES.md}` and `scripts/audit-ai-docs.sh`. Nothing here is optional; a missing file means the install did not finish.",
            "evidence": "tests/install-sh/f8d-ai-factory-sot-materialized.test.sh"
          },
          {
            "id": "fill-passport",
            "title": "Fill the project passport",
            "action": "Replace every `<PLACEHOLDER>` in `.ai-factory/DESCRIPTION.md` (domain, stack, constraints, non-goals). This is the file AGENTS.md sends every future session to first, so a passport left unfilled degrades every later session.",
            "evidence": "packages/core/templates/shared/DESCRIPTION.template.md"
          },
          {
            "id": "prove-rules-not-inert",
            "title": "Prove the rules are not inert on your layout",
            "action": "`bash scripts/check-rule-globs.sh` — fails when a shipped custom rule matches ZERO files in your layout, i.e. it is installed but silently enforcing nothing. If it fires, widen `RULE_GLOBS` in `eslint.config.mjs` to cover your layout. On a brand-new skeleton with no source files yet this FAILS by design — every rule matches zero files. That is the expected first run; re-run it once your first `src/` files exist.",
            "evidence": "scripts/check-rule-globs.sh (shipped at core; verified in a temp consumer 2026-08-08)"
          },
          {
            "id": "watch-a-rule-fire",
            "title": "Watch a rule actually fire",
            "action": "`bash scripts/check-fences-fire.sh` — plants deliberately-bad input in a temp dir and asserts the installed ESLint rules go RED on it. This is the first-rule-fires moment: an installed rule that has never been seen to fire is an unproven claim.",
            "evidence": "scripts/check-fences-fire.sh (shipped at core; prior-art-evaluations.md#184)"
          },
          {
            "id": "fire-on-your-code",
            "title": "Fire a rule on YOUR code, not a fixture",
            "action": "Plant one violation of a shipped getff rule in YOUR tree and run your stack's native gate — the RED line names your file. npm: add `z.string().parse(input)` to a file under a boundary glob (`**/routes/**`, `**/handlers/**`, `**/controllers/**`, `**/app/api/**`, `**/actions/**`), run `npm run lint` — RED from `rules-as-tests/no-unsafe-zod-parse` (R2, the one custom rule wired unconditionally; R7/R8 fire only under `AIF_STRICT_RUNTIME=1`). Lanes: python — add `datetime.utcnow()`, run `ruff check . --config .getff/ruff-bans.toml`; cargo — add `std::env::var(\"X\")`, run `cargo clippy`; go — add `os.Getenv(\"X\")`, run `golangci-lint run`.",
            "evidence": "templates/ts-server/eslint.config.mjs:179 (R2 unconditional); setup.d/70-deps.sh:75 (npm run lint); setup.d/45-python.sh:587 / 46-cargo.sh:256 / 47-go.sh:252 (lane firing self-checks)"
          },
          {
            "id": "run-the-gate",
            "title": "Run the gate you will run every day",
            "action": "`bash scripts/audit-ai-docs.sh` — drift + code-vs-docs probes (~10 sec). Expect findings on a fresh project; the INSTALL-FOR-AI «Expected first-run failures» table lists which ones are normal.",
            "evidence": "scripts/audit-ai-docs.sh"
          },
          {
            "id": "research-your-stack",
            "title": "Continue into rule research in the same session",
            "action": "Invoke `/rule-research` (or read `.claude/agents/rule-researcher.md` on a harness without skills). The installer delivered a curated starter set; researching stack-specific rules from live documentation is the next step of the same lifecycle, not a later project.",
            "evidence": ".claude/skills/rule-research/SKILL.md (shipped at core)"
          }
        ]
      },
      "env": {
        "goal": "core + the multi-model contour surface: tier criteria on disk and one idea through /arch",
        "profileFlag": "--profile env",
        "steps": [
          {
            "id": "install",
            "title": "Install at env depth",
            "action": "`bash <getff>/install.sh <stack> --profile env`. Already on core? Re-run that same command — the deeper payload is added and every core artefact stays byte-identical except `.prettierignore`, whose managed block gains the new paths. Do not reach for `--refresh` to upgrade: it re-delivers fixes to what you already have and does not reliably deepen an install — on a `core` project `--refresh --profile env` exits 0 while `tier-home.md` and `.claude/skills/arch/` stay absent.",
            "evidence": "install.sh:1342-1350 — the tier-home refresh arm is presence-gated («refresh must not create it on core»); `arch` is in no do_refresh skill loop. Measured on a core consumer against staging 2026-08-09: `--refresh --profile env` → exit 0, tier-home NO, arch NO (the worktree-scripts arm at install.sh:1251-1256 was ungated and delivered 4 env-tier scripts on a core project; it has carried the env|factory|presence gate since #1334, so that depth-boundary defect is closed); plain `--profile env` → exit 0, tier-home YES, arch YES, every pre-existing core file byte-identical except .prettierignore. Also INSTALL-FOR-AI.md «Install depth profiles»"
          },
          {
            "id": "verify-payload",
            "title": "Verify the payload landed",
            "action": "`ls AGENTS.md .ai-factory/ scripts/` plus `.ai-factory/tier-home.md` and the five skills env adds over core: `.claude/skills/arch/`, `.claude/skills/night-mode/`, `.claude/skills/orchestrator/`, `.claude/skills/pipeline/` and `.claude/skills/reviewer/`.",
            "evidence": "setup.d/30-templates.sh (tier-home at env+); verified in a temp consumer 2026-08-08"
          },
          {
            "id": "fill-passport",
            "title": "Fill the project passport",
            "action": "Replace every `<PLACEHOLDER>` in `.ai-factory/DESCRIPTION.md`.",
            "evidence": "packages/core/templates/shared/DESCRIPTION.template.md"
          },
          {
            "id": "prove-rules-not-inert",
            "title": "Prove the rules are not inert on your layout",
            "action": "`bash scripts/check-rule-globs.sh`, then `bash scripts/check-fences-fire.sh` to see a rule go RED on planted input. On a brand-new skeleton with no source files yet this FAILS by design — every rule matches zero files. That is the expected first run; re-run it once your first `src/` files exist.",
            "evidence": "scripts/check-rule-globs.sh; scripts/check-fences-fire.sh"
          },
          {
            "id": "read-tier-home",
            "title": "Read the tier + degradation SSOT",
            "action": "Open `.ai-factory/tier-home.md`. It owns the Tier 0/1/2 routing criteria and the capability-absence degradation matrix — what still works when there is no aif runtime, no executor-tier model, or a non-Claude-Code harness. AGENTS.md only points here; the criteria live in this one file.",
            "evidence": "packages/core/templates/shared/tier-home.md §2, §3"
          },
          {
            "id": "arch-one-idea",
            "title": "Take one idea through `/arch`",
            "action": "Invoke `/arch <topic>` — the external design contour turns a raw idea into a reviewed design plus a routed handoff. On a harness without skills, read `.claude/skills/arch/SKILL.md` and follow it by hand.",
            "evidence": ".claude/skills/arch/SKILL.md (shipped at env+; verified in a temp consumer 2026-08-08)"
          }
        ]
      },
      "factory": {
        "goal": "env + one task driven through the pipeline",
        "profileFlag": "--profile factory (legacy equivalents: --with-aif-suite, --all)",
        "steps": [
          {
            "id": "install",
            "title": "Install at factory depth",
            "action": "`bash <getff>/install.sh <stack> --profile factory`. Pick this ONLY if this machine runs the aif-handoff operator runtime — the factory payload dead-ends without it.",
            "evidence": "install.sh; INSTALL-FOR-AI.md «Install depth profiles»"
          },
          {
            "id": "verify-payload",
            "title": "Verify the payload landed",
            "action": "`ls .claude/skills/` — on top of env you should see `.claude/skills/dispatcher/`, `.claude/skills/aif-doctor/`, `.claude/skills/harvest/`, `.claude/skills/story/` and `.claude/skills/claude-glm-executor-handoff/`. `pipeline` and `night-mode` are NOT factory additions — they arrive with env+ and are already there.",
            "evidence": "setup.d/10-skills.sh; verified in a temp consumer 2026-08-08"
          },
          {
            "id": "fill-passport",
            "title": "Fill the project passport",
            "action": "Replace every `<PLACEHOLDER>` in `.ai-factory/DESCRIPTION.md`.",
            "evidence": "packages/core/templates/shared/DESCRIPTION.template.md"
          },
          {
            "id": "prove-rules-not-inert",
            "title": "Prove the rules are not inert on your layout",
            "action": "`bash scripts/check-rule-globs.sh`, then `bash scripts/check-fences-fire.sh` to see a rule go RED on planted input. On a brand-new skeleton with no source files yet this FAILS by design — every rule matches zero files. That is the expected first run; re-run it once your first `src/` files exist.",
            "evidence": "scripts/check-rule-globs.sh; scripts/check-fences-fire.sh"
          },
          {
            "id": "read-tier-home",
            "title": "Read the tier + degradation SSOT",
            "action": "Open `.ai-factory/tier-home.md` — it decides which tier a task routes to, and what degrades when a capability is absent.",
            "evidence": "packages/core/templates/shared/tier-home.md §2, §3"
          },
          {
            "id": "write-a-kickoff",
            "title": "Write your first kickoff",
            "action": "Create `.ai-factory/orchestrator-prompts/<work-item>/kickoff.md`: a `Type:` line (`fix` / `research` / `feature`), the goal, and — if the work splits into parallel sub-steps — a `## §1 Sub-wave` section with one table row per sub-step.",
            "evidence": "setup.d/30-templates.sh (creates .ai-factory/orchestrator-prompts/)"
          },
          {
            "id": "run-pipeline",
            "title": "Ask the pipeline what to start next",
            "action": "Invoke `/pipeline`. It reads your kickoffs plus `.ai-factory/orchestrator-prompts/plan.md` (created on first run), ranks them, and emits a launch table. An empty backlog reports «nothing queued» — that is normal, not an error.",
            "evidence": ".claude/skills/pipeline/SKILL.md (shipped at env+; verified in a temp consumer 2026-08-08)"
          },
          {
            "id": "dispatch-one",
            "title": "Dispatch the top row and read the result",
            "action": "Dispatch the launch table's top row, then bring the finished branch back with `/harvest`. If a task stalls or the runtime misbehaves, `/aif-doctor` is the diagnostic entry point.",
            "evidence": ".claude/skills/harvest/SKILL.md; .claude/skills/aif-doctor/SKILL.md (shipped at factory)"
          }
        ]
      }
    }
  },
  "rosters": {
    "source": "docs/site/reference/",
    "families": {
      "A": {
        "familyName": "installer layers",
        "path": "docs/site/reference/A.json",
        "members": 16
      },
      "B": {
        "familyName": "skills",
        "path": "docs/site/reference/B.json",
        "members": 18
      },
      "C": {
        "familyName": "agents",
        "path": "docs/site/reference/C.json",
        "members": 12
      },
      "D": {
        "familyName": "hooks",
        "path": "docs/site/reference/D.json",
        "members": 25
      },
      "E": {
        "familyName": "templates",
        "path": "docs/site/reference/E.json",
        "members": 38
      },
      "F1": {
        "familyName": "rules",
        "path": "docs/site/reference/F1.json",
        "members": 29
      },
      "F2": {
        "familyName": "generated rules",
        "path": "docs/site/reference/F2.json",
        "members": 26
      },
      "F3": {
        "familyName": "scripts",
        "path": "docs/site/reference/F3.json",
        "members": 28
      },
      "G": {
        "familyName": "packages",
        "path": "docs/site/reference/G.json",
        "members": 8
      },
      "H": {
        "familyName": "bridge CLI",
        "path": "docs/site/reference/H.json",
        "members": 8
      },
      "I": {
        "familyName": "plugin",
        "path": "docs/site/reference/I.json",
        "members": 13
      }
    }
  },
  "counts": {
    "principles": {
      "source": "packages/core/principles/",
      "predicate": "files matching /\\.test\\.ts$/",
      "count": 50
    },
    "priorArtRows": {
      "source": "docs/meta-factory/prior-art-evaluations.md",
      "predicate": "entry-table rows matching /^\\| \\d+ \\| /",
      "count": 286
    },
    "researchPatches": {
      "source": "docs/meta-factory/research-patches/",
      "predicate": "files matching /^2026-.*\\.md$/",
      "count": 273
    },
    "specs": {
      "source": "docs/superpowers/specs/",
      "predicate": "files matching /^2026-.*\\.md$/",
      "count": 87
    },
    "academicSources": {
      "source": [
        "docs/",
        "skills/getff/references/"
      ],
      "predicate": "unique http(s) URLs on doi.org or arxiv.org",
      "count": 44
    }
  },
  "enforcementOutcomes": {
    "source": [
      "AGENTS.md:78",
      "AGENTS.md:98",
      "packages/core/composition/demo/root-agents-demo.ts:133"
    ],
    "nodes": {
      "no-direct-env-var": {
        "cargo-clippy-toml": "rendered",
        "npm-eslint-declarative": "FF7001",
        "astgrep-python-yaml": "FF7001",
        "ruff-tidy-imports-toml": "FF7001"
      },
      "no-direct-process-env": {
        "cargo-clippy-toml": "FF7001",
        "npm-eslint-declarative": "rendered",
        "astgrep-python-yaml": "FF7002",
        "ruff-tidy-imports-toml": "FF7002"
      },
      "no-datetime-now": {
        "cargo-clippy-toml": "FF7001",
        "npm-eslint-declarative": "FF7002",
        "astgrep-python-yaml": "rendered",
        "ruff-tidy-imports-toml": "FF7001"
      }
    }
  }
}
