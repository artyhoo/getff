---
# Per-stack deps-hash sentinels (DH-S1 multistack, kickoff #1016). One per stack; each is
# stamped by /tool-bootstrapping on first run. The UserPromptSubmit hook reads these and
# warns per-stack when a baseline is <pending> (unbaselined) or drifted (real change).
deps-hash-npm: <pending — populated on first tool-bootstrap run>
deps-hash-python: <pending — populated on first tool-bootstrap run>
deps-hash-cargo: <pending — populated on first tool-bootstrap run>
# Legacy bare deps-hash: kept for backward-compat with consumers installed before DH-S1
# (read as the npm slot if deps-hash-npm: is absent). Safe to leave as <pending>.
deps-hash: <pending — populated on first tool-bootstrap run>
last-bootstrap: <YYYY-MM-DD>
aif-version: <e.g. 2.1.0>
---

<!-- The installer seeds this file at .ai-factory/tool-decisions.md with the deps-hash
     sentinels above; commit it. Run /tool-bootstrapping to populate the first bootstrap
     (stamps the real per-stack deps-hash-N values); the UserPromptSubmit hook then tracks
     deps-hash changes per stack automatically (all three stacks: JS/python/cargo). -->

## Accepted

| Tool       | Type | Accepted    | Rationale                                                                                                                            |
| ---------- | ---- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| context7   | MCP  | recommended | strongly recommended for doc lookup (rule 2); NOT auto-installed — install if absent, loop degrades to WebSearch/DeepWiki without it |
| github-mcp | MCP  | YYYY-MM-DD  | Replace with actual rationale: which project workflow requires this tool                                                             |

## Rejected

| Tool       | Type | Rejected | Reason | Re-eval trigger |
| ---------- | ---- | -------- | ------ | --------------- |
| _none yet_ | —    | —        | —      | —               |

## Pending review

<!-- Add tools proposed by AIF /aif but not yet decided, or tools due for re-evaluation. -->
<!-- Format: - <tool-name>: <reason for pending> -->
